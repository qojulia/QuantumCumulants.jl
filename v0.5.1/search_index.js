var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/weighted_phase_invariance/#Weighted-Phase-Invariance","page":"Weighted Phase Invariance","text":"Many quantum optical systems possess a U(1) phase symmetry: the Hamiltonian is invariant under a rightarrow a e^itheta for every mode, which implies that every average carrying a net phase vanishes. The Superradiant Laser example exploits this to discard all phase-carrying averages with a filter_func, dramatically shrinking the set of equations.\n\nSometimes the conserved quantity is not the bare excitation number but a weighted excitation number. Consider three bosonic modes a, b and f coupled by a non-degenerate down-conversion process in which one a photon splits into one b photon and one f photon,\n\nH = Delta_a a^dagger a + Delta_b b^dagger b + Delta_f f^dagger f + g(a^dagger b f + a b^dagger f^dagger)\n\nEnergy conservation of the interaction ties the mode frequencies together, and the dynamics conserves the weighted excitation number\n\n2 a^dagger a + b^dagger b + f^dagger f \n\ni.e. mode a carries weight 2 while b and f carry weight 1. The symmetry is now a rightarrow a e^2 itheta, b rightarrow b e^itheta, f rightarrow f e^itheta, and an average survives only if its weights sum to zero. This is exactly the question raised in issue #237: how to write the filter when the modes carry different weights.\n\nWe start by loading the packages.\n\nusing QuantumCumulants\nusing ModelingToolkitBase, OrdinaryDiffEqTsit5\nusing Plots\n\nEach mode lives on its own Fock space. We keep track of which subspace an operator acts on through its space index (the second argument of the operator constructor), since the weight is attached to the mode, not to the operator type.\n\nha = FockSpace(:a)\nhb = FockSpace(:b)\nhf = FockSpace(:f)\nh = ha ⊗ hb ⊗ hf\n\n@qnumbers a::Destroy(h, 1) b::Destroy(h, 2) f::Destroy(h, 3)\n@variables Δa Δb Δf g κa κb κf\n\nH = Δa * a' * a + Δb * b' * b + Δf * f' * f + g * (a' * b * f + a * b' * f')\n\nJ = [a, b, f] # photon loss from each mode\nrates = [κa, κb, κf]\nnothing # hide\n\nWe now build the filter. The phase function varphi assigns to each operator its weight under the U(1) rotation. The only change compared to the unweighted case is that an annihilation or creation operator contributes mp w, where w is the weight of the mode it acts on. We read the mode off the operator's space_index and look the weight up in a dictionary.\n\nimport QuantumCumulants.SecondQuantizedAlgebra as SQA\n\nweights = Dict(1 => 2, 2 => 1, 3 => 1) # space_index => U(1) weight\n\nφ(x) = 0\nφ(x::SQA.Destroy) = -weights[x.space_index]\nφ(x::SQA.Create) = weights[x.space_index]\nfunction φ(q::SQA.QAdd) # walk operator-product expression\n    for (term, _) in q.arguments\n        p = 0\n        for op in term.ops\n            p += φ(op)\n        end\n        return p\n    end\n    return 0\nend\nfunction φ(avg)\n    SQA.is_average(avg) || return 0\n    return φ(SQA.undo_average(avg))\nend\nphase_invariant(x) = iszero(φ(x))\n\nOnly the weights' ratios matter, so Dict(1 => 4, 2 => 2, 3 => 2) would give the same filter. We can check that the interaction term and the relevant correlators are kept while single-mode coherences are discarded.\n\nφ(average(a' * b * f)), φ(average(a)), φ(average(a' * b))\n\nThe interaction a^dagger b f involves three operators, so a closed and non-trivial description requires a third-order cumulant expansion. We derive the equations for the mode populations and complete the system with the filter.\n\nops = [a' * a, b' * b, f' * f]\neqs = meanfield(ops, H, J; rates = rates, order = 3)\neqs_c = complete(eqs; filter_func = phase_invariant)\nnothing # hide\n\nThe filter keeps only the weighted-phase-invariant averages. Without it the completion is far larger.\n\nlength(eqs_c), length(complete(eqs; order = 3))\n\nFinally we evolve the system. We start with four photons in mode a and watch them convert into pairs of b and f photons before everything decays.\n\n@named sys = System(eqs_c)\nsys = complete(sys)\n\nu0 = zeros(ComplexF64, length(eqs_c))\nu0[1] = 4.0 # ⟨a'a⟩(0) = 4, all other averages zero\nps = [Δa => 0.0, Δb => 0.0, Δf => 0.0, g => 1.0, κa => 0.1, κb => 0.2, κf => 0.2]\n\nprob = ODEProblem(sys, merge(initial_values(eqs_c, u0), Dict(ps)), (0.0, 30.0))\nsol = solve(prob, Tsit5())\nnothing # hide\n\nWe extract the populations and plot them. The conversion of one a photon into a b and an f photon keeps langle b^dagger b rangle and langle f^dagger f rangle equal at all times.\n\nna = real.(get_solution(sol, a' * a, eqs_c).(sol.t))\nnb = real.(get_solution(sol, b' * b, eqs_c).(sol.t))\nnf = real.(get_solution(sol, f' * f, eqs_c).(sol.t))\n\nplot(sol.t, na; label = \"⟨a'a⟩\", xlabel = \"t\", ylabel = \"photon number\")\nplot!(sol.t, nb; label = \"⟨b'b⟩\")\nplot!(sol.t, nf; label = \"⟨f'f⟩\", linestyle = :dash)\n\n\n\nThis page was generated using Literate.jl.","title":"Weighted Phase Invariance"},{"category":"section","location":"noise/#Noise-and-measurement-backaction","page":"Noise & measurement backaction","text":"The mean-field pipeline derives the unconditional dynamics: the Lindblad master equation averaged over all measurement outcomes, where the white-noise term drops out (see the theory section). When a decay channel is continuously monitored, that is no longer the whole story. The measurement record feeds back on the state (measurement backaction), and the conditional evolution is a stochastic master equation driven by the measurement noise.\n\nQuantumCumulants derives the cumulant-expanded equations for this conditional evolution directly. The result is a NoiseMeanfieldEquations: each equation carries a deterministic drift and a noise drift proportional to a Wiener increment dW.","title":"Noise & measurement backaction"},{"category":"section","location":"noise/#Requesting-backaction:-the-efficiencies-keyword","page":"Noise & measurement backaction","text":"Noise is opt-in. Pass efficiencies to meanfield, one entry per collapse operator, giving the detection efficiency of that channel. An efficiency of 0 means the channel is dissipative but not monitored (it contributes to the drift only); a non-zero efficiency adds the corresponding measurement backaction:\n\nusing QuantumCumulants\n\n# ... build H and the jump operators J ...\nJ            = [a, σ(1, 2, j), σ(2, 1, j), σ(2, 2, j)]\nrates        = [κ, γ, η, χ]\nefficiencies = [ξ, 0, 0, 0]   # only the cavity-output channel is measured\n\neqs = meanfield(ops, H, J; rates=rates, efficiencies=efficiencies, order=2)\n\nBecause efficiencies is supplied, eqs is a NoiseMeanfieldEquations rather than a MeanfieldEquations. Each averaged equation now has the schematic form\n\ndlangle mathcalOrangle = underbrace(dots)_textdriftdt + underbrace(dots)_textnoise driftdW\n\nwhere the noise drift is the measurement-conditioned term. The choice of jump operator sets the measurement: for example a cavity output hat a e^iomega_l t realises heterodyne detection at local-oscillator frequency omega_l.","title":"Requesting backaction: the efficiencies keyword"},{"category":"section","location":"noise/#Closing,-scaling,-evaluating","page":"Noise & measurement backaction","text":"complete, scale, and evaluate operate on a NoiseMeanfieldEquations exactly as they do on a deterministic one; the noise column is carried through unchanged:\n\neqs_c      = complete(eqs)\nscaled_eqs = scale(eqs_c)   # permutation-symmetric ensemble","title":"Closing, scaling, evaluating"},{"category":"section","location":"noise/#Solving:-deterministic-vs.-stochastic","page":"Noise & measurement backaction","text":"The System built from a NoiseMeanfieldEquations carries both columns: solving it as an SDEProblem integrates drift + noise, producing a single conditional trajectory for one measurement realisation. It needs a noise process; for white measurement noise this is a Wiener process from StochasticDiffEq.jl.\n\nFor the unconditional (ensemble-mean) trajectory you want the drift only. Convert to a deterministic MeanfieldEquations(scaled_eqs) first; the constructor strips the noise drift, and the resulting System integrates as an ordinary ODEProblem.\n\nIn both cases, build the problem from mtkcompile(System(...)) and pass the parameters through parameter_map, which keeps only the keys that survive compilation (stripping the noise can drop a parameter such as ξ that appeared only in the noise term):\n\nusing ModelingToolkitBase, OrdinaryDiffEqTsit5, StochasticDiffEqLowOrder, DiffEqNoiseProcess\n\nu0 = zeros(ComplexF64, length(scaled_eqs))\n\n# Unconditional (drift-only) evolution:\nsys_det  = mtkcompile(System(MeanfieldEquations(scaled_eqs); name=:sys_det))\ndict_det = parameter_map(sys_det, merge(Dict(unknowns(sys_det) .=> u0), Dict(p .=> p0)))\nsol_det  = solve(ODEProblem(sys_det, dict_det, (0.0, T)), Tsit5())\n\n# A single conditional trajectory:\nsys_st   = mtkcompile(System(scaled_eqs; name=:sys_st))\ndict_st  = parameter_map(sys_st, merge(Dict(unknowns(sys_st) .=> u0), Dict(p .=> p0)))\nnoise    = RealWienerProcess(0.0, 0.0)\nsol_traj = solve(SDEProblem(sys_st, dict_st, (0.0, T); noise=noise), EM(); dt=T/2e5)\n\nAveraging many conditional trajectories (via an EnsembleProblem) reproduces the deterministic result, while individual trajectories show the backaction. get_solution extracts averages from either kind of solution.\n\nA complete, runnable walkthrough, including the ensemble averaging and plots, is the heterodyne detection example.","title":"Solving: deterministic vs. stochastic"},{"category":"section","location":"noise/#Retrodiction","page":"Noise & measurement backaction","text":"Continuous monitoring also supports retrodiction: using the measurement record to estimate the state at an earlier time (the smoothing / past-quantum-state problem). This is the backward evolution, requested with direction=Backward():\n\neqs_backward = meanfield(ops, H, J; rates=rates, efficiencies=efficiencies,\n                         direction=Backward(), order=2)\n\nBackward() flips the sign of the commutator and uses the adjoint Lindblad recycling with the trace-preserving term, producing the backward (effect-operator) equations. The EvolutionDirection tag is stored on the equations and threaded through System, so the rest of the workflow (complete, scale, solve) is unchanged. Combining a forward filter with a backward filter yields the smoothed estimate.","title":"Retrodiction"},{"category":"section","location":"examples/driven_dissipative_ising/#Driven-dissipative-transverse-field-Ising-chain","page":"Driven-dissipative transverse-field Ising chain","text":"In this example we use Pauli operators to study a one-dimensional transverse-field Ising chain that is also subject to dissipation. Pauli operators are the natural language for two-level systems (qubits, spin-1/2), and because they are Hermitian the resulting cumulant equations carry real coefficients.\n\nThe Hamiltonian of a chain of N spins reads\n\nH = -J sum_i=1^N-1 sigma^z_i sigma^z_i+1 - h_x sum_i=1^N sigma^x_i\n\nwhere J is the nearest-neighbour Ising coupling and h_x is the strength of a transverse field. On top of the coherent dynamics, each spin decays towards its ground state with rate gamma through the collapse operator\n\nsigma^-_i = tfrac12left(sigma^x_i - isigma^y_iright) = granglelangle e_i\n\nThe competition between the entangling Ising term, the transverse drive and the local dissipation builds up correlations between the spins. A plain mean-field treatment (first-order cumulants) factorises all of these correlations and gives qualitatively wrong dynamics. The cumulant expansion is a systematically improvable hierarchy, though: by going to higher order we keep ever larger groups of correlated spins and converge towards the exact result. We will see this explicitly by comparing orders 1, 2 and 3 against the exact master equation for a chain small enough to solve directly.\n\nAs always, we start by loading the packages and setting up the operators.\n\nusing QuantumCumulants\nusing ModelingToolkitBase\nusing ModelingToolkitBase: unknowns\nusing OrdinaryDiffEqLowOrderRK\nusing QuantumOptics\nusing Plots\n\nN = 6 # number of spins in the chain\n\nEach site lives in its own PauliSpace; the full Hilbert space is their tensor product. The last argument of Pauli selects the site, and the axis argument selects the component (1 = x, 2 = y, 3 = z).\n\nh = ⊗([PauliSpace(Symbol(:spin, i)) for i in 1:N]...)\nσx(i) = Pauli(h, :σ, 1, i)\nσy(i) = Pauli(h, :σ, 2, i)\nσz(i) = Pauli(h, :σ, 3, i)\nσm(i) = (σx(i) - 1im * σy(i)) / 2 # lowering operator |g><e| on site i\n\n@variables J hx γ\n\nH = -J * sum(σz(i) * σz(i + 1) for i in 1:(N - 1)) - hx * sum(σx(i) for i in 1:N)\nc_ops = [σm(i) for i in 1:N]\nnothing # hide\n\nWe derive the equations of motion for the magnetisation langlesigma^z_irangle of every spin and complete the set at a given cumulant order. order = 1 reproduces the bare mean-field equations; higher orders progressively add two-spin, three-spin, ... correlations.\n\nfunction magnetization_eqs(order)\n    eqs = meanfield([σz(i) for i in 1:N], H, c_ops; rates = [γ for i in 1:N], order = order)\n    complete!(eqs)\n    return eqs\nend\n\neqs = [magnetization_eqs(order) for order in 1:3]\nnothing # hide\n\nThe number of equations grows quickly with the cumulant order, since we track all correlations up to that many spins.\n\n[length(e) for e in eqs]\n\nTo solve numerically we build a System, choose the physical parameters, and set the initial state. We start with every spin pointing up, e e dots erangle. The initial averages are obtained directly from this product state with initial_values(eqs, ψ₀), which converts the QuantumOptics.jl state into the correct moments.\n\np = Dict(J => 1.0, hx => 1.0, γ => 0.2)\nψ0 = tensor([spinup(SpinBasis(1 // 2)) for _ in 1:N]...)\n\nfunction solve_cumulant(eqs; tend = 20.0)\n    sys = mtkcompile(System(eqs; name = :sys))\n    u0 = initial_values(eqs, ψ0)\n    dict = parameter_map(sys, merge(Dict(unknowns(sys) .=> u0), p))\n    # `eval_expression = true` compiles the right-hand side via `eval` instead of a\n    # RuntimeGeneratedFunction, which is noticeably faster to compile for the larger\n    # higher-order systems; `build_initializeprob = false` skips the (here redundant)\n    # initialization problem since `initial_values` already provides every state.\n    prob = ODEProblem(sys, dict, (0.0, tend); eval_expression = true, build_initializeprob = false)\n    sol = solve(prob, RK4(), saveat = 0.1)\n    mz = [real.(get_solution(sol, σz(i), eqs).(sol.t)) for i in 1:N]\n    return sol.t, mz\nend\n\nsols = [solve_cumulant(e) for e in eqs]\nnothing # hide\n\nFor a chain of only six spins we can also integrate the full Lindblad master equation directly in QuantumOptics.jl and use it as the ground truth. We map the symbolic operators onto numeric ones acting on a tensor product of spin-1/2 bases with to_numeric.\n\nb = tensor([SpinBasis(1 // 2) for _ in 1:N]...)\nHn = to_numeric(-p[J] * sum(σz(i) * σz(i + 1) for i in 1:(N - 1)) - p[hx] * sum(σx(i) for i in 1:N), b)\nJn = [to_numeric(σm(i), b) for i in 1:N]\n\ntout, ρt = timeevolution.master(collect(0:0.1:20.0), ψ0, Hn, Jn; rates = [p[γ] for _ in 1:N])\nmz_exact = [real.(expect.(Ref(to_numeric(σz(i), b)), ρt)) for i in 1:N]\nnothing # hide\n\nLet us compare the magnetisation of the central spin across cumulant orders.\n\nc = N ÷ 2\ncolors = [:firebrick, :steelblue, :seagreen]\nplt = plot(\n    tout,\n    mz_exact[c];\n    label = \"master equation\",\n    lw = 3,\n    color = :black,\n    xlabel = \"γt\",\n    ylabel = \"⟨σz⟩ (central spin)\",\n    legend = :topright,\n)\nfor order in 1:3\n    t, mz = sols[order]\n    plot!(plt, t, mz[c]; label = \"cumulants (order $order)\", lw = 2, color = colors[order])\nend\nplt\n\nThe story is the convergence of the hierarchy. Mean-field (order 1) shows large, persistent oscillations: by factorising the spin-spin correlations it badly misjudges how the Ising coupling damps the collective dynamics. Order 2 already removes most of the spurious oscillation, and order 3 sits essentially on top of the exact master equation. This is exactly the regime where higher cumulant orders pay off: strong coupling (J sim h_x) with only weak dissipation (gamma ll J) builds up correlations that low orders cannot capture.\n\nFinally, we visualise the full space-time dynamics of the chain from the third-order solution. Each spin starts up (bright) and relaxes towards the steady state, with the transverse field and Ising coupling imprinting a transient pattern along the chain.\n\nt3, mz3 = sols[3]\nheatmap(\n    t3,\n    1:N,\n    reduce(hcat, mz3)';\n    xlabel = \"γt\",\n    ylabel = \"site i\",\n    colorbar_title = \"⟨σz_i⟩\",\n    color = :balance,\n    clims = (-1, 1),\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Driven-dissipative transverse-field Ising chain"},{"category":"section","location":"examples/waveguide/#Waveguide-Energy-Transfer","page":"Waveguide Energy Transfer","text":"In this example, we investigate the collective behaviour of atomic ensembles coupled to a waveguide. The waveguide-mediated dipole-dipole Hamiltonian is\n\n    H = sum_i neq j^N Omega_ij^1mathrmD sigma^+_i sigma^-_j\n\nwith the coupling rate Omega^1mathrmD_ij. The incoherent part describing the dissipative processes is accounted for by the Lindblad term\n\n    mathcalLrho=frac12sum_ij^N Gamma^1mathrmD_ij (2 sigma^-_i rho sigma^+_j-sigma^+_i sigma^-_j rho - rho sigma^+_i sigma^-_j)\n\nwith the collective fiber-mediated decay rates Gamma^1mathrmD_ij. For M atomic ensembles, where each of the N_i atoms within an ensemble behaves identically, we can use M spin-N_i2 systems to describe them. This means\n\n    H = sum_i neq j^M Omega_ij^1mathrmD S^+_i S^-_j hspace075cm mathrmand hspace075cm mathcalLrho=frac12sum_ij^M Gamma^1mathrmD_ij (2 S^-_i rho S^+_j-S^+_i S^-_j rho - rho S^+_i S^-_j)\n\nwith S^pm_i = S^x pm i S^y = sum_k=1^N_i sigma^pm_k, where S^xyz_i = frac12 sum_k=1^N_i sigma^xyz_k.\n\nAfter loading the needed packages we define the Hilbert space and spin operators of the M atomic ensembles coupled to the waveguide. We also define the numerical Parameters and utilize that Omega_ij = Omega_ji and Gamma_ij = Gamma_ji. Due to this the derived equations are simplified further.\n\nusing QuantumCumulants\nusing ModelingToolkitBase\nusing OrdinaryDiffEqTsit5\nusing QuantumOptics\nusing Plots\n\nM_p = 2 # number of pumped spins\nM_np = 2 # number of non-pumped spins\nM = M_p + M_np\n\nh_spin(i) = SpinSpace(Symbol(\"spin_$(i)\")) # Hilbert space\nh = tensor([h_spin(i) for i in 1:M]...)\n\nSx(i) = Spin(h, Symbol(\"S$(i)\"), 1, i) # Operators\n\nSy(i) = Spin(h, Symbol(\"S$(i)\"), 2, i)\nSz(i) = Spin(h, Symbol(\"S$(i)\"), 3, i)\n\nSm(i) = (Sx(i) - 1im * Sy(i))\nSp(i) = (Sx(i) + 1im * Sy(i))\n\n# Note: QuantumCumulants re-exports symbols `Ω` and `Γ`, so we use `Ωp`/`Γp` here.\nfunction _make_param(name::Symbol) # Parameter constructor\n    vars = ModelingToolkitBase.@variables $name\n    return vars[1]\nend\nconst _Ω_cache = Dict{Tuple{Int, Int}, Any}()\nconst _Γ_cache = Dict{Tuple{Int, Int}, Any}()\nfunction Ωp(i, j) # Parameter Ω_{i,j} symmetric\n    key = i > j ? (j, i) : (i, j)\n    return get!(_Ω_cache, key) do\n        _make_param(Symbol(\"Ω_$(key[1])_$(key[2])\"))\n    end\nend\nfunction Γp(i, j) # Parameter Γ_{i,j} symmetric\n    key = i > j ? (j, i) : (i, j)\n    return get!(_Γ_cache, key) do\n        _make_param(Symbol(\"Γ_$(key[1])_$(key[2])\"))\n    end\nend\nnothing # hide\n\nWith the symbolic operators and Parameters we define the Hamiltonian and the collective jump operators with the corresponding rates. For rates written in matrix form the program automatically assumes collective dissipation according to\n\n    mathcalLrho=frac12sum_ij^M R_ij (2 J_i rho J^+_j-J^+_i rho J_j - rho J^+_i J_j)\n\nwith the jump operator J_i and the corresponding decay rate matrix R_ij. This implementation is similar to the one in QuantumOptics.jl for collective dissipation.\n\nH = sum((i ≠ j) * Ωp(i, j) * Sp(i) * Sm(j) for i in 1:M for j in 1:M) # Hamiltonian\n\nJ = [Sm(c1) for c1 in 1:M] # Jump operators and rate matrix\nrates = [Γp(c1, c2) for c1 in 1:M, c2 in 1:M]\nnothing # hide\n\nNow we create a complete list of operators, which we use to derive the second-order equations. This is considerable faster than to automatically complete the equations. We only show one of the 90 rather lengthy equations.\n\nS(i) = [Sx(i), Sy(i), Sz(i)] # create list of operators\nSiSi(i) = [Sx(i)Sx(i), Sx(i)Sy(i), Sx(i)Sz(i), Sy(i)Sy(i), Sy(i)Sz(i), Sz(i)Sz(i)]\nops = [];\n[push!(ops, S(i)...) for i in 1:M]\n[push!(ops, SiSi(i)...) for i in 1:M]\nfor i in 1:M, j in i:M\n    if i ≠ j\n        for α in 1:3, β in 1:3\n            push!(ops, S(i)[α] * S(j)[β])\n        end\n    end\nend\nprintln(\"Number of equations = $(length(ops))\")\n\neqs = meanfield(ops, H, J; rates = rates, order = 2) # derive equations\nmeanfield(Sz(1), H, J; rates = rates, order = 2)\nnothing # hide\n\nbeginalign\nfracddt langle S1^zrangle  = -10 langle S1^y  S4^yrangle  Gamma_1_4 -10 langle S1^x  S3^xrangle  Gamma_1_3 -10 langle S1^y  S1^yrangle  Gamma_1_1 -10 langle S1^y  S2^yrangle  Gamma_1_2 -10 langle S1^zrangle  Gamma_1_1 -10 langle S1^x  S1^xrangle  Gamma_1_1 + 2 langle S1^y  S2^xrangle  Omega_1_2 + 2 langle S1^y  S4^xrangle  Omega_1_4 -10 langle S1^x  S2^xrangle  Gamma_1_2 -10 langle S1^x  S4^xrangle  Gamma_1_4 -2 langle S1^x  S3^yrangle  Omega_1_3 -2 langle S1^x  S4^yrangle  Omega_1_4 -10 langle S1^y  S3^yrangle  Gamma_1_3 + 2 langle S1^y  S3^xrangle  Omega_1_3 -2 langle S1^x  S2^yrangle  Omega_1_2\nendalign\n\nWe define the numerical system Parameters and the initial state. For spin operators the initial state is unfortunately not fully trivial with all zeros. Therefore, we use the numeric conversion which allows us to define the quantum state in QuantumOptics.jl and convert it to the correct initial average values.\n\nΓ_ = 1.0  # Γ1D # System Parameters\nΩ_ = Γ_ / 2 # Ω1D\n\nx_p = [i / M_p for i in 0:(M_p - 1)] * 2π # positions\nx_np = [i / M_np for i in 0:(M_np - 1)] * 2π\nx_ls = [x_p; x_np]\n\nΓij(i, j) = cos(abs(x_ls[i] - x_ls[j]))\nΓ_ls = [Γij(i, j) for i in 1:M for j in 1:M] * Γ_\nΩij(i, j) = sin(abs(x_ls[i] - x_ls[j]))\nΩ_ls = [Ωij(i, j) for i in 1:M for j in 1:M] * Ω_\n\nps = [[Γp(i, j) for i in 1:M for j in 1:M]; [Ωp(i, j) for i in 1:M for j in 1:M];]\np0 = [Γ_ls; Ω_ls;]\nnothing # hide\n\nNote that the function coherentspinstate can only reliably produce states for spins with a length of about 10^4, for spindown or spinup there is no restriction. Also, we created the function LazyKet, which allows you to define product states without calculating the tensor product directly. This makes it possible to define initial (product) states for very large quantum systems. The function initial_values(eqs, ψ) calculates the initial values for a set of equations eqs with the initial quantum state psi.\n\nN_p = 8000 # number of pumped atoms\nN_np = 2000 # number of non-pumped atoms\nbs1 = SpinBasis(N_p / 2 / M_p) # spin basis for pumped atoms\nbs2 = SpinBasis(N_np / 2 / M_np) # spin basis for non-pumped atoms\nb = tensor([bs1 for i in 1:M_p]..., [bs2 for i in 1:M_np]...)\n\nΘ = π / 3 # corresponds to an excitation of 75%\nψ_p = coherentspinstate(bs1, Θ, 0.0) # initially pumped atoms\nψ_np = spindown(bs2) # initially non-pumped atoms\nψ0 = LazyKet(b, ([ψ_p for i in 1:M_p]..., [ψ_np for i in 1:M_np]...))\nu0 = initial_values(eqs, ψ0)  # initial state\nnothing # hide\n\nFinally, we create the ODE-problem, calculate the dynamics and plot the results. We see a fast transfer of the population from the initially excited to the other atomic ensemble.\n\nSince ps/p0 carry diagonal entries Ω_i_i/Γ_i_i that the Hamiltonian never references (it sums over i ≠ j), we filter them out with parameter_map(sys, …) because MTK v10 rejects superfluous parameter assignments.\n\nsys = System(eqs; name = :sys)\nsys = mtkcompile(sys)\ndict = parameter_map(sys, merge(Dict(unknowns(sys) .=> u0), Dict(ps .=> p0)))\nprob = ODEProblem(sys, dict, (0.0, 8.0e-3))\nsol = solve(prob, Tsit5(), abstol = 1.0e-6, reltol = 1.0e-6)\n\nt = sol.t * 1.0e3\nSz_t(i) = real.(get_solution(sol, Sz(i), eqs)(sol.t))\nN_ls = [[N_p for i in 1:M_p] ./ M_p; [N_np for i in 1:M_np] ./ M_np]\nσ22_t(i) = Sz_t(i) / N_ls[i] .+ 1 / 2\nσ22_p_t = sum(σ22_t(i) for i in 1:M_p) ./ M_p\nσ22_np_t = sum(σ22_t(i) for i in (M_p + 1):(M_p + M_np)) ./ M_np\n\np = plot(xlabel = \"Γ1D t ⋅ 10³\", ylabel = \"⟨σ²²⟩\")\nplot!(p, t, σ22_p_t, label = \"pumped\")\nplot!(p, t, σ22_np_t, label = \"non-pumped\")\nplot(p, size = (500, 300))","title":"Waveguide Energy Transfer"},{"category":"section","location":"examples/waveguide/#Package-versions","page":"Waveguide Energy Transfer","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status([\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\", \"QuantumOptics\"], mode = PKGMODE_MANIFEST)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/ramsey_spectroscopy/#Ramsey-Spectroscopy","page":"Ramsey Spectroscopy","text":"A typical problem in quantum optics is the interrogation of an atom with an external driving field. In this brief example we apply the Ramsey interferometry on a single two-level atom. The distinctiveness of this method is the time dependent laser field. In particular, two short pi2-pulses are employed with a free evolution between them. This system is described by the simple Hamiltonian\n\nH = - Delta sigma^22 +  Omega(t) (sigma^21 + sigma^12)\n\nwhere Delta = omega_l - omega_a is the detuning between the laser and atomic resonance frequency, and Omega(t) describes the time dependent driving field. Additionally we include atomic decay and dephasing, with the rates Gamma and nu, respectively.\n\nWe start by loading the packages.\n\nusing QuantumCumulants\nusing OrdinaryDiffEqTsit5, OrdinaryDiffEqLowOrderRK, ModelingToolkitBase\nusing Plots\n\nBeside defining the symbolic parameters we additionally need to register a time dependent external function f(t) for our driving laser. To this end we first need to define the independent variable t of our time evolution. Due to the registration, f(t) is treated as a parameter in the symbolic equations, but at the numerical evaluation we can assign every function to it.\n\n@variables Δ Ω Γ ν\n@register_symbolic f(t)\n\nAfter defining the Hilbert space and the operator of the two-level atom we construct the time dependent Hamiltonian as well as the jump operator list with the corresponding rates.\n\nh = NLevelSpace(:atom, 2) # Hilbert space\n\nσ(i, j) = Transition(h, :σ, i, j) # Operators\n\nSeed a meanfield call so we can reuse its MTK-aware iv t in the time-dependent Hamiltonian below.\n\neqs_seed = meanfield([σ(2, 2), σ(1, 2)], -Δ * σ(2, 2), [σ(1, 2), σ(2, 2)]; rates = [Γ, ν])\nt = eqs_seed.iv\n\nH = -Δ * σ(2, 2) + f(t) * Ω / 2 * (σ(1, 2) + σ(2, 1)) # Hamiltonian\n\nJ = [σ(1, 2), σ(2, 2)] # Jump operators & rates\nrates = [Γ, ν]\nnothing # hide\n\nThe two-level atom is completely described with the excited state population and the coherence. Therefore, we derive the equations for these two operators.\n\neqs = meanfield([σ(2, 2), σ(1, 2)], H, J; rates = rates, iv = t)\ncomplete!(eqs)\nnothing # hide\n\nbeginalign\nfracddt langle sigma^22rangle  = -10 Gamma langle sigma^22rangle  + frac12 i Omega fleft( t right) langle sigma^12rangle  + frac-12 i Omega fleft( t right) langle sigma^21rangle  \nfracddt langle sigma^12rangle  = frac-12 i Omega fleft( t right) -05 Gamma langle sigma^12rangle  -05 nu langle sigma^12rangle  + 1 i Delta langle sigma^12rangle  + 1 i Omega fleft( t right) langle sigma^22rangle\nendalign\n\nTo calculate the dynamic of the system we create a system of ordinary differential equations and define the numeric parameters with the time dependent function.\n\nsys = System(eqs; name = :sys)\nsys_c = mtkcompile(sys)\n\nParameter\n\nΓ_ = 1.0\nΩ_ = 500Γ_\nΔ_ = 0Γ_\nν_ = 0.2Γ_\n\ntp = π / 2Ω_ # π/2-pulse\ntf = 1 / 20Γ_ # free evolution without drive\n\nfunction f(t)\n    if t < tp || (t > tp + tf && t < 2tp + tf)\n        return 1\n    else\n        0\n    end\nend\n\nps = [Γ, Ω, Δ, ν]\np0 = [Γ_, Ω_, Δ_, ν_]\n\n\nu0 = zeros(ComplexF64, length(eqs)) # Initial state\nnothing # hide\n\nFinally, we calculate and plot the time evolution.\n\ndict = merge(Dict(ps .=> p0), Dict(unknowns(sys_c) .=> u0))\nprob = ODEProblem(sys_c, dict, (0.0, 2tp + tf))\nsol = solve(prob, Tsit5(), maxiters = 1.0e7)\n\nt_arr = sol.t # Plot time evolution\ns22 = real.(get_solution(sol, σ(2, 2), eqs).(t_arr))\nplot(t_arr, s22, xlabel = \"tΓ\", ylabel = \"⟨σ22⟩\", legend = false, size = (600, 300))\n\nScanning over the detuning for the excited state population leads to the well-known Ramsey fringes.\n\nΔ_ls = [-2000:4:2000;]Γ_\ns22_ls = zeros(length(Δ_ls))\n\nfor i in 1:length(Δ_ls)\n    dict = merge(Dict(ps .=> [Γ_; Ω_; Δ_ls[i]; ν_]), Dict(unknowns(sys_c) .=> u0))\n    prob_i = ODEProblem(sys_c, dict, (0.0, 2tp + tf))\n    sol_i = solve(prob_i, RK4(); adaptive = false, dt = 1.0e-5)\n    s22_ls[i] = real(get_solution(sol_i, σ(2, 2), eqs)(sol_i.t[end]))\nend\n\nplot(Δ_ls, s22_ls, xlabel = \"Δ/Γ\", ylabel = \"⟨σ22⟩\", legend = false, size = (600, 300))","title":"Ramsey Spectroscopy"},{"category":"section","location":"examples/ramsey_spectroscopy/#Package-versions","page":"Ramsey Spectroscopy","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status([\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\", \"OrdinaryDiffEqLowOrderRK\"], mode = PKGMODE_MANIFEST)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/single-atom-laser-spectrum/#Spectrum-of-a-single-atom-laser","page":"Spectrum of a single-atom laser","text":"In this example, we will compute the spectrum of the single-atom laser, which is also shown in the Tutorial. The Hamiltonian of the system is\n\nH = Delta a^dagger a + gleft(a^dagger sigma^ge + asigma^egright)\n\nwhere Delta = omega_mathrmc - omega_mathrma is the detuning between the cavity and the atomic resonance frequency, respectively. The cavity loses photons at a rate kappa, and the atom is subject to spontaneous emission with a rate gamma. Additionally, the atom is incoherently driven with a rate nu, such that it can provide lasing gain to the system. We start, by specifying this model.\n\nusing Latexify # hide\nset_default(double_linebreak = true) # hide\nusing QuantumCumulants\nusing ModelingToolkitBase, OrdinaryDiffEqLowOrderRK\nusing Plots\n\n@variables Δ g γ κ ν # Define parameters\n\nhf = FockSpace(:cavity) # Define hilbert space\nha = NLevelSpace(:atom, (:g, :e))\nh = hf ⊗ ha\n\na = Destroy(h, :a) # Define the fundamental operators\ns = Transition(h, :σ, :g, :e)\n\nH = Δ * a' * a + g * (a' * s + a * s') # Hamiltonian\n\nJ = [a, s, s'] # Collapse operators\nrates = [κ, γ, ν]\nnothing # hide\n\nThe first equation we want to derive is that for the average photon number langle a^dagger a rangle.\n\neq_n = meanfield(a' * a, H, J; rates = rates, order = 2) # Derive equation for average photon number\nnothing # hide\n\nThe remaining equations will be computed automatically using the complete function. However, we want to exclude terms such as langle a rangle since these are phase-dependent and therefore 0 in our phase-invariant system. To this end, we provide a custom filter function to complete. This function should return true, if the given average should be included, and false if it should be excluded (just like Julia's native filter function requires). We write a small function phi that computes the phase of an average, such that phi(a) = -1, phi(a^dagger) = 1, and phi(a^dagger a) = phi(a) + phi(a^dagger) = 0. Similarly, we want to have phi(sigma^eg)=1=-phi(sigma^ge), and phi(sigma^ee)=0. An average of an operator x is then said to be phase invariant if phi(x)=0.\n\nimport QuantumCumulants.SecondQuantizedAlgebra as SQA\n\nϕ(x) = 0 # Custom filter function -- include only phase-invariant terms\nϕ(::Destroy) = -1\nϕ(::Create) = 1\nfunction ϕ(t::Transition)\n    return if (t.i == 2 && t.j == 1)\n        1\n    elseif (t.i == 1 && t.j == 2)\n        -1\n    else\n        0\n    end\nend\nfunction ϕ(q::SQA.QAdd) # walk the operator-product expression\n    for (term, _) in q.arguments\n        p = 0\n        for op in term.ops\n            p += ϕ(op)\n        end\n        return p\n    end\n    return 0\nend\nfunction ϕ(avg)\n    SQA.is_average(avg) || return 0\n    return ϕ(SQA.undo_average(avg))\nend\nphase_invariant(x) = iszero(ϕ(x))\n\neqs = complete(eq_n; filter_func = phase_invariant) # Complete equations\n\nIn order to compute the spectrum, we first compute the correlation function g(tau) = langle a^dagger(t_0 + tau) a(t_0)rangle equiv langle a^dagger a_0rangle\n\nNote that the CorrelationFunction finds the equation for g(tau) and then completes the system of equations by using its own version of the complete function. Setting the keyword steady_state=true, we tell the function not to derive equations of motion for operators that do not depend on tau, but only on t_0 (if t_0 is in steady state, these values do not change with tau).\n\nc = CorrelationFunction(a', a, eqs; steady_state = true, filter_func = phase_invariant) # Correlation function\nnothing # hide\n\nlangle a^dagger a_0rangle\n\nAs we can see, there are only two equations necessary to obtain the correlation function:\n\nc.eqs\nnothing # hide\n\nbeginalign fracddt langle a^dagger a_0rangle = 10 i g langle sigma^eg a_0rangle + 10 i Delta langle a^dagger a_0rangle -05 kappa langle a^dagger a_0rangle \nfracddt langle sigma^eg a_0rangle = 10 i g langle a^dagger a_0rangle -05 gamma langle sigma^eg a_0rangle -05 nu langle sigma^eg a_0rangle -20 i langle sigma^eerangle g langle a^dagger a_0rangle\nendalign\n\nThe spectrum can now be computed by solving the above system of equations and then taking the Fourier transform, or by taking the Fourier transform directly, which is done by constructing an instance of the Spectrum type. Here, we will compare both approaches.\n\nIn any case, we need to compute the steady state of the system numerically.\n\nps = (Δ, g, γ, κ, ν)\nsys = mtkcompile(System(eqs; name = :sys))\np0 = (1.0, 1.5, 0.25, 1.0, 4.0)\nu0 = zeros(ComplexF64, length(eqs.states))\nprob = ODEProblem(sys, merge(initial_values(eqs, u0), Dict(ps .=> p0)), (0.0, 10.0))\nsol = solve(prob, RK4())  # Numerical solution\nnothing # hide\n\nNow, we can compute the time evolution of the correlation function similarly. Since the initial state of this system does not necessarily depend on all steady-state values, we can use the correlation_u0 function which automatically generates the correct initial state vector required. Similarly, we use correlation_p0 which generates the list of parameters including all needed steady-state values.\n\ncsys = mtkcompile(System(c; name = :csys)) # Time evolution of correlation function\nu0_c = correlation_u0(c, sol.u[end])\np0_c = correlation_p0(c, sol.u[end], collect(ps .=> p0))\nprob_c = ODEProblem(csys, merge(u0_c, Dict(p0_c)), (0.0, 500.0))\nsol_c = solve(prob_c, RK4(), save_idxs = 1)\nnothing # hide\n\nFinally, we borrow the FFT function from QuantumOptics.jl and compute the spectrum from the solution. Note that this requires an equidistant list of times, and we therefore interpolate the solution from the differential equation.\n\nτ = range(0.0, sol_c.t[end], length = 15001) # Interpolate solution\ncorr = sol_c.(τ)\n\n\nusing QuantumOptics.timecorrelations: correlation2spectrum\nω, s_fft = correlation2spectrum(τ, corr) # Compute spectrum\nnothing # hide\n\nNow, in order to compare we also compute the spectrum by constructing it directly from the correlation function and plot the results.\n\nS = Spectrum(c, ps) # Spectrum\ns_laplace = S(ω, sol.u[end], collect(p0))\nnothing # hide\n\nplot(ω, s_fft, label = \"Spectrum (FFT)\", xlabel = \"ω\")\nplot!(ω, s_laplace, label = \"Spectrum (Laplace)\")\nxlims!(-3, 3)\n\nAs expected, both methods yield exactly the same spectrum. The difference is just in the method used, with the Laplace transform having a computational advantage.","title":"Spectrum of a single-atom laser"},{"category":"section","location":"examples/single-atom-laser-spectrum/#Package-versions","page":"Spectrum of a single-atom laser","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status([\"QuantumCumulants\", \"OrdinaryDiffEqLowOrderRK\"], mode = PKGMODE_MANIFEST)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"correlation/#Correlation-functions-and-spectra","page":"Correlation functions and spectra","text":"Two quantities that are often of interest in a system are two-time correlation functions and spectral densities. Consider, for example, two operators a and b. Their two-time correlation function is given by\n\ng(ttau) = langle a(t+tau) b(t)rangle\n\nAs we can see, we need to take some care here since g depends on two different times. The spectral density corresponding to a and b is given by the Fourier transform,\n\nS(omega) = 2textReleftint dtau e^-iomegataug(ttau)right\n\nSo in any case, we need to treat the two-time correlation function before we can obtain the spectrum.","title":"Correlation functions and spectra"},{"category":"section","location":"correlation/#Correlation-function","page":"Correlation functions and spectra","text":"In order to compute a correlation function, we first evolve a system of equations up to a time t. Then, we can derive another set of equations that describe the correlation function. This new set of equations is then evolved from time t up to a time t+tau. The correlation function is then stored in the first entry of the result. The initial state of the set of equations describing the correlation function will be determined by the state of the original system at time t.\n\nWhenever an instance of a CorrelationFunction is created, a set of equations is derived with meanfield and then closed via the cumulant closure. Internally, QuantumCumulants.jl introduces an ancilla subspace carrying the second operator op2 at time t; the τ-equations are derived for averages that touch this ancilla, while non-ancilla averages on the RHS are treated as steady-state coefficients. Depending on the size and order of the considered system, this can take some time. An important distinction that can reduce the computation time substantially is whether the original system has been evolved to steady state. This is controlled by the keyword steady_state=true (the default) on CorrelationFunction.\n\nTo clarify the usage, consider the simple case of a cavity with resonance frequency omega_mathrmc that initially has a finite number of photons inside which decay over time at a rate kappa. We want to compute the two-time correlation function of the field (first-order degree of coherence) given by\n\ng(ttau) = langle a^dagger(t+tau)a(t)rangle\n\nGiven the Hamiltonian H = omega_mathrmca^dagger a and the collapse operator a, it is easy to derive the equation\n\nfracddtaua^dagger(t+tau) = (iomega_mathrmc - frackappa2)a^dagger(t+tau)\n\nSince a(t) is independent of tau we can simply multiply the above equation with a(t) from the right and average to obtain\n\nfracddtaulangle a^dagger(t+tau) a(t)rangle = (iomega_mathrmc - frackappa2)langle a^dagger(t+tau) a(t)rangle\n\nNote that this is generally valid, and will lead to a system of equations that is linear in a(t). The above is the equation of motion for the correlation function g(ttau). In this very simple case we can solve it analytically and find\n\ng(ttau) = langle a^dagger(t)a(t)rangle e^(iomega_mathrmc - kappa2)tau\n\nThis is the essential procedure with which correlation functions can be computed within QuantumCumulants.jl. In code, the above is just:\n\nusing QuantumCumulants # hide\nh = FockSpace(:cavity)\na = Destroy(h, :a)\n@variables ωc::Real κ::Real\nH = ωc*a'*a\nme = meanfield(a'*a, H, [a]; rates=[κ])\n\nc = CorrelationFunction(a', a, me; steady_state=false)\nnothing # hide\n\nWhen the CorrelationFunction is constructed, an additional Hilbert space is added internally which represents the system at the time t. In our case, this means that another FockSpace is added. The τ-equations live on the extended product space and are stored in c.eqs.\n\nTo solve the original system numerically, build its MTK system via System and feed it into OrdinaryDiffEq. The ⟨a'a⟩ equation depends only on κ; ωc only enters once we evolve the correlation function (which carries a ⟨a†⟩ factor whose phase rotates at ωc):\n\nusing ModelingToolkitBase, OrdinaryDiffEqTsit5\n\nsys = mtkcompile(System(me; name=:cav))\nn0 = 20.0 # Initial number of photons in the cavity\nu0 = initial_values(me, [ComplexF64(n0)])   # me.states is the single ⟨a'a⟩ state\nωc_val, κ_val = 1.0, 1.0\nprob = ODEProblem(sys, merge(u0, Dict(κ => κ_val)), (0.0, 2.0)) # end time not in steady state\nsol = solve(prob, Tsit5())\nnothing # hide\n\nNumerically computing the correlation function works in the same way. The initial state of the τ-system depends on the final state of the system, and correlation_u0 picks the right steady-state averages out of the original solution automatically. Parameters needed by the τ-system are propagated with correlation_p0:\n\ncsys = mtkcompile(System(c; name=:cav_corr))\nu0_c = correlation_u0(c, sol.u[end])\np0_c = correlation_p0(c, sol.u[end], [ωc => ωc_val, κ => κ_val])\nprob_c = ODEProblem(csys, merge(u0_c, Dict(p0_c)), (0.0, 10.0))\nsol_c = solve(prob_c, Tsit5(), save_idxs=1)\nnothing # hide\n\nFinally, let's check our numerical solution against the analytic one obtained above:\n\nusing Test # hide\ng_analytic(τ) = @. sol.u[end][1] * exp((im*ωc_val - 0.5*κ_val)*τ)\n@test isapprox(sol_c.u, g_analytic(sol_c.t), rtol=1e-4)\n\nNote that this was a very simple case. Usually the system of equations describing the correlation function is much more complex and depends on multiple other correlation functions (see for example Spectrum of a single-atom laser).","title":"Correlation function"},{"category":"section","location":"correlation/#Time-dependent-Hamiltonians","page":"Correlation functions and spectra","text":"So far the Hamiltonian was time-independent. When H (or the jump operators or rates) depends on the time variable t, for instance through a registered drive or modulation f(t), some care is needed. The correlation function\n\ng(t_0tau) = langle a^dagger(t_0+tau)a(t_0)rangle\n\nis evaluated by the quantum regression theorem at the absolute time t_0+tau, not at tau: the modulation keeps running during the delay tau, starting from the time t_0 at which the original evolution stopped. QuantumCumulants.jl implements this by substituting t to t_0 + tau in the correlation equations. You supply t_0 through the iv0 keyword of CorrelationFunction.\n\nAs an illustration, take a cavity with a sinusoidally modulated frequency omega_c(t) = omega_c + deltacos(nut). We register the modulation and build the time-dependent system on the mean-field independent variable t:\n\n@variables δ::Real t0::Real\n@register_symbolic ωmod(t)\nν = 0.8 # modulation frequency (numeric)\nωmod(t) = cos(ν*t)\n\nt = me.iv\nH_t = (ωc + δ*ωmod(t))*a'*a\nme_t = meanfield(a'*a, H_t, [a]; rates=[κ])\n\nc_t = CorrelationFunction(a', a, me_t; iv0=t0)\nnothing # hide\n\nWithout iv0, a time-dependent system raises an informative error, since the correlation equations would otherwise contain the orphaned time variable t. We evolve the original system to some stop time t0, then evolve the correlation with t0 propagated as a parameter:\n\nsys_t = mtkcompile(System(me_t; name=:cav_t))\nωc_val, κ_val, δ_val, t0_val = 2.0, 0.5, 1.5, 5.0\nu0_t = initial_values(me_t, [ComplexF64(4.0)]) # 4 photons initially\nprob_t = ODEProblem(sys_t, merge(u0_t, Dict(κ => κ_val)), (0.0, t0_val))\nsol_t = solve(prob_t, Tsit5(), abstol=1e-12, reltol=1e-12)\n\ncsys_t = mtkcompile(System(c_t; name=:cav_t_corr))\nu0_ct = correlation_u0(c_t, sol_t.u[end])\np0_ct = correlation_p0(c_t, sol_t.u[end], [ωc => ωc_val, κ => κ_val, δ => δ_val, t0 => t0_val])\nprob_ct = ODEProblem(csys_t, merge(u0_ct, Dict(p0_ct)), (0.0, 8.0))\nsol_ct = solve(prob_ct, Tsit5(), abstol=1e-12, reltol=1e-12, save_idxs=1)\nnothing # hide\n\nThe frequency modulation cancels in the photon-number dynamics, so langle a^dagger arangle(t_0) = n_0 e^-kappa t_0, and the correlation has the closed form\n\ng(t_0tau) = langle a^dagger arangle(t_0)\nexpleft-tfrackappa2tau + iomega_ctau + itfracdeltanubigl(sin(nu(t_0+tau)) - sin(nu t_0)bigr)right\n\nwhich depends on the stop time t_0 through the modulation phase. We verify the numerical solution against it:\n\nusing Test # hide\nn_t0 = real(sol_t.u[end][1])\ng_t(τ) = @. n_t0 * exp(-0.5*κ_val*τ + im*(ωc_val*τ + (δ_val/ν)*(sin(ν*(t0_val+τ)) - sin(ν*t0_val))))\n@test isapprox(sol_ct.u, g_t(sol_ct.t), rtol=1e-6)\n\nA worked physical application, recovering the Mollow triplet of a pulsed drive from a plateau time t_0, is given in Mollow Triplet from a Pulsed Drive.","title":"Time-dependent Hamiltonians"},{"category":"section","location":"correlation/#Spectrum-calculation","page":"Correlation functions and spectra","text":"There are two possible ways to compute the spectrum given a correlation function:\n\nSolving the differential equation needed to obtain g(ttau) and taking the Fourier transform.\nTaking the (symbolic) Laplace transform of the system of equations describing a correlation function.\n\nOn the one hand, the first approach works generally, but is computationally more intense. The second approach, on the other hand, yields a simple linear system of equations which is easy to solve, but only works when the correlation function has been computed starting from the steady state. Both methods can be easily used with QuantumCumulants.jl.","title":"Spectrum calculation"},{"category":"section","location":"correlation/#Numerical-solution-of-g(t,\\tau)","page":"Correlation functions and spectra","text":"As mentioned above, this approach works generally, regardless of whether the system is in steady state at time t. However, it has some computational drawbacks. Additionally, the spectrum is not always well defined when not in steady state. This approach is the same as the one used in QuantumOptics.jl, and we can borrow the implemented FFT function from there:\n\nτ = collect(range(0.0, sol_c.t[end], length=101)) # need equidistant list of times for FFT\nusing QuantumOptics.timecorrelations: correlation2spectrum\nω, s = correlation2spectrum(τ, sol_c.(τ))\nnothing # hide\n\nThe spectrum obtained in this way roughly has a FWHM of κ and is centred around the chosen ωc. The fact that the FWHM is not exactly κ illustrates the computational drawback: in order to obtain the correct FWHM we would have to increase the integration time by orders of magnitude. For larger systems, this can be computationally expensive.","title":"Numerical solution of g(ttau)"},{"category":"section","location":"correlation/#Steady-state:-using-the-Laplace-transform","page":"Correlation functions and spectra","text":"A useful property of the two-time correlation function is that, if the system is in steady state at time t, then the system of equations is linear, since b(t) can occur at most once in each product. We can therefore write any system of equations describing the correlation function as\n\nfracddtau textbfy(tau) = textbfM textbfy(tau) + textbfc\n\nwhere textbfy(tau) is the vector containing the left-hand-side of the correlation function system (g(ttau) equiv y_1(tau)). The matrix textbfM contains coefficients consisting of parameters and steady-state values, and is independent of time, and the vector textbfc includes other constants.\n\nWe define textbfx(s) = mathcalLleft(textbfy(tau)right), i.e. textbfx(s) is the Laplace transform of textbfy(tau) with respect to tau. Applying the Laplace transform to the differential equation above, we obtain\n\n(s - textbfM)textbfx(s) = textbfy(0) + fractextbfcs\n\nThe Laplace transform is equivalent to the Fourier transform at the point s=i omega, i.e. the spectrum is given by S(omega) = 2textReleftx_1(iomega)right. Therefore, we can reduce the task to solving the equation\n\nAtextbfx = b + c\n\nwhere A = iomega - textbfM, b = textbfy(0) and c=textbfc(iomega). In most cases, solving the above matrix equation is much faster than doing an additional time evolution to obtain the correlation function.\n\nThis approach is implemented with the Spectrum type, which performs the Laplace transform and computes the matrix A and the vectors b and c symbolically. Functions returning all those things in numerical form depending on the steady-state values and given parameters are generated via Symbolics build_function. Usage is as follows:\n\nc = CorrelationFunction(a', a, me; steady_state=true) # need to specify steady state\nS = Spectrum(c, (ωc, κ))\nnothing # hide\n\nThe above performs the Laplace transform on a symbolic level (i.e. it derives the matrix A). To actually compute the spectrum, we evaluate at a frequency ω, using the final state of the original system and the parameter values:\n\ns = S(ω, sol.u[end], [ωc_val, κ_val])\nnothing # hide","title":"Steady state: using the Laplace transform"},{"category":"section","location":"correlation/#Examples:","page":"Correlation functions and spectra","text":"Mollow Triplet\nSpectrum of a single-atom laser","title":"Examples:"},{"category":"section","location":"symbolic_sums/#Indexed-and-scaled-systems","page":"Indexed and scaled systems","text":"Many physical systems contain multiple identical elements: N atoms in a cavity, a chain of emitters, a register of qubits. Rather than writing N copies of every equation, you write the Hamiltonian once with indexed operators and symbolic sums, derive the equations in terms of a running index, and only commit to a concrete N at the numerical stage.\n\nThe indexed-operator machinery itself (Index, IndexedOperator, IndexedVariable, the summation constructor Σ (also written ∑), and the automatic diagonal splitting of products) is provided by SecondQuantizedAlgebra.jl; see its Symbolic Sums and Indices guide for how to build indexed expressions. This page covers what QuantumCumulants adds on top: deriving, closing, and collapsing or unrolling an indexed mean-field system.","title":"Indexed and scaled systems"},{"category":"section","location":"symbolic_sums/#A-worked-example:-Tavis-Cummings","page":"Indexed and scaled systems","text":"We take N two-level atoms in a single-mode cavity. The atom subspace carries an Index i, the per-atom coupling is an IndexedVariable, and the Hamiltonian sums over the atoms with Σ:\n\nusing QuantumCumulants\n\nha = NLevelSpace(:atoms, 2)\nhc = FockSpace(:cavity)\nh = hc ⊗ ha\n\n@variables N::Int Δ::Real κ::Real γ::Real ν::Real\n\ni = Index(h, :i, N, ha)\n\n@qnumbers b::Destroy(h)\nσ(x, y, z) = IndexedOperator(Transition(h, :σ, x, y), z)\ngi = IndexedVariable(:g, i)\n\nH = Δ*b'*b + ∑(gi*(b*σ(2, 1, i) + b'*σ(1, 2, i)), i)\nnothing # hide\n\nundersetioversetNsum g_i  b  sigma_i^21 + undersetioversetNsum g_i  b^dagger  sigma_i^12 + Delta b^dagger b\n\nmeanfield and complete work exactly as in the scalar case; an indexed jump operator J_i with rate R_i contributes the dissipator\n\nfrac12 sum_i R_i left( 2 J_i^dagger mathcalO J_i - mathcalO J_i^dagger J_i -  J_i^dagger J_i mathcalO right)\n\nwith a scalar or matching-index rate:\n\nJ     = [b, σ(1, 2, i), σ(2, 1, i)]\nrates = [κ, γ, ν]\n\neqs = meanfield(b'b, H, J; rates=rates, order=2)\neqs_comp = complete(eqs)\nnothing # hide","title":"A worked example: Tavis-Cummings"},{"category":"section","location":"symbolic_sums/#Evaluate-vs.-scale","page":"Indexed and scaled systems","text":"The closed equations still contain symbolic sums and the symbolic bound N. There are two routes to concrete numeric equations:\n\nevaluate unrolls each sum into N per-site equations. Use it when the atoms differ (different couplings or rates).\nscale collapses permutation-equivalent terms by assuming the atoms are identical, typically yielding far fewer equations.\n\nBoth accept an h::Vector{Int} of subspace space_index values to target specific Hilbert factors, so a hybrid system can unroll some subspaces and collapse others.\n\nHere we unroll for N = 3 atoms; the numeric value of N is passed via limits:\n\nevaled = evaluate(eqs_comp; limits=(N => 3))\nnothing # hide","title":"Evaluate vs. scale"},{"category":"section","location":"symbolic_sums/#Numerical-solution","page":"Indexed and scaled systems","text":"The unrolled (or scaled) equations feed into System like any other. A per-atom coupling g_i is a vector parameter; parameter_map turns a dict of symbolic parameters (scalar or array-valued) into the substitution dict the compiled system expects:\n\nusing ModelingToolkitBase\nsys = mtkcompile(System(evaled; name=:tc))\n\nusing OrdinaryDiffEqTsit5\nu0 = zeros(ComplexF64, length(evaled.states))\np  = parameter_map(evaled, Dict(\n    Δ  => 0.0,\n    gi => [0.75, 1.2, 1.5],\n    γ  => 0.25,\n    κ  => 1.0,\n    ν  => 1.5,\n))\nprob = ODEProblem(sys, merge(initial_values(evaled, u0), p), (0.0, 10.0))\nsol = solve(prob, Tsit5())\nnothing # hide\n\nget_solution evaluates any operator-average trajectory. After evaluate, the per-atom excited-state averages are concrete entries in evaled.states:\n\nusing Plots\nusing SecondQuantizedAlgebra: undo_average\n\nts = range(0.0, 10.0; length=200)\nn  = real.(get_solution(sol, b'*b, evaled).(ts))\n\n# Per-atom excited-state averages produced by `evaluate(eqs; limits=(N => 3))`.\npe_states = [s for s in evaled.states if string(undo_average(s)) |> contains(\"σ\") &&\n                                          string(undo_average(s)) |> contains(\"22\")]\n\npl = plot(ts, n, label=\"Photon number\", xlabel=\"t\")\nfor (k, s) in enumerate(pe_states)\n    plot!(pl, ts, real.(get_solution(sol, undo_average(s), evaled).(ts)),\n        label=\"Excited state population of atom $(k)\")\nend\npl # hide","title":"Numerical solution"},{"category":"section","location":"examples/mollow/#Mollow-Triplet","page":"Mollow Triplet","text":"In the following example, we show how one can compute the resonance spectrum of a single, coherently driven atom. The Hamiltonian describing the system is given by\n\nH = -Deltasigma^ee + Omegaleft(sigma^ge + sigma^egright)\n\nwhere Delta = omega_ell - omega_a is the detuning between the laser and the atomic resonance frequency and Omega is the Rabi frequency of the laser. The atom is also subject to decay at rate gamma with the damping operator sigma^ge = grangle langle e  projecting it from the excited state erangle  to its ground state grangle.\n\nusing Latexify # hide\nset_default(double_linebreak = true) # hide\nusing QuantumCumulants\nusing ModelingToolkitBase, OrdinaryDiffEqLowOrderRK\nusing Plots\n\nObtaining the equations of motion for the system is simple. Note that in this case, we are not actually making any assumptions: in the special case of a single atom (even if it has more than two levels), there exists a simple mapping between the equations of motions for averages and the density operator. In our case, the density operator rho is determined by only two average values, langlesigma^egrangle = rho_eg = rho_ge^*  and langle sigma^eerangle = rho_ee = 1 - rho_gg. In other words, we are solving the master equation component-wise.\n\nh = NLevelSpace(:atom, (:g, :e)) # Hilbert space\n\n@variables Δ Ω γ # Operators\nσ(i, j) = Transition(h, :σ, i, j)\nH = Δ * σ(:e, :e) + Ω * (σ(:g, :e) + σ(:e, :g))\nJ = [σ(:g, :e)]\n\n\neqs = meanfield([σ(:e, :g), σ(:e, :e)], H, J; rates = [γ]) # Equations\ncomplete!(eqs)\n\nIn order to compute the correlation function given by\n\ng(tau) = langle sigma^eg(t_0+tau)sigma^ge(t_0)rangle equiv langle sigma^egsigma^ge_0rangle\n\nwhere t_0 is any time after which the original system has reached steady state. Note, that in the second step we simplified the notation by dropping the temporal arguments. The subscript 0 indicates operators given at t_0.\n\nThe correlation function of the system given by eqs can be computed as follows.\n\nc = CorrelationFunction(σ(:e, :g), σ(:g, :e), eqs; steady_state = true) # Correlation Function\nnothing # hide\n\nNote that the above actually derives another set of equations, which might take some time. Specifically, it is deriving the equation of motion for g(tau) and automatically completes the system of equation deriving the necessary equations of motion for all average values on which g(tau) depends. Here, the system of equations reads\n\nc.eqs\nnothing # hide\n\nbeginalign\nfracddt langle sigma^eg  sigma_0^gerangle  = 10 i langle sigma_0^gerangle  Omega + 10 i Delta langle sigma^eg  sigma_0^gerangle  -20 i Omega langle sigma^ee  sigma_0^gerangle  -05 gamma langle sigma^eg  sigma_0^gerangle  \nfracddt langle sigma^ee  sigma_0^gerangle  = -10 i Omega langle sigma^eg  sigma_0^gerangle  + 10 i Omega langle sigma^ge  sigma_0^gerangle  -10 gamma langle sigma^ee  sigma_0^gerangle  \nfracddt langle sigma^ge  sigma_0^gerangle  = -10 i langle sigma_0^gerangle  Omega -10 i Delta langle sigma^ge  sigma_0^gerangle  + 20 i Omega langle sigma^ee  sigma_0^gerangle  -05 gamma langle sigma^ge  sigma_0^gerangle\nendalign\n\nAs mentioned above, to compute the time evolution of the system itself, we are effectively solving a master equation. One way to obtain the spectrum would be to solve the system in order to obtain the time evolution of the correlation function, and then take the Fourier transform. However, a computationally more efficient way is to take the Laplace transform of the correlation function directly, which yields a simple linear system of equations. Then we only have to compute a matrix inverse instead of the time evolution to obtain the spectrum.\n\nIn the following, we will use the latter approach:\n\nps = (Δ, Ω, γ)\nS = Spectrum(c, ps)\nnothing # hide\n\nmathcalF(langle σ^eg  σ_0^gerangle )(omega)\n\nConstructing the Spectrum from the correlation function, automatically derives a matrix A and a vector b determining the set of linear equations\n\nA(omega)x(omega) = b(omega)\n\nwhere the spectrum is then given by the first entry of the solution vector, S(omega)=x_1(omega).\n\nTo find the spectrum, we first need to compute the time evolution of the system up to steady state.\n\nsys = System(eqs; name = :sys)\nssys = mtkcompile(sys)\n\np0 = (0.0, 2.0, 1.0)\nu0 = zeros(ComplexF64, 2)\ndict = merge(Dict(unknowns(ssys) .=> u0), Dict(ps .=> p0))\nprob = ODEProblem(ssys, dict, (0.0, 20.0))\nsol = solve(prob, RK4())\nnothing # hide\n\nplot(\n    sol.t,\n    real.(get_solution(sol, σ(:e, :e), eqs).(sol.t)),\n    xlabel = \"γt\",\n    label = \"Excited state population\",\n)\n\nNow, solving the linear system to obtain the spectrum can simply be done by calling the instance at a range of frequencies, and providing the proper steady-state values and numerical parameters.\n\nω = range(-6pi, 6pi, length = 1001)\ns = S(ω, sol.u[end], p0)\nplot(ω, s, xlabel = \"ω - ωℓ\", label = \"Resonance spectrum\")\n\nThe resulting spectrum shows a prominent peak at the resonance point (omega=omega_ell=omega_a), but also two more peaks around omega approx pm Omega^2gamma. These two resonances originate from the dressed states. These three peaks are called Mollow Triplet.","title":"Mollow Triplet"},{"category":"section","location":"examples/mollow/#Package-versions","page":"Mollow Triplet","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqLowOrderRK\", \"ModelingToolkitBase\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/heterodyne_detection/#Heterodyne-detection-of-emission-from-atomic-ensemble","page":"Heterodyne detection of emission from atomic ensemble","text":"This example implements the heterodyne detection of the light emitted by an ensemble of atoms, described in H. Yu, et. al., Phys. Rev. Lett 133, 073601 (2024). We describe the stochastic master equation time evolution that governs the measurement backaction on the system.\n\nusing QuantumCumulants\nusing ModelingToolkitBase\nusing OrdinaryDiffEqLowOrderRK\nusing StochasticDiffEqLowOrder: SDEProblem, EM, EnsembleProblem, ReturnCode\nusing DiffEqNoiseProcess: RealWienerProcess\nusing Plots\nusing Random # hide\n\nThe system we discuss is an ensemble of N equivalent two level systems with transition operators sigma^alpha beta_j and frequency omega_a) coupled to a cavity mode hat a, all with the same coupling g and free space decay rate gamma. The cavity has frequency omega_c and decay rate kappa. We are in the frame rotating with the frequency omega_a=omega_c.\n\nThe system Hamiltonian is\n\nH=omega_chat a^daggerhat a+omega_asum_jsigma^22_j+hat a^daggersigma^12_j+ghat asigma^21_j\n\n@variables N ωa γ η χ ωc κ g ξ ωl\n@register_symbolic pulse(t)\n\nhc = FockSpace(:resonator)\nha = NLevelSpace(:atom, 2)\nh = hc ⊗ ha\n\nj = Index(h, :j, N, ha)\nk = Index(h, :k, N, ha)\n\n@qnumbers a::Destroy(h, 1)\nσ(α, β, k) = IndexedOperator(Transition(h, :σ, α, β, 2), k)\n\neqs_seed = meanfield(a, ωc * a' * a, [a]; rates = [κ])\nt = eqs_seed.iv\n\nH = ωc * a' * a + ωa * Σ(σ(2, 2, j), j) + g * a' * Σ(σ(1, 2, j), j) + g * a * Σ(σ(2, 1, j), j);\nnothing #hide\n\nWe include decay of the cavity mode, where we choose the decay operator to be hat aexp(rmiomega_lt) for convenience (we will see later why this is reasonable, and you can convince yourself that it gives the same decay term as hat a). The individual decay of the atoms with rate gamma is given by the decay operator sigma^12_j.\n\nWe also include an incoherent pump sigma^21_j with amplitude eta, which is a pulse that is on between t_0 and t_0+t_1. The last Lindblad term is a dephasing term with strength chi corresponding to the operator sigma^22_j.\n\nJ = [a * exp(1.0im * ωl * t), σ(1, 2, j), σ(2, 1, j), σ(2, 2, j)]\nrates = [κ, γ, η * pulse(t), 2 * χ]\n\npulse(t) = (t > t0 && t < t0 + t1) * 1.0\nnothing # hide\n\nThe measurement terms are included by defining measurement efficiencies for all decay channels. A channel with efficiency zero is ignored, i.e. not measured. The operator hat aexp(rmiomega_lt) corresponding to heterodyne detection with local oscillator frequency omega_l is measured with efficiency xi.\n\nSuch measurement terms are then included in the equation of motion for system as\n\nbeginalign\ndlanglehat Arangle=sqrtxikappa2langle hat a^dagger rme^-rmiomega_lthat A+hat Ahat arme^rmiomega_lt -hat alanglehat a rme^rmiomega_lt+hat a^dagger rme^-rmiomega_ltranglerangle\nendalign\n\nfor the operator hat a rme^rmiomega_lt corresponding to heterodyne detection.\n\nefficiencies = [ξ, 0, 0, 0]\nops = [a', a' * a, σ(2, 2, k), σ(1, 2, k), a * a]\neqs = meanfield(ops, H, J; rates = rates, efficiencies = efficiencies, direction = Forward(), order = 2, iv = t)\nnothing # hide\n\nbeginalign\nfracddt langle a^daggerrangle = -05 kappa langle a^daggerrangle + dWdt left( -1 sqrtxi kappa e^-10 i omegal t langle a^daggerrangle ^2 + sqrtxi kappa e^10 i omegal t langle a^dagger arangle -1 langle arangle sqrtxi kappa e^10 i omegal t langle a^daggerrangle + sqrtxi kappa e^-10 i omegal t langle a^dagger a^daggerrangle right) + 1 i undersetjoversetNsum g langle sigmaj^21rangle + 1 i omegac langle a^daggerrangle  fracddt langle a^dagger arangle = -1 i undersetjoversetNsum g langle a^dagger sigmaj^12rangle + 1 i undersetjoversetNsum g langle a sigmaj^21rangle + left( -1 langle arangle sqrtxi kappa e^10 i omegal t langle a^dagger arangle + sqrtxi kappa e^-10 i omegal t left( -2 langle arangle langle a^daggerrangle ^2 + 2 langle a^daggerrangle langle a^dagger arangle + langle arangle langle a^dagger a^daggerrangle right) + left( langle a arangle langle a^daggerrangle + 2 langle arangle langle a^dagger arangle -2 langle arangle ^2 langle a^daggerrangle right) sqrtxi kappa e^10 i omegal t -1 sqrtxi kappa e^-10 i omegal t langle a^daggerrangle langle a^dagger arangle right) dWdt -10 kappa langle a^dagger arangle  fracddt langle sigmak^22rangle = -10 mathrmpulseleft( t right) eta langle sigmak^22rangle + 1 i langle a^dagger sigmak^12rangle g + dWdt left( langle a sigmak^22rangle sqrtxi kappa e^10 i omegal t -1 langle arangle sqrtxi kappa e^10 i omegal t langle sigmak^22rangle -1 sqrtxi kappa e^-10 i omegal t langle sigmak^22rangle langle a^daggerrangle + langle a^dagger sigmak^22rangle sqrtxi kappa e^-10 i omegal t right) -10 gamma langle sigmak^22rangle -1 i g langle a sigmak^21rangle + mathrmpulseleft( t right) eta  fracddt langle sigmak^12rangle = -05 gamma langle sigmak^12rangle -1 i omegaa langle sigmak^12rangle -1 chi langle sigmak^12rangle + dWdt left( sqrtxi kappa langle a^dagger sigmak^12rangle e^-10 i omegal t -1 sqrtxi kappa e^-10 i omegal t langle sigmak^12rangle langle a^daggerrangle -1 langle arangle sqrtxi kappa langle sigmak^12rangle e^10 i omegal t + langle a sigmak^12rangle sqrtxi kappa e^10 i omegal t right) -1 i langle arangle g -05 mathrmpulseleft( t right) eta langle sigmak^12rangle + 2 i langle a sigmak^22rangle g  fracddt langle a arangle = -10 langle a arangle kappa + left( left( 3 langle a arangle langle arangle -2 langle arangle ^3 right) sqrtxi kappa e^10 i omegal t + left( langle a arangle langle a^daggerrangle + 2 langle arangle langle a^dagger arangle -2 langle arangle ^2 langle a^daggerrangle right) sqrtxi kappa e^-10 i omegal t -1 langle a arangle sqrtxi kappa e^-10 i omegal t langle a^daggerrangle -1 langle a arangle langle arangle sqrtxi kappa e^10 i omegal t right) dWdt -2 i undersetjoversetNsum g langle a sigma_j^12rangle -2 i langle a arangle omegac\nendalign\n\nThe completion and scaling as previously discussed in other examples work exactly the same way for equations including noise terms.\n\neqs_c = complete(eqs; get_adjoints = false)\nscaled_eqs = scale(eqs_c)\nnothing # hide\n\nHere we define the actual values for the system parameters. We then show that the deterministic time evolution without the noise terms is still accessible by using the constructor System for the stochastic system of equations and the syntax for the simulation of the time evolution is as usual.\n\n# frequencies are in kHz\nωc_ = 0.0\nκ_ = 2π * 1.13e3\nξ_ = 0.12\nN_ = 5.0e4\nωa_ = 0.0\nγ_ = 2π * 0.375\nη_ = 2π * 20\nχ_ = 0.016\ng_ = 2π * 0.73\nωl_ = 2π * 1.0e3\nt0 = 0.0\nt1 = 20.0e-3\np = [N, ωa, γ, η, χ, ωc, κ, g, ξ, ωl]\np0 = [N_, ωa_, γ_, η_, χ_, ωc_, κ_, g_, ξ_, ωl_]\nT_end = 0.1 # 0.1ms\n\nTo compare the stochastic evolution to the no-measurement case, build the deterministic system by passing MeanfieldEquations(scaled_eqs) to System. The constructor strips the noise drift, which also drops ξ from the compiled parameter list (it appeared only inside the sqrt(ξ κ) factor of the noise term). Because the physical parameter dict p .=> p0 still carries ξ, we filter it through parameter_map(sys, …); the helper keeps only the entries whose key is a live parameter or unknown of the compiled system and silently drops the rest.\n\nsys = mtkcompile(System(MeanfieldEquations(scaled_eqs); name = :sys))\nu0 = zeros(ComplexF64, length(scaled_eqs))\ndict = parameter_map(sys, merge(Dict(unknowns(sys) .=> u0), Dict(p .=> p0)))\nprob = ODEProblem(sys, dict, (0.0, T_end))\nsol_det = solve(prob, RK4(), dt = T_end / 2.0e5)\n\ngraph = plot(xlabel = \"Time (ms)\", ylabel = \"Cavity photon number\")\nplot!(graph, sol_det.t, real(get_solution(sol_det, a'a, scaled_eqs)(sol_det.t)), legend = false)\n\nThe stochastic time evolution is accessible via the constructor SDESystem, whose syntax is exactly the same as for the System, but with keyword args as defined in the SDE tutorial. We need to provide a noise process for the measurement. If the noise is white the appropriate noise process is a Wiener process. The SDEProblem is constructed just as the ODEProblem, but with an additional noise argument.\n\nWe can then make use of the EnsembleProblem, which automatically runs multiple instances of the stochastic equations of motion. The number of trajectories can be set in the solve call. See the tutorial linked above for more details of the function calls here.\n\nsys_st = mtkcompile(System(scaled_eqs; name = :sys_st))\ndict_st = parameter_map(sys_st, merge(Dict(unknowns(sys_st) .=> u0), Dict(p .=> p0)))\n\nRandom.seed!(2) # hide\nnoise = RealWienerProcess(0.0, 0.0)\nprob_st = SDEProblem(sys_st, dict_st, (0.0, T_end); noise = noise)\nsol_test = solve(prob_st, EM(); dt = T_end / 2.0e5);\n\nplot(\n    sol_test.t,\n    real(get_solution(sol_test, a'a, scaled_eqs)(sol_test.t)),\n    xlabel = \"Time (ms)\",\n    ylabel = \"Cavity photon number\",\n    legend = false,\n)\n\nRandom.seed!(1) # hide\neprob = EnsembleProblem(prob_st)\ntraj = 200\ntspan = range(0.0, T_end, length = 201)\nsol = solve(\n    eprob,\n    EM(),\n    dt = T_end / 2.0e5,\n    save_noise = true,\n    trajectories = traj,\n    saveat = tspan,\n)\nnothing # hide\n\nWe plot the average of the cavity photon number for the stochastic and deterministic equation of motion with the trajectories in gray in the background. We can see that the dynamics of the system is indeed modified by the measurement backaction.\n\nn_avg_ = zeros(length(tspan)) # average photon number\na_real_avg_ = zeros(length(tspan)) # average field (real part)\ntraj_succ = zeros(traj) # trajectories can be numerically unstable\nfor i in 1:traj\n    sol_ = sol.u[i]\n    if sol_.retcode == ReturnCode.Success\n        n_avg_ .+= real(get_solution(sol_, a'a, scaled_eqs)(tspan))\n        a_real_avg_ .+= real(get_solution(sol_, a, scaled_eqs)(tspan))\n        traj_succ[i] = 1\n    end\nend\n\n@show sum(traj_succ)\nn_avg = n_avg_ / sum(traj_succ)\na_real_avg = a_real_avg_ / sum(traj_succ)\ngraph1 = plot(xlabel = \"Time (ms)\", ylabel = \"Cavity photon number\")\nfor i in 1:traj\n    plot!(\n        graph1,\n        sol.u[i].t,\n        real(get_solution(sol.u[i], a'a, scaled_eqs)(sol.u[i].t)),\n        color = :grey,\n        alpha = 0.75,\n        label = nothing,\n    )\nend\nplot!(graph1, tspan, n_avg, color = :red, label = nothing)\n\nFor the following cavity field amplitude we see that the ensemble average becomes zero, but the trajectories have finite cavity field amplitude.\n\ngraph2 = plot(xlabel = \"Time (ms)\", ylabel = \"Real part cavity field\")\nfor i in 1:traj\n    plot!(graph2, tspan, real(get_solution(sol.u[i], a', scaled_eqs)(tspan)), color = :grey, alpha = 0.75, label = nothing)\nend\nplot!(graph2, tspan, a_real_avg, color = :red, label = nothing)","title":"Heterodyne detection of emission from atomic ensemble"},{"category":"section","location":"examples/heterodyne_detection/#Package-versions","page":"Heterodyne detection of emission from atomic ensemble","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"ModelingToolkitBase\", \"OrdinaryDiffEqLowOrderRK\", \"StochasticDiffEqLowOrder\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/unique_squeezing/#Unique-Steady-State-Squeezing","page":"Unique Steady-State Squeezing","text":"In this example we show the unique squeezing observed in a driven Dicke model described by N two-level systems coupled to a quantized harmonic oscillator [K. Gietka. et. al., Phys. Rev. Lett. 131, 223604 (2023)].  First we present the full dynamics with a second order cumulant expansion. The Hamiltonian describing the system is\n\nbeginalign\nH = omega a^dagger a + fracOmega2 sum_j  sigma^j_z + fracg2 sum_j  (a^dagger + a) sigma^j_x + eta ( a  e^i omega_d t + a^dagger e^-i omega_d t)\nendalign\n\nfor N = 1 it describes the driven quantum Rabi model. Additionally the system features a decay channel, losses of the harmonic oscillator with rate kappa.\n\nWe start by loading the packages.\n\nusing QuantumCumulants\nusing OrdinaryDiffEqTsit5, ModelingToolkitBase\nusing ModelingToolkitBase: t_nounits as t\nusing Plots\nnothing # hide\n\nWe define the Hilbert space and the symbolic parameters of the system.\n\nhf = FockSpace(:harmonic) # Define hilbert space\nha = NLevelSpace(Symbol(:spin), 2)\nh = hf ⊗ ha\n\n@variables ω Ω ωd η κ g γ N ξ # Parameter\nnothing # hide\n\nOn the Hilbert space we create the destroy operator a of the harmonic oscillator and the (indexed) transition operator sigma_i^xy for the i-th two-level system.\n\n@qnumbers a::Destroy(h)\nσ(x, y, i) = IndexedOperator(Transition(h, :σ, x, y), i)\nnothing # hide\n\nWith the symbolic parameters, operators and indices we define the Hamiltonian and Liouvillian of the system. Note, however, that in the strong coupling regime g sim g_cequiv sqrtomega Omega the driving term and jump operators have to be redefined. For a strongly interacting system, the ground state is very different from the ground state of a non-interacting system. Therefore, using jump operators of a non-interacting system would lead to extraction of energy from the ground state of a strongly interacting system. The correct operators are the ones that diagonalise the Hamiltonian with adiabatiacally eliminated spins.\n\nb = a * cosh(ξ) + a' * sinh(ξ) # Operators diagonalizing the Hamiltonian,i.e. approximate new eigenmodes of the system\n\ni = Index(h, :i, N, ha) # Indices\nj = Index(h, :j, N, ha)\n\nHf = ω * a' * a + η * (b' * exp(-1im * ωd * t) + b * exp(1im * ωd * t))\nHa = Ω * Σ(σ(2, 2, i) - σ(1, 1, i), i) / 2\nHi = g * Σ((σ(1, 2, i) + σ(2, 1, i)) * (a + a'), i) / 2\nH = Hf + Ha + Hi # Hamiltonian\n\nJ = [b, σ(1, 2, i)] # Jump operators & and rates\nrates = [κ, γ]\n\nps = [ω, Ω, ωd, g, η, κ, γ, N, ξ] # symbolic and numeric parameter list\n\nFirst we derive the mean-field equations in second order for langle a rangle, langle a^dagger a rangle and langle sigma^22_j rangle, then we complete the system to obtain a closed set of equations.\n\neqs = meanfield([a, a'a, σ(2, 2, j)], H, J; rates = rates, order = 2)\nnothing # hide\n\nbeginalign\nfracddt langle arangle  = -05 i left( undersetioversetNsum g  langle sigma_i^12rangle  + undersetioversetNsum g  langle sigma_i^21rangle  right) -1 i omega langle arangle  -1 i eta sinhleft( xi right) e^1 i t omegad -1 i eta e^-1 i t omegad cosh(xi)^* -05 kappa coshleft( xi right) langle arangle  cosh(xi)^* + 05 kappa sinhleft( xi right) langle arangle  sinh(xi)^* \nfracddt langle a^dagger  arangle  = 05 i left( undersetioversetNsum g  langle a  sigma_i^12rangle  + undersetioversetNsum g  langle a  sigma_i^21rangle  right) -05 i left( undersetioversetNsum g  langle a^dagger  sigma_i^12rangle  + undersetioversetNsum g  langle a^dagger  sigma_i^21rangle  right) + kappa sinhleft( xi right) sinh(xi)^* + kappa sinhleft( xi right) sinh(xi)^* langle a^dagger  arangle  -1 i eta sinhleft( xi right) langle a^daggerrangle  e^1 i t omegad + 1 i eta coshleft( xi right) langle arangle  e^1 i t omegad -1 i eta langle a^daggerrangle  e^-1 i t omegad cosh(xi)^* + 1 i eta langle arangle  e^-1 i t omegad sinh(xi)^* -10 kappa coshleft( xi right) cosh(xi)^* langle a^dagger  arangle  \nfracddt langle sigma_j^22rangle  = -10 gamma langle sigma_j^22rangle  -05 i g left( langle a^dagger  sigma_j^21rangle  + langle a  sigma_j^21rangle  right) + 05 i g left( langle a^dagger  sigma_j^12rangle  + langle a  sigma_j^12rangle  right)\nendalign\n\neqs_c = complete(eqs)\nlength(eqs_c)\n\nAll two-level systems behave identically, due to this permutation symmetry of the system we can scale-up the equations.\n\neqs_sc = scale(eqs_c)\nscale(eqs) # Example scaling on the first three equations\nnothing # hide\n\nbeginalign\nfracddt langle arangle  = -05 i left( N g langle sigma_1^21rangle  + N g langle sigma_1^12rangle  right) -1 i omega langle arangle  -1 i eta sinhleft( xi right) e^1 i t omegad -1 i eta e^-1 i t omegad cosh(xi)^* -05 kappa coshleft( xi right) langle arangle  cosh(xi)^* + 05 kappa sinhleft( xi right) langle arangle  sinh(xi)^* \nfracddt langle a^dagger  arangle  = -05 i left( N g langle a^dagger  sigma_1^21rangle  + N g langle a^dagger  sigma_1^12rangle  right) + 05 i left( N g langle a  sigma_1^21rangle  + N g langle a  sigma_1^12rangle  right) + kappa sinhleft( xi right) sinh(xi)^* + kappa sinhleft( xi right) sinh(xi)^* langle a^dagger  arangle  -1 i eta sinhleft( xi right) langle a^daggerrangle  e^1 i t omegad + 1 i eta coshleft( xi right) langle arangle  e^1 i t omegad -1 i eta langle a^daggerrangle  e^-1 i t omegad cosh(xi)^* + 1 i eta langle arangle  e^-1 i t omegad sinh(xi)^* -10 kappa coshleft( xi right) cosh(xi)^* langle a^dagger  arangle  \nfracddt langle sigma_1^22rangle  = -05 i g left( langle a^dagger  sigma_1^21rangle  + langle a  sigma_1^21rangle  right) + 05 i g left( langle a^dagger  sigma_1^12rangle  + langle a  sigma_1^12rangle  right) -10 gamma langle sigma_1^22rangle\nendalign\n\nTo calculate the dynamics of the system we create a system of ordinary differential equations with its initial state and numerical parameters.\n\nsys = System(eqs_sc; name = :sys) # symbolic ordinary differential equation system\nsys = mtkcompile(sys)\nu0 = zeros(ComplexF64, length(eqs_sc)); # initial state\n\nω_ = 1.0 # Parameters\nΩ_ = 2.0e3ω_\nη_ = 4ω_\nκ_ = ω_\nγ_ = ω_\n\nDerived quantities (gc, g, ωd, ξ) depend on N. We pick the largest N (the well-converged case) as the canonical global value used by the post-solve effective-model analysis below; the loop redefines them locally per N_.\n\nN_global = 100\ngc_ = sqrt(Ω_ * ω_ / N_global)\ng_ = 0.9gc_\nωd_ = sqrt(1 - g_^2 / gc_^2) * ω_\nξ_ = 1 / 4 * log(1 - N_global * g_^2 / (ω_ * Ω_))\n\nWe solve the dynamics for four different numbers of two-level systems N = 1 2 10 100.\n\nsol_ls = []\nN_ls = [1, 2, 10, 100]\nfor N_ in N_ls\n    local gc_l = sqrt(Ω_ * ω_ / N_)\n    local g_l = 0.9gc_l\n    local ωd_l = sqrt(1 - g_l^2 / gc_l^2) * ω_\n    local ξ_l = 1 / 4 * log(1 - N_ * g_l^2 / (ω_ * Ω_))\n    local p0 = [ω_, Ω_, ωd_l, g_l, η_, κ_, γ_, N_, ξ_l]\n    local u0_dict = Dict{Any, Any}(unknowns(sys) .=> u0)\n    local dict = merge(u0_dict, Dict{Any, Any}(ps .=> p0))\n    local prob = ODEProblem(sys, dict, (0.0, 4π / ωd_l))\n    local sol = solve(prob, Tsit5(); saveat = π / 30ωd_l, reltol = 1.0e-10, abstol = 1.0e-10)\n    push!(sol_ls, sol)\nend\n\nc_ls = [:black, :red, :blue, :cyan] # plot results\np1 = plot(xlabel = \"ω t\", ylabel = \"Δ² O\")\np2 = plot(xlabel = \"ω t\", ylabel = \"⟨σz⟩\")\nfor k in 1:length(N_ls)\n    sol = sol_ls[k]\n    t_ = sol.t\n\n    adag_a = get_solution(sol, a' * a, eqs_sc).(t_)\n    aa = get_solution(sol, a * a, eqs_sc).(t_)\n    adag_adag = get_solution(sol, a' * a', eqs_sc).(t_)\n    a_ = get_solution(sol, a, eqs_sc).(t_)\n    adag = get_solution(sol, a', eqs_sc).(t_)\n\n    sqx = adag_adag + aa + 2 * adag_a .+ 1 - (adag + a_) .^ 2\n    sqy = adag_adag + aa - 2 * adag_a .- 1 - (adag - a_) .^ 2\n    plot!(p1, t_, real.(sqx), label = \"N = $(N_ls[k])\", color = c_ls[k])\n    plot!(p1, t_, -real.(sqy), ls = :dash, label = nothing, color = c_ls[k])\n\n    s22 = get_solution(sol, σ(2, 2, j(1)), eqs_sc).(t_)\n    plot!(p2, t_, real.(2s22 .- 1), color = c_ls[k], label = nothing)\nend\nplot(\n    p1,\n    p2,\n    layout = (1, 2),\n    size = (700, 250),\n    bottom_margin = 5 * Plots.mm,\n    left_margin = 5 * Plots.mm,\n)","title":"Unique Steady-State Squeezing"},{"category":"section","location":"examples/unique_squeezing/#Effective-model","page":"Unique Steady-State Squeezing","text":"For a sufficiently low excitation we can adiabatically eliminate the dynamics of the two-level system(s). This leads to an effective Hamiltonian\n\nbeginalign\nH_mathrma = omega a^dagger a - fracg^24 Omega(a + a^dagger)^2 + eta ( a  e^i omega_d  t + a^dagger e^-i omega_d  t)\nendalign\n\nWe calculate now the dynamics for this effective model and compare it with the full system. Note that this Hamiltonian is quadratic, which means that a second order description is exact.\n\n@variables gΩ # g^2/4Ω\nH_a = Hf - N * gΩ * (a + a')^2 # effective Hamiltonian, N is added for the sake of intensitivity\n\neqs_a = meanfield([a, a'a, a * a], H_a, [b]; rates = [κ], order = 2)\nnothing # hide\n\nbeginalign\nfracddt langle arangle  = -1 i omega langle arangle  + 2 i N gOmega left( langle a^daggerrangle  + langle arangle  right) -1 i eta sinhleft( xi right) e^1 i t omegad -1 i eta e^-1 i t omegad cosh(xi)^* -05 kappa coshleft( xi right) langle arangle  cosh(xi)^* + 05 kappa sinhleft( xi right) langle arangle  sinh(xi)^* \nfracddt langle a^dagger  arangle  = kappa sinhleft( xi right) sinh(xi)^* + 2 i N gOmega langle a^dagger  a^daggerrangle  -2 i N gOmega langle a  arangle  + kappa sinhleft( xi right) sinh(xi)^* langle a^dagger  arangle  -1 i eta sinhleft( xi right) langle a^daggerrangle  e^1 i t omegad + 1 i eta coshleft( xi right) langle arangle  e^1 i t omegad -1 i eta langle a^daggerrangle  e^-1 i t omegad cosh(xi)^* + 1 i eta langle arangle  e^-1 i t omegad sinh(xi)^* -10 kappa coshleft( xi right) cosh(xi)^* langle a^dagger  arangle  \nfracddt langle a  arangle  = 2 i N gOmega -2 i omega langle a  arangle  + 4 i N gOmega left( langle a^dagger  arangle  + langle a  arangle  right) -10 kappa sinhleft( xi right) cosh(xi)^* + kappa sinhleft( xi right) sinh(xi)^* langle a  arangle  -10 kappa coshleft( xi right) cosh(xi)^* langle a  arangle  -2 i eta sinhleft( xi right) langle arangle  e^1 i t omegad -2 i eta langle arangle  e^-1 i t omegad cosh(xi)^*\nendalign\n\nsys_a = System(eqs_a; name = :sys_a) # symbolic ordinary differential equation system\nsys_a = mtkcompile(sys_a)\n\nu0_a = zeros(ComplexF64, length(eqs_a)) # initial state\n\ngΩ_ = g_^2 / (4Ω_) # Additional parameter\nN_ = 69 # the final result does not depend on N\n\nps_a = [ω, ωd, η, κ, N, gΩ, ξ] # symbolic parameter list (matches sys_a)\np0_a = [ω_, ωd_, η_, κ_, N_, gΩ_, ξ_]\n\nu0_a_dict = initial_values(eqs_a, u0_a)\ndict_a = merge(u0_a_dict, Dict(ps_a .=> p0_a))\nprob_a = ODEProblem(sys_a, dict_a, (0.0, 4π / ωd_)) # define and solve numeric ordinary differential equation problem\nsol_a = solve(prob_a, Tsit5(); saveat = π / 30ωd_, reltol = 1.0e-8, abstol = 1.0e-8)\nnothing # hide\n\nsol = sol_ls[4] # plot results\nt_ = sol.t\nadag_a = get_solution(sol, a' * a, eqs_sc).(t_)\naa = get_solution(sol, a * a, eqs_sc).(t_)\nadag_adag = get_solution(sol, a' * a', eqs_sc).(t_)\na_ = get_solution(sol, a, eqs_sc).(t_)\nadag = get_solution(sol, a', eqs_sc).(t_)\nsqx = adag_adag + aa + 2 * adag_a .+ 1 - (adag + a_) .^ 2\nsqy = adag_adag + aa - 2 * adag_a .- 1 - (adag - a_) .^ 2\n\nt_a = sol_a.t\nadag_a_a = get_solution(sol_a, a' * a, eqs_a).(t_a)\naa_a = get_solution(sol_a, a * a, eqs_a).(t_a)\nadag_adag_a = get_solution(sol_a, a' * a', eqs_a).(t_a)\na_a = get_solution(sol_a, a, eqs_a).(t_a)\nadag_a2 = get_solution(sol_a, a', eqs_a).(t_a)\nsqx_a = adag_adag_a + aa_a + 2 * adag_a_a .+ 1 - (adag_a2 + a_a) .^ 2\nsqy_a = adag_adag_a + aa_a - 2 * adag_a_a .- 1 - (adag_a2 - a_a) .^ 2\n\np = plot(xlabel = \"ω t\", ylabel = \"Δ² O\")\nplot!(p, t_, real.(sqx), label = \"X - Full model\")\nplot!(p, t_, -real.(sqy), label = \"P - Full model\", ls = :dash)\nplot!(p, t_a, real.(sqx_a), label = \"X - Effective model\")\nplot!(p, t_a, -real.(sqy_a), label = \"P - Effective model\", ls = :dash)\nplot(p, size = (500, 200))","title":"Effective model"},{"category":"section","location":"examples/unique_squeezing/#Package-versions","page":"Unique Steady-State Squeezing","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status([\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\"], mode = PKGMODE_MANIFEST)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/cavity_antiresonance_indexed/#Cavity-Antiresonance","page":"Cavity Antiresonance","text":"In this example we investigate a system of N closely spaced quantum emitters inside a coherently driven single mode cavity. The model is described in D. Plankensteiner, et. al., Phys. Rev. Lett. 119, 093601 (2017). The Hamiltonian of this system is composed of three parts H = H_c + H_a + H_mathrmint, the driven cavity H_c, the dipole-dipole interacting atoms H_a and the atom-cavity interaction H_mathrmint:\n\nbeginalign\nH_mathrmc = hbar Delta_c a^dagger a + hbar eta (a^dagger + a) \n\nH_a = hbar Delta_a sumlimits_j sigma_j^22 + hbar sumlimits_i neq j Omega_ij sigma_i^21 sigma_j^12\n\nH_mathrmint = hbar sumlimits_j g_j (a^dagger sigma_j^12 + a sigma_j^21)\nendalign\n\nAdditionally the system features two decay channels, the lossy cavity with photon decay rate kappa and collective atomic emission described by the decay-rate matrix Gamma_ij.\n\nWe start by loading the packages.\n\nusing QuantumCumulants\nusing OrdinaryDiffEqTsit5, ModelingToolkitBase\nusing Plots\n\nThe Hilbert space for this system is given by one cavity mode and N two-level atoms. Here we use symbolic indices, sums and double sums to define the system. The parameters g_j  Gamma_ij and Omega_ij are defined as indexed variables of atom i and j. We will describe the system in first order mean-field.\n\nhc = FockSpace(:cavity) # Hilbert space\nha = NLevelSpace(Symbol(:atom), 2)\nh = hc ⊗ ha\n\n@variables N Δc η Δa κ # Parameters\ng(i) = IndexedVariable(:g, i)\nΓ(i, j) = DoubleIndexedVariable(:Γ, i, j)\nΩ(i, j) = DoubleIndexedVariable(:Ω, i, j; identical = false)\n\n\ni = Index(h, :i, N, ha) # Indices\nj = Index(h, :j, N, ha)\n\nThe kwarg ’identical=false’ for the double indexed variable specifies that Omega_ij = 0 for i = j. Now we create the operators on the composite Hilbert space using the textttIndexedOperator constructor, which assigns each textttTransition operator an textttIndex.\n\n@qnumbers a::Destroy(h)\nσ(x, y, k) = IndexedOperator(Transition(h, :σ, x, y), k)\nnothing # hide\n\nWe define the Hamiltonian and Liouvillian. For the collective atomic decay we write the corresponding dissipative processes with a double indexed variable R_ij and an indexed jump operator J_j, such that an operator average langle mathcalO rangle follows the equation\n\nbeginequation\nlangle dotmathcalO rangle = sum_ij R_ij left( langle J_i^dagger mathcalO J_j rangle - frac12 langle J_i^dagger J_j mathcalO rangle - frac12 langle mathcalO J_i^dagger J_j rangle right)\nendequation\n\nThe inner dipole-dipole sum excludes the diagonal i == j by passing the non_equal vector [i] to the inner Σ (SQA v0.5 replaced the old non_equal=true keyword with this explicit form).\n\nHc = Δc * a'a + η * (a' + a) # Hamiltonian\nHa = Δa * Σ(σ(2, 2, i), i) + Σ(Σ(Ω(i, j) * σ(2, 1, i) * σ(1, 2, j), j, [i]), i)\nHi = Σ(g(i) * (a' * σ(1, 2, i) + a * σ(2, 1, i)), i)\nH = Hc + Ha + Hi\n\nJ = [a, σ(1, 2, i)] # Jump operators\nrates = [κ, Γ(i, j)]\nnothing # hide\n\nWe derive the system of equations in first order mean-field.\n\neqs = meanfield(a, H, J; rates = rates, order = 1)\ncomplete!(eqs)\nnothing # hide\n\nbeginalign\nfracddt langle arangle  = -1 i eta -1 i undersetioversetNsum g_i  langle sigma_i^12rangle  -05 kappa langle arangle  -1 i Deltac langle arangle  \nfracddt langle sigma_k^12rangle  = undersetjnekoversetNsum Gamma_kj  langle sigma_j^12rangle   langle sigma_k^22rangle  -05 undersetjoversetNsum Gamma_kj  langle sigma_j^12rangle  -1 i undersetjneikoversetNsum Omega_kj  langle sigma_j^12rangle  + 2 i undersetjneikoversetNsum Omega_kj  langle sigma_j^12rangle   langle sigma_k^22rangle  -1 i g_k langle arangle  -1 i Deltaa langle sigma_k^12rangle  + 2 i g_k langle arangle  langle sigma_k^22rangle  \nfracddt langle sigma_k^22rangle  = -05 undersetinejkoversetNsum Gamma_ik  langle sigma_i^21rangle   langle sigma_k^12rangle  + 1 i undersetinejkoversetNsum Omega_ik  langle sigma_i^21rangle   langle sigma_k^12rangle  -1 i undersetjneikoversetNsum Omega_kj  langle sigma_k^21rangle   langle sigma_j^12rangle  -05 undersetjnekoversetNsum Gamma_kj  langle sigma_k^21rangle   langle sigma_j^12rangle  -10 Gamma_kk langle sigma_k^22rangle  -1 i g_k langle arangle  langle sigma_k^21rangle  + 1 i g_k langle a^daggerrangle  langle sigma_k^12rangle\nendalign\n\nTo create the equations for a specific number of atoms we use the function evaluate.\n\nN_ = 2\neqs_ = evaluate(eqs; limits = (N => N_))\nsys = mtkcompile(System(eqs_; name = :sys))\nnothing # hide\n\nFinally we need to define the initial state of the system and the numerical parameters. In the end we want to obtain the transmission rate T of our system. For this purpose we calculate the steady state photon number in the cavity langle a rangle^2 for different laser frequencies.\n\nu0 = zeros(ComplexF64, length(eqs_.states))\nΓ_ = 1.0 # parameter\nd = 2π * 0.08 #0.08λ\nθ = π / 2\n\nΩij(i, j) =\n    i == j ? 0 :\n    Γ_ * (-3 / 4) * ((1 - (cos(θ))^2) * cos(d) / d - (1 - 3 * (cos(θ))^2) * (sin(d) / (d^2) + (cos(d) / (d^3))))\nΓij(i, j) =\n    i == j ? Γ_ :\n    Γ_ * (3 / 2) * ((1 - (cos(θ))^2) * sin(d) / d + (1 - 3 * (cos(θ))^2) * ((cos(d) / (d^2)) - (sin(d) / (d^3))))\n\ng_ = 2Γ_\nκ_ = 20Γ_\nΔa_ = 0Γ_\nΔc_ = 0Γ_\nη_ = κ_ / 100\n\ngi_ = [g_ * (-1)^k for k in 1:N_]\nΓij_ = [Γij(k, l) for k in 1:N_, l in 1:N_]\nΩij_ = [Ωij(k, l) for k in 1:N_, l in 1:N_]\n\nΔ_ls = [-10:0.05:10;] * Γ_\nn_ls = zeros(length(Δ_ls))\nfor k in eachindex(Δ_ls)\n    Δc_i = Δ_ls[k]\n    Δa_i = Δc_i + Ωij(1, 2)\n    p = parameter_map(\n        eqs_, Dict(\n            Δc => Δc_i, η => η_, Δa => Δa_i, κ => κ_,\n            g(i) => gi_, Γ(i, j) => Γij_, Ω(i, j) => Ωij_,\n        )\n    )\n    prob_ = ODEProblem(sys, merge(initial_values(eqs_, u0), Dict(p)), (0.0, 20.0))\n    sol_ = solve(prob_, Tsit5())\n    n_ls[k] = abs2(get_solution(sol_, a, eqs_)(sol_.t[end]))\nend\nnothing # hide\n\nThe transmission rate T with respect to the pump laser detuning is given by the relative steady state intra-cavity photon number n(Delta)n_mathrmmax. We qualitatively reproduce the antiresonance from D. Plankensteiner, et. al., Phys. Rev. Lett. 119, 093601 (2017) for two atoms.\n\nT = n_ls ./ maximum(n_ls)\nplot(Δ_ls, T, xlabel = \"Δ/Γ\", ylabel = \"T\", legend = false)","title":"Cavity Antiresonance"},{"category":"section","location":"examples/cavity_antiresonance_indexed/#Package-versions","page":"Cavity Antiresonance","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\", \"ModelingToolkitBase\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"api/#API","page":"API","text":"Pages = [\"API.md\"]\nDepth = 2:3","title":"API"},{"category":"section","location":"api/#API:-Hilbert-Spaces","page":"API","text":"","title":"Hilbert Spaces"},{"category":"section","location":"api/#API:-Operators","page":"API","text":"","title":"q-Numbers"},{"category":"section","location":"api/#API:-Meanfield","page":"API","text":"","title":"Mean field"},{"category":"section","location":"api/#API:-Average","page":"API","text":"","title":"Average"},{"category":"section","location":"api/#API:-Introspection","page":"API","text":"","title":"Introspection"},{"category":"section","location":"api/#API:-correlation","page":"API","text":"","title":"Correlation functions"},{"category":"section","location":"api/#API:-Sums","page":"API","text":"","title":"Symbolic Summations"},{"category":"section","location":"api/#API:-Measurement-Backaction","page":"API","text":"","title":"Measurement Backaction"},{"category":"section","location":"api/#API:-Utils","page":"API","text":"","title":"Utility functions"},{"category":"type","location":"api/#SecondQuantizedAlgebra.HilbertSpace","page":"API","text":"HilbertSpace\n\nAbstract supertype for Hilbert spaces. Concrete subtypes: FockSpace, NLevelSpace, PauliSpace, SpinSpace, PhaseSpace, and ProductSpace for tensor products. Compose with ⊗ or tensor.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.HilbertSpace"},{"category":"type","location":"api/#SecondQuantizedAlgebra.ProductSpace","page":"API","text":"ProductSpace{T} <: HilbertSpace\n\nComposite Hilbert space formed by the tensor product of multiple subspaces. The type parameter T is a concrete Tuple type for type-stable storage. Constructed via ⊗ or tensor, not directly.\n\nOperators on a ProductSpace take a positional index specifying which subspace they act on, e.g. Destroy(h, :a, 1).\n\nExamples\n\njulia> FockSpace(:cavity) ⊗ NLevelSpace(:atom, 2)\nℋ(cavity) ⊗ ℋ(atom)\n\nSee also ⊗, tensor.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.ProductSpace"},{"category":"type","location":"api/#SecondQuantizedAlgebra.FockSpace","page":"API","text":"FockSpace(name::Symbol) <: HilbertSpace\n\nHilbert space for a bosonic mode. Hosts Destroy and Create operators satisfying a a^dagger = 1.\n\nExamples\n\njulia> FockSpace(:cavity)\nℋ(cavity)\n\nSee also Destroy, Create, ⊗.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.FockSpace"},{"category":"type","location":"api/#SecondQuantizedAlgebra.NLevelSpace","page":"API","text":"NLevelSpace(name::Symbol, n::Int)\nNLevelSpace(name::Symbol, n::Int, ground_state::Int)\nNLevelSpace(name::Symbol, levels::Tuple{Vararg{Symbol}})\n\nHilbert space for an N-level system (atoms, qubits, qudits). Hosts Transition operators iranglelangle j cdot kranglelangle l = delta_jk iranglelangle l.\n\nThe ground_state (default 1) selects the projector granglelangle g that the canonical basis omits; arithmetic keeps it atomic and expand_completeness materialises granglelangle g = 1 - sum_k neq gkranglelangle k on demand. Levels can be integers (default 1:n) or symbolic names.\n\nExamples\n\njulia> NLevelSpace(:atom, 3)\nℋ(atom)\n\njulia> NLevelSpace(:atom, (:g, :e))\nℋ(atom)\n\nSee also Transition, expand_completeness.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.NLevelSpace"},{"category":"type","location":"api/#SecondQuantizedAlgebra.PauliSpace","page":"API","text":"PauliSpace(name::Symbol) <: HilbertSpace\n\nHilbert space for a two-level system. Hosts Pauli operators satisfying sigma_j sigma_k = delta_jk I + i epsilon_jkl sigma_l.\n\nExamples\n\njulia> PauliSpace(:spin)\nℋ(spin)\n\nSee also Pauli, SpinSpace.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.PauliSpace"},{"category":"type","location":"api/#SecondQuantizedAlgebra.SpinSpace","page":"API","text":"SpinSpace(name::Symbol) <: HilbertSpace\n\nHilbert space for collective spin angular momentum. Hosts Spin operators satisfying S_j S_k = i epsilon_jkl S_l. The algebra is independent of the spin size S, which enters only via the QuantumOpticsBase.SpinBasis chosen for numeric evaluation.\n\nExamples\n\njulia> SpinSpace(:S)\nℋ(S)\n\nSee also Spin, PauliSpace.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.SpinSpace"},{"category":"type","location":"api/#SecondQuantizedAlgebra.PhaseSpace","page":"API","text":"PhaseSpace(name::Symbol) <: HilbertSpace\n\nHilbert space for position and momentum quadratures. Hosts Position and Momentum operators satisfying p x = -i; arithmetic canonicalizes products to place position left of momentum.\n\nExamples\n\njulia> PhaseSpace(:osc)\nℋ(osc)\n\nSee also Position, Momentum, FockSpace.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.PhaseSpace"},{"category":"function","location":"api/#QuantumInterface.:⊗","page":"API","text":"⊗(spaces::HilbertSpace...)\n\nCreate a ProductSpace from multiple Hilbert spaces. Flattens nested ProductSpace arguments so that (A ⊗ B) ⊗ C == A ⊗ B ⊗ C. Unicode input \\otimes<tab>; ASCII alias tensor.\n\nExamples\n\njulia> FockSpace(:a) ⊗ FockSpace(:b) ⊗ NLevelSpace(:atom, 2)\nℋ(a) ⊗ ℋ(b) ⊗ ℋ(atom)\n\nSee also ProductSpace, tensor.\n\n\n\n\n\n","title":"QuantumInterface.:⊗"},{"category":"function","location":"api/#QuantumInterface.tensor","page":"API","text":"tensor(spaces::HilbertSpace...)\n\nASCII alias for ⊗: create a ProductSpace from multiple Hilbert spaces.\n\nExamples\n\njulia> tensor(FockSpace(:a), FockSpace(:b))\nℋ(a) ⊗ ℋ(b)\n\nSee also ⊗, ProductSpace.\n\n\n\n\n\n","title":"QuantumInterface.tensor"},{"category":"type","location":"api/#SecondQuantizedAlgebra.QSym","page":"API","text":"QSym <: QField\n\nAbstract type for fundamental (leaf) operators in the expression tree.\n\nEvery QSym carries three fields identifying its site:\n\nname::Symbol — display name\nspace_index::Int — position in ProductSpace (1 for single spaces)\nindex::Index — symbolic summation index (Index), or NO_INDEX\n\nConcrete subtypes: Destroy, Create, Transition, Pauli, Spin, Position, Momentum.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.QSym"},{"category":"type","location":"api/#SecondQuantizedAlgebra.QAdd","page":"API","text":"QAdd <: QField\n\nThe sole compound expression type: a sum of eagerly-ordered operator products.\n\nAll arithmetic on QSym operators produces a QAdd. Iterating over a QAdd yields Pair{QTerm, CNum} entries; read term.ops for the operator sequence and term.ne for the scoped index constraints.\n\nSee also QTerm, prefactor, operators, Σ, constraint_pairs.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.QAdd"},{"category":"type","location":"api/#SecondQuantizedAlgebra.QTerm","page":"API","text":"QTerm\n\nA single entry of a QAdd sum: an ordered operator product plus the pairwise index inequality constraints that scope that product.\n\nFields\n\nops::Vector{QSym} — the ordered operator product\nne::Vector{NonEqualPair} — pairwise α ≠ β constraints (canonicalized)\n\nQTerm is the dict key in QTermDict; iterating a QAdd yields Pair{QTerm, CNum}, and callers reach term.ops / term.ne directly.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.QTerm"},{"category":"function","location":"api/#SecondQuantizedAlgebra.prefactor","page":"API","text":"prefactor(s::QAdd) -> CNum\n\nReturn the Complex{Num} prefactor of a single-term QAdd.\n\nThrows ArgumentError if s contains more than one term. For multi-term expressions, iterate over the QAdd directly.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> prefactor(2 * a' * a)\n2\n\nSee also operators, sorted_arguments.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.prefactor"},{"category":"function","location":"api/#SecondQuantizedAlgebra.operators","page":"API","text":"operators(eqs::AbstractMeanfieldEquations)\n\nThe operator products on the left-hand sides, one per tracked moment. Paired with states by position (states(eqs)[i] == average(operators(eqs)[i])).\n\n\n\n\n\noperators(s::QAdd) -> Vector{QSym}\n\nReturn the ordered operator sequence of a single-term QAdd.\n\nThrows ArgumentError if s contains more than one term. For multi-term expressions, iterate over the QAdd directly and read term.ops from each QTerm.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> length(operators(a' * a))\n2\n\nSee also prefactor, sorted_arguments.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.operators"},{"category":"function","location":"api/#SecondQuantizedAlgebra.normal_order","page":"API","text":"normal_order(expr::QField) -> QAdd\n\nRoute every term of expr through the canonicalization pipeline.\n\nIn practice this is the identity on anything built through public arithmetic: *, +, -, ^, commutator, Σ, substitute, and adjoint all canonicalize eagerly, so the result of any such call is already normal-ordered. Reach for normal_order explicitly only when an expression was assembled through low-level internals that bypass the arithmetic, or when interfacing with code that expects a finalizer call. simplify uses it internally before simplifying coefficients.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> a * a'\n1 + a' * a\n\njulia> normal_order(a * a')\n1 + a' * a\n\nSee also simplify, expand_completeness, normal_to_symmetric, symmetric_to_normal.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.normal_order"},{"category":"function","location":"api/#SecondQuantizedAlgebra.qadjoint","page":"API","text":"qadjoint(x)\n\nHermitian conjugate that distributes through mixed operator/symbolic expressions.\n\nOn a QField returns adjoint(x). On a Number returns adjoint(x). On a SymbolicUtils.BasicSymbolic tree, recurses into arguments so coefficients distribute (e.g. qadjoint(2im * a) becomes -2im * a') rather than producing an opaque conj(...) wrapper.\n\nAliased as qconj and dagger.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.qadjoint"},{"category":"function","location":"api/#SecondQuantizedAlgebra.inner_adjoint","page":"API","text":"inner_adjoint(x)\n\nPush the adjoint inside ⟨...⟩ averages: rewrites conj(⟨X⟩) as ⟨X†⟩ so the result stays expressed as an average of an operator rather than a conj wrapper around one. Used when building equations of motion where both sides must share the canonical \"average-of-operator\" form for substitution and hashing.\n\nOn non-average sub-expressions, behaves like qadjoint.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.inner_adjoint"},{"category":"function","location":"api/#SecondQuantizedAlgebra.symmetric_to_normal","page":"API","text":"symmetric_to_normal(expr) -> QAdd\n\nConvert expr from symmetric (Weyl) to normal ordering. Exact inverse of normal_to_symmetric.\n\nExamples\n\njulia> hf = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(hf);\n\njulia> symmetric_to_normal(normal_to_symmetric(a' * a))\na' * a\n\njulia> hp = PhaseSpace(:osc);\n\njulia> @qnumbers x::Position(hp) p::Momentum(hp);\n\njulia> symmetric_to_normal(normal_to_symmetric(x * p))\nx * p\n\nSee also normal_to_symmetric, normal_order.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.symmetric_to_normal"},{"category":"function","location":"api/#SecondQuantizedAlgebra.normal_to_symmetric","page":"API","text":"normal_to_symmetric(expr) -> QAdd\n\nConvert expr from normal to symmetric (Weyl) ordering. Exact inverse of symmetric_to_normal.\n\nExamples\n\njulia> hf = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(hf);\n\njulia> normal_to_symmetric(a' * a)\n-1//2 + a' * a\n\njulia> hp = PhaseSpace(:osc);\n\njulia> @qnumbers x::Position(hp) p::Momentum(hp);\n\njulia> normal_to_symmetric(x * p)\n1//2im + x * p\n\nSee also symmetric_to_normal, normal_order.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.normal_to_symmetric"},{"category":"function","location":"api/#SecondQuantizedAlgebra.expand_completeness","page":"API","text":"expand_completeness(q) -> QAdd\n\nRewrite every ground-state projector sigma^gg in q via the completeness relation sigma^gg = 1 - sum_k neq g sigma^kk.\n\n* keeps sigma^gg atomic; reach for expand_completeness when downstream code needs the projector eliminated, e.g. before converting to a numeric basis where the sigma^kk for k neq g form the independent degrees of freedom.\n\nExamples\n\njulia> h = NLevelSpace(:atom, 2);\n\njulia> σ11 = Transition(h, :σ, 1, 1);\n\njulia> expand_completeness(σ11)\n1 - σ₂₂\n\nSee also assume_distinct_index, normal_order.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.expand_completeness"},{"category":"function","location":"api/#SecondQuantizedAlgebra.anticommutator","page":"API","text":"anticommutator(a, b) -> QAdd\n\nReturn the anticommutator a b = ab + ba as a QAdd.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> anticommutator(a, a')\n1 + 2 * a' * a\n\nSee also commutator.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.anticommutator"},{"category":"function","location":"api/#SecondQuantizedAlgebra.is_average","page":"API","text":"is_average(x) -> Bool\n\nWhether x is a symbolic average object created by average.\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> is_average(average(a)), is_average(a)\n(true, false)\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.is_average"},{"category":"function","location":"api/#SecondQuantizedAlgebra.undo_average","page":"API","text":"undo_average(expr) -> QAdd\n\nRecursively strip symbolic averages and return the underlying operator expression. Summation metadata is restored. Also accepts a Symbolics.Equation, returning a Pair{QAdd, QAdd}.\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> undo_average(average(a' * a)) == a' * a\ntrue\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.undo_average"},{"category":"function","location":"api/#SymbolicUtils.substitute","page":"API","text":"substitute(expr, d::Dict)\n\nSubstitute symbolic parameters and/or operators in expr using dictionary d.\n\nSupports both symbolic coefficient replacement (for example x => 2) and operator replacement. The result is re-canonicalized and returned as a QAdd.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> @variables x;\n\njulia> substitute(x * a' * a, Dict(x => 2))\n2 * a' * a\n\nSee also change_index.\n\n\n\n\n\n","title":"SymbolicUtils.substitute"},{"category":"function","location":"api/#SymbolicUtils.simplify","page":"API","text":"simplify(expr::QField) -> QAdd\n\nNormal-order expr, then simplify each coefficient symbolically and drop summation indices that no surviving term depends on.\n\nThe operator-level work (commutation, same-site composition, like-term collection) all happens inside normal_order. What simplify adds on top is purely at the symbolic-coefficient layer: Symbolics.expand followed by SymbolicUtils.simplify runs on each surviving prefactor, and any term whose coefficient simplifies to zero is dropped. A final pass removes summation indices that no remaining term references.\n\nThat symbolic step is expensive, so reach for simplify as a finalizer when cancellations or accumulated symbolic factors need to be folded; use normal_order for intermediate steps.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> @variables x y;\n\njulia> expr = (x^2 + 2x*y + y^2) * a' * a - (x + y)^2 * a' * a\n(x^2 - ((x + y)^2) + 2x*y + y^2) * a' * a\n\njulia> simplify(expr)\n0\n\nSee also normal_order, expand, expand_completeness.\n\n\n\n\n\n","title":"SymbolicUtils.simplify"},{"category":"function","location":"api/#SymbolicUtils.expand","page":"API","text":"expand(expr::QField) -> QAdd\n\nExpand the symbolic prefactor of each term via Symbolics.expand.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> @variables x y;\n\njulia> expand((x + y)^2 * a)\n(x^2 + 2x*y + y^2) * a\n\nSee also simplify.\n\n\n\n\n\n","title":"SymbolicUtils.expand"},{"category":"macro","location":"api/#SecondQuantizedAlgebra.@qnumbers","page":"API","text":"@qnumbers ops...\n\nConvenience macro for constructing named quantum operators.\n\nEach argument has the form name::OperatorType(hilbert_space, args...). The macro calls OperatorType(hilbert_space, :name, args...) and binds the result to name in the calling scope. Multiple operators can be declared in one call.\n\nExamples\n\njulia> h = FockSpace(:fock);\n\njulia> @qnumbers a::Destroy(h)\n(a,)\n\nSee also Destroy, Transition, Pauli, Spin.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.@qnumbers"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Destroy","page":"API","text":"Destroy <: QSym\n\nBosonic annihilation operator a on a FockSpace. Satisfies the canonical commutation relation a a^dagger = 1. The adjoint a' returns the corresponding Create operator.\n\nExamples\n\njulia> h = FockSpace(:cavity);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> a * a'\n1 + a' * a\n\nSee also Create, FockSpace, @qnumbers.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Destroy"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Create","page":"API","text":"Create <: QSym\n\nBosonic creation operator a^dagger on a FockSpace. The adjoint of Destroy; typically obtained via a' rather than constructed directly.\n\nSee also Destroy, FockSpace.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Create"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Transition","page":"API","text":"Transition <: QSym\n\nTransition operator iranglelangle j on an NLevelSpace. Satisfies the composition rule iranglelangle j cdot kranglelangle l = delta_jk iranglelangle l, with adjoint iranglelangle j^dagger = jranglelangle i.\n\nEach Transition carries the ground-state level and number of levels of its host NLevelSpace so the eager arithmetic can keep sigma^gg atomic in canonical form (use expand_completeness to materialise the identity granglelangle g = 1 - sum_k neq gkranglelangle k when needed).\n\nExamples\n\njulia> h = NLevelSpace(:atom, 3);\n\njulia> σ = Transition(h, :σ, 1, 2);\n\njulia> σ * σ'\nσ₁₁\n\njulia> σ' * σ\nσ₂₂\n\nSee also NLevelSpace, expand_completeness, @qnumbers.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Transition"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Pauli","page":"API","text":"Pauli <: QSym\n\nPauli operator sigma_x sigma_y sigma_z on a PauliSpace. The axis field selects the component: 1 = x, 2 = y, 3 = z. Hermitian (σ' == σ) and satisfies sigma_j sigma_k = delta_jk I + i epsilon_jkl sigma_l.\n\nExamples\n\njulia> h = PauliSpace(:s);\n\njulia> σx = Pauli(h, :σ, 1); σy = Pauli(h, :σ, 2);\n\njulia> σx * σy\nim * σz\n\njulia> σx * σx\n1\n\nSee also PauliSpace, Spin.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Pauli"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Spin","page":"API","text":"Spin <: QSym\n\nAngular momentum operator S_x S_y S_z on a SpinSpace. The axis field selects the component: 1 = x, 2 = y, 3 = z. Hermitian (S' == S) and satisfies S_j S_k = i epsilon_jkl S_l (applied eagerly by *).\n\nExamples\n\njulia> h = SpinSpace(:S);\n\njulia> Sx = Spin(h, :S, 1); Sy = Spin(h, :S, 2);\n\njulia> Sy * Sx\n-im * Sz + Sx * Sy\n\nSee also SpinSpace, Pauli.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Spin"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Position","page":"API","text":"Position <: QSym\n\nPosition (quadrature) operator x on a PhaseSpace. Hermitian (x' == x) and related to Fock operators by x = (a + a^dagger)sqrt2. Canonical pair with Momentum: p x = -i.\n\nExamples\n\njulia> h = PhaseSpace(:osc);\n\njulia> @qnumbers x::Position(h) p::Momentum(h);\n\njulia> p * x\n-im + x * p\n\nSee also Momentum, PhaseSpace.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Position"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Momentum","page":"API","text":"Momentum <: QSym\n\nMomentum (quadrature) operator p on a PhaseSpace. Hermitian (p' == p) and related to Fock operators by p = i(a^dagger - a)sqrt2. Canonical pair with Position: p x = -i.\n\nSee also Position, PhaseSpace.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Momentum"},{"category":"function","location":"api/#QuantumCumulants.meanfield","page":"API","text":"meanfield(ops, H, J=QField[]; kwargs...)\nmeanfield(op, H, J=QField[]; kwargs...)\n\nEquations of motion for the averages of ops under the Hamiltonian H and the collapse operators J. The result is the Heisenberg (quantum Langevin) equation with noise neglected: each jump adds a Lindblad term sum_i r_i left(J_i^dagger O J_i - tfrac12J_i^dagger J_i Oright). Returns a MeanfieldEquations, or a NoiseMeanfieldEquations when efficiencies is supplied (measurement backaction).\n\nThe returned right-hand sides are left unsimplified; call simplify! (or SymbolicUtils.simplify) on the equations you want to inspect.\n\nArguments\n\nops: the operator(s) whose equations of motion are derived.\nH: the operator defining the system Hamiltonian.\nJ: the collapse (jump) operators of the system; defaults to none.\n\nKeyword arguments\n\nJdagger=adjoint.(J): the adjoints of the jump operators.\nrates=ones(length(J)): decay rates for J. A square Matrix instead selects collective dissipation across the jump vector.\nefficiencies=nothing: detector efficiencies per jump. When given, the result is a NoiseMeanfieldEquations carrying a measurement-backaction noise drift.\ndirection=Forward(): Forward for ordinary evolution, Backward for retrodiction.\norder=nothing: if set, a cumulant_expansion to this order is applied. An Int caps every subspace; a Vector{Int} caps each Hilbert subspace separately.\nmix_choice=maximum: for a Vector order, how to combine the per-subspace caps on a term that acts on several subspaces.\niv=ModelingToolkitBase.t_nounits: the independent (time) variable of the system.\n\n\n\n\n\n","title":"QuantumCumulants.meanfield"},{"category":"function","location":"api/#SecondQuantizedAlgebra.commutator","page":"API","text":"commutator(a, b) -> QAdd\n\nReturn the commutator a b = ab - ba as a QAdd.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> commutator(a, a')\n1\n\nSee also anticommutator, normal_order.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.commutator"},{"category":"function","location":"api/#SecondQuantizedAlgebra.acts_on","page":"API","text":"acts_on(expr) -> Vector{Int}\n\nSorted unique space_index values that expr acts on. Works on QSym, QAdd, averaged BasicSymbolic expressions, and Numbers (Int[]).\n\njulia> h = FockSpace(:a) ⊗ NLevelSpace(:b, 2);\n\njulia> @qnumbers a::Destroy(h, 1) σ::Transition(h, 1, 2, 2);\n\njulia> acts_on(a' * a), acts_on(a' * σ)\n([1], [1, 2])\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.acts_on"},{"category":"type","location":"api/#QuantumCumulants.AbstractMeanfieldEquations","page":"API","text":"AbstractMeanfieldEquations\n\nSupertype of MeanfieldEquations and NoiseMeanfieldEquations.\n\n\n\n\n\n","title":"QuantumCumulants.AbstractMeanfieldEquations"},{"category":"type","location":"api/#QuantumCumulants.MeanfieldEquations","page":"API","text":"MeanfieldEquations\n\nConcrete equation set produced by meanfield when no measurement backaction is requested. The authoritative cumulant hierarchy is graph; the vector fields are a derived view regenerated from it.\n\nFields\n\ngraph: the source of truth (moments, drifts, treatments, sys, ctx).\nequations: the averaged differential equations (derived view).\noperator_equations: the same equations at the operator level.\nstates: the averages on the left-hand sides.\noperators: the operators on the left-hand sides.\nhamiltonian: the system Hamiltonian.\njumps, jumps_dagger: the collapse operators and their adjoints.\nrates: the decay rates corresponding to jumps.\niv: the independent (time) variable.\norder: the cumulant-expansion order, or nothing.\ndirection: Forward or Backward evolution.\n\n\n\n\n\n","title":"QuantumCumulants.MeanfieldEquations"},{"category":"function","location":"api/#SecondQuantizedAlgebra.average","page":"API","text":"average(expr) -> BasicSymbolic | Number\n\nBuild the symbolic average langle mathrmexpr rangle. Distributes over sums, pulls c-number prefactors out, leaves scalars unchanged. Displayed as ⟨…⟩.\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> avg = average(a' * a);\n\njulia> is_average(avg)\ntrue\n\njulia> avg + 1\n1 + ⟨a' * a⟩\n\nSee also undo_average, numeric_average.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.average"},{"category":"function","location":"api/#QuantumCumulants.cumulant_expansion","page":"API","text":"cumulant_expansion(expr, order; mix_choice=maximum)\n\nExpand every average in expr whose order exceeds order into lower-order moments via the joint-cumulant identity, neglecting the joint cumulant above order. order is an Int (one cap for all subspaces) or a Vector{Int} (a cap per Hilbert subspace, combined by mix_choice on mixed terms). The output is raw; apply SymbolicUtils.simplify for a canonical form.\n\nSee also: https://en.wikipedia.org/wiki/Cumulant#Joint_cumulants\n\nExamples\n\njulia> h = FockSpace(:a) ⊗ FockSpace(:b);\n\njulia> @qnumbers a::Destroy(h, 1) b::Destroy(h, 2);\n\njulia> cumulant_expansion(average(a * b), 1)\n⟨b⟩*⟨a⟩\n\n\n\n\n\ncumulant_expansion(eqs::MeanfieldEquations, order)\n\nCumulant-expand every RHS of eqs to order, returning a new MeanfieldEquations of the same shape with that order stored. The system's mix_choice (held in eqs.graph.sys) drives the mixed-order truncation. Noise systems take their order through meanfield, not here.\n\n\n\n\n\n","title":"QuantumCumulants.cumulant_expansion"},{"category":"function","location":"api/#QuantumCumulants.cumulant","page":"API","text":"cumulant(op, n=get_order(op))\n\nThe n-th joint cumulant of op (either an operator or an average), the signed sum over partitions of its operator factors into n blocks. The result is raw and unsimplified; apply SymbolicUtils.simplify for a canonical form.\n\nExamples\n\njulia> h = FockSpace(:a) ⊗ FockSpace(:b);\n\njulia> @qnumbers a::Destroy(h, 1) b::Destroy(h, 2);\n\njulia> cumulant(a * b)\n⟨a * b⟩ - ⟨b⟩*⟨a⟩\n\njulia> cumulant(a * b, 1)\n⟨a * b⟩\n\njulia> cumulant(a * b, 3)\n0\n\n\n\n\n\n","title":"QuantumCumulants.cumulant"},{"category":"function","location":"api/#QuantumCumulants.get_order","page":"API","text":"get_order(expr)\n\nHighest moment order appearing in expr, the order used to decide whether a term is expanded by cumulant_expansion. Numbers have order 0, a bare operator order 1, and a product the number of its operator factors.\n\nExamples\n\njulia> h = FockSpace(:a) ⊗ FockSpace(:b);\n\njulia> @qnumbers a::Destroy(h, 1) b::Destroy(h, 2);\n\njulia> get_order(a)\n1\n\njulia> get_order(a * b)\n2\n\njulia> get_order(1)\n0\n\n\n\n\n\n","title":"QuantumCumulants.get_order"},{"category":"function","location":"api/#QuantumCumulants.states","page":"API","text":"states(eqs::AbstractMeanfieldEquations)\n\nThe average variables  evolved by eqs, i.e. the dynamical unknowns, one per tracked moment. Paired with operators by position.\n\n\n\n\n\n","title":"QuantumCumulants.states"},{"category":"function","location":"api/#QuantumCumulants.moments","page":"API","text":"moments(eqs::AbstractMeanfieldEquations)\n\nOrdered operatormoment map: each tracked operator product to its average op. The correspondence the cumulant hierarchy is built on.\n\n\n\n\n\n","title":"QuantumCumulants.moments"},{"category":"function","location":"api/#QuantumCumulants.moment_variable_map","page":"API","text":"moment_variable_map(eqs::AbstractMeanfieldEquations)\n\nOrdered map from each moment average op to the time-dependent MTK unknown u(t) that System assigns it. The bridge initial_values, get_solution and parameter_map resolve moments against.\n\n\n\n\n\n","title":"QuantumCumulants.moment_variable_map"},{"category":"function","location":"api/#QuantumCumulants.closure_report","page":"API","text":"closure_report(eqs::AbstractMeanfieldEquations; filter_func=nothing)\n\nSummarise the closure status of eqs:\n\nn_states: number of tracked moments.\nby_order: tracked-moment count per cumulant order.\nmissing: moments still needed to close the system (find_missing).\nclosed: whether nothing is missing.\n\nfilter_func mirrors find_missing / complete!.\n\n\n\n\n\n","title":"QuantumCumulants.closure_report"},{"category":"function","location":"api/#QuantumCumulants.noise_channels","page":"API","text":"noise_channels(eqs::AbstractMeanfieldEquations)\n\nThe monitored measurement channels: one entry per jump with nonzero efficiency, each an independent Brownian in the SDE System builds. Each entry is (; index, jump, rate, efficiency); a deterministic system has none.\n\n\n\n\n\n","title":"QuantumCumulants.noise_channels"},{"category":"type","location":"api/#QuantumCumulants.CorrelationFunction","page":"API","text":"CorrelationFunction\n\nTwo-time correlation function ⟨op1(t+τ) op2(t)⟩ of a solved mean-field system, constructed via the quantum regression theorem. Build one with the CorrelationFunction(op1, op2, eqs0) constructor, then feed it to ModelingToolkitBase.System together with correlation_u0 and correlation_p0 to solve the τ-evolution, or to Spectrum for the power spectrum.\n\nFields\n\nop1, op2: the operators whose correlation is computed.\nop2_ancilla: op2 re-embedded on the ancilla subspace aon_ancilla (internal).\naon_ancilla: Hilbert-subspace index of the ancilla carrying op2 (internal).\neqs: the closed equations of motion in the delay τ.\neqs0: the steady-state system the correlation is built on.\nτ: the delay independent variable.\nsteady_state: if true, ambient averages are held constant; if false, they are evolved alongside the τ-states.\nambient: the τ-system's ambient averages (steady-state coefficients, not τ-states), computed once from the closed eqs so the u0/System/spectrum paths need not re-walk it.\n\n\n\n\n\n","title":"QuantumCumulants.CorrelationFunction"},{"category":"type","location":"api/#ModelingToolkitBase.System","page":"API","text":"ModelingToolkitBase.System(eqs::MeanfieldEquations; name)\nModelingToolkitBase.System(eqs::NoiseMeanfieldEquations; name)\n\nBuild an MTK System from the equation set: one u(t) variable per state, the drift resolved to those variables. A NoiseMeanfieldEquations becomes an SDE with one independent Brownian column per monitored channel, each carrying that channel's noise drift (sign +1 Forward, -1 Backward). RHS leaves not matching a state become parameters.\n\n\n\n\n\nModelingToolkitBase.System(c::CorrelationFunction; name)\n\nMTK System for the τ-evolution. Ambient steady-state averages become parameters (values supplied via correlation_p0).\n\n\n\n\n\n","title":"ModelingToolkitBase.System"},{"category":"type","location":"api/#QuantumCumulants.Spectrum","page":"API","text":"Spectrum(c::CorrelationFunction, ps)\n\nThe Laplace-transformed correlation as a callable: S(ω, u_end, p0) returns the symmetric power spectrum 2·Re{∫₀^∞ g(τ)e^{-iωτ}dτ}. Solves (iω·I - A)X̃ = x̃(0) where A is the τ-system Jacobian at steady state and x̃ = x - x_∞ is centred so the one-sided transform converges. The linear/anti-linear split (the conjugate columns from get_adjoints=false) is handled by the 2n augmentation.\n\nExamples\n\njulia> h = FockSpace(:cavity);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> eqs = meanfield([a' * a], a' * a, [a]; rates = [1.0], order = 2);\n\njulia> c = CorrelationFunction(a', a, eqs);\n\njulia> Spectrum(c)\nℱ(⟨a' * a⟩)(ω)\n\n\n\n\n\n","title":"QuantumCumulants.Spectrum"},{"category":"function","location":"api/#QuantumCumulants.correlation_u0","page":"API","text":"correlation_u0(c, u_end)\n\nInitial values for the τ-evolution of the CorrelationFunction c. Each ancilla state ⟨X(τ) op2ancilla⟩ starts from the steady-state value ⟨X op2⟩ (with op2 on its original subspace), looked up in `uend`.\n\nSee also: CorrelationFunction, correlation_p0.\n\n\n\n\n\n","title":"QuantumCumulants.correlation_u0"},{"category":"function","location":"api/#QuantumCumulants.correlation_p0","page":"API","text":"correlation_p0(c, u_end, ps_p0)\n\nParameter dictionary for the τ-ODE of the CorrelationFunction c: the user parameters ps_p0 together with the numeric values of the ambient steady-state averages, looked up in u_end.\n\nSee also: CorrelationFunction, correlation_u0.\n\n\n\n\n\n","title":"QuantumCumulants.correlation_p0"},{"category":"type","location":"api/#SecondQuantizedAlgebra.Index","page":"API","text":"Index(h::HilbertSpace, name::Symbol, range, space)\nIndex(h::HilbertSpace, name::Symbol, range, space_index::Int)\n\nSymbolic summation index for site- or mode-resolved operator expressions.\n\nRepresents labels such as i, j, or k that attach to operators and parameters. Pass a subspace type or integer position to specify which factor of a ProductSpace the index ranges over. Use with IndexedOperator to build objects like a_i and with Σ to build sums sum_i a_i^dagger a_i.\n\nExamples\n\njulia> h = FockSpace(:site) ⊗ FockSpace(:cavity);\n\njulia> i = Index(h, :i, 10, FockSpace(:site));\n\njulia> j = Index(h, :j, 10, 1); # same subspace via integer position\n\njulia> i == j\nfalse\n\nSee also has_index, IndexedOperator, Σ, change_index.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Index"},{"category":"function","location":"api/#SecondQuantizedAlgebra.IndexedOperator","page":"API","text":"IndexedOperator(op::QSym, i::Index) -> QSym\n\nReturn the indexed version of an operator.\n\nIndexedOperator keeps the operator type and quantum numbers, and only changes the symbolic index label. Use it to build objects such as a_i, σ_j₁₂, or x_k before forming sums with Σ.\n\nUse IndexedOperator(op, NO_INDEX) to remove an index.\n\nExamples\n\njulia> h = FockSpace(:cavity);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> i = Index(h, :i, 5, h);\n\njulia> IndexedOperator(a, i)\na_i\n\nSee also Index, Σ, change_index.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.IndexedOperator"},{"category":"function","location":"api/#SecondQuantizedAlgebra.IndexedVariable","page":"API","text":"IndexedVariable(name::Symbol, i::Index) -> Num\n\nCreate a symbolic single-indexed parameter mathrmname(i).\n\nUse this for site-dependent quantities in Hamiltonians, for example detunings or local couplings.\n\nExamples\n\njulia> h = FockSpace(:site) ⊗ FockSpace(:cavity);\n\njulia> i = Index(h, :i, 10, 1);\n\njulia> ω = IndexedVariable(:ω, i)\nω(i)\n\nSee also DoubleIndexedVariable.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.IndexedVariable"},{"category":"function","location":"api/#SecondQuantizedAlgebra.DoubleIndexedVariable","page":"API","text":"DoubleIndexedVariable(name::Symbol, i::Index, j::Index; identical::Bool=true) -> Num\n\nCreate a symbolic two-index parameter mathrmname(i j).\n\nUse this for pairwise interactions such as hopping amplitudes or coupling matrices.\n\nKeyword arguments\n\nidentical::Bool = true — if false, the variable evaluates to zero when i == j, enforcing that the parameter is only defined for distinct sites.\n\nExamples\n\njulia> h = FockSpace(:site) ⊗ FockSpace(:cavity);\n\njulia> i = Index(h, :i, 10, 1);\n\njulia> j = Index(h, :j, 10, 1);\n\njulia> J = DoubleIndexedVariable(:J, i, j; identical = false)\nJ(i, j)\n\nSee also IndexedVariable.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.DoubleIndexedVariable"},{"category":"function","location":"api/#SecondQuantizedAlgebra.Σ","page":"API","text":"Σ(expr, i::Index, non_equal::Vector{Index} = Index[])\nΣ(expr, i::Index, j::Index, rest::Index...)\n∑(expr, i::Index, ...)\n\nBuild symbolic sums over indices, for example sum_i expr.\n\nReturns a QAdd carrying i in its indices field. If expr does not depend on i, the sum is evaluated eagerly as i.range * expr.\n\nThe optional non_equal records pairwise inequality constraints i ≠ j per term. Diagonal splitting is performed automatically: if expr carries another free index j on the same Hilbert subspace as i, the contribution at i = j is emitted as a separate diagonal term and the off-diagonal term gains the constraint (i, j).\n\nMultiple positional indices create nested sums: Σ(expr, i, j) is equivalent to Σ(Σ(expr, i), j). The Unicode alias ∑ is also exported.\n\nExamples\n\njulia> h = FockSpace(:site);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> i = Index(h, :i, 3, h);\n\njulia> Σ(IndexedOperator(a', i) * IndexedOperator(a, i), i)\nΣ(i=1:3) a_i' * a_i\n\nSee also Index, IndexedOperator, constraint_pairs.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.Σ"},{"category":"function","location":"api/#SecondQuantizedAlgebra.change_index","page":"API","text":"change_index(expr, from::Index, to::Index)\n\nRename an index in an expression by replacing from with to everywhere.\n\nOperator indices are swapped directly. Symbolic prefactors (e.g. IndexedVariable, DoubleIndexedVariable) are substituted via Symbolics.substitute using the sym fields of the indices. DoubleIndexedVariable nodes with identical=false automatically evaluate to zero if the substitution makes both arguments equal.\n\nExamples\n\njulia> h = FockSpace(:site);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> i = Index(h, :i, 5, h); j = Index(h, :j, 5, h);\n\njulia> expr = IndexedVariable(:ω, i) * IndexedOperator(a, i);\n\njulia> change_index(expr, i, j)\nω(j) * a_j\n\nSee also get_indices.\n\n\n\n\n\nchange_index(expr, pairs::AbstractDict{Index, Index})\n\nSimultaneous (batched) variant of change_index: substitute every key in pairs by its mapped value in one pass.\n\nUnlike a sequence of single-pair change_index calls, this never produces an intermediate state where a renamed-into index temporarily collides with another encountered index. Use it when the rename describes a permutation or any map whose image overlaps its domain, e.g. swapping two indices:\n\njulia> change_index(expr, Dict(i => j, j => i))   # swap, not fuse\n\nA pair (idx => idx) is a no-op; change_index(expr, Dict()) returns expr unchanged.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.change_index"},{"category":"function","location":"api/#SecondQuantizedAlgebra.get_indices","page":"API","text":"get_indices(expr) -> Vector{Index}\n\nReturn all symbolic indices that appear in expr.\n\nReturns a Vector{Index} of unique indices found in operator fields and summation metadata. Excludes the sentinel NO_INDEX.\n\nExamples\n\njulia> h = FockSpace(:site);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> i = Index(h, :i, 5, h);\n\njulia> get_indices(IndexedOperator(a, i))\n1-element Vector{Index}:\n i\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.get_indices"},{"category":"function","location":"api/#SecondQuantizedAlgebra.has_index","page":"API","text":"has_index(idx::Index) -> Bool\n\nReturn true when idx is an actual symbolic index.\n\nReturns false for the sentinel NO_INDEX, used for operators without an attached index.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.has_index"},{"category":"function","location":"api/#SecondQuantizedAlgebra.assume_distinct_index","page":"API","text":"assume_distinct_index(q::QAdd, pairs::Vector{Tuple{Index, Index}}) -> QAdd\n\nRe-canonicalize q under the declared pairwise ≠ constraints on free indices, then run expand_completeness so any ground-state projectors that emerge from same-site composition are expanded.\n\nUse this when two free indices semantically range over distinct atoms or modes but no Σ supplies the constraint. SQA cannot infer \"different symbol implies different site\" automatically: two operators carrying different free indices on the same Hilbert subspace remain in their physical order, and no same-site collapse fires between them. Declaring the pair distinct here lets the canonical sort resolve their relationship and triggers any composition or completeness rewriting it unlocks.\n\nExamples\n\njulia> h = NLevelSpace(:atom, 2);\n\njulia> j = Index(h, :j, 5, h); k = Index(h, :k, 5, h);\n\njulia> σ(i, m, idx) = IndexedOperator(Transition(h, :σ, i, m), idx);\n\njulia> q = σ(2, 1, k) * σ(1, 2, j);\n\njulia> assume_distinct_index(q, [(j, k)])\nσ_j₁₂ * σ_k₂₁\n\nSee also expand_completeness.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.assume_distinct_index"},{"category":"function","location":"api/#SecondQuantizedAlgebra.index_slot","page":"API","text":"index_slot(x) -> Union{Int, Nothing}\n\nConcrete slot integer of a per-slot index sym minted by (i::Index)(k), or nothing for any other symbol. Lets consumers recover the position k without parsing the symbol's name.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.index_slot"},{"category":"function","location":"api/#QuantumCumulants.evaluate","page":"API","text":"evaluate(eqs::AbstractMeanfieldEquations; limits=nothing, h=Int[])\nevaluate(c::CorrelationFunction; limits=nothing, h=Int[])\n\nMaterialise a symbolic indexed system into a concrete, fixed-size one by inserting the index ranges and unrolling the sums. The result is ready for ModelingToolkitBase.System; its operators carry no remaining sum scope.\n\nKeyword arguments\n\nlimits=nothing: the concrete size of each symbolic index range, given as a Pair (N => 3), a tuple of pairs, or a Dict. Required whenever a range bound is symbolic.\nh=Int[]: restrict unrolling to these Hilbert subspaces (by acts_on index). Empty unrolls every subspace covered by limits.\n\n\n\n\n\n","title":"QuantumCumulants.evaluate"},{"category":"function","location":"api/#QuantumCumulants.scale","page":"API","text":"scale(eqs::AbstractMeanfieldEquations; h=Int[])\nscale!(eqs::AbstractMeanfieldEquations; h=Int[])\n\nReduce a permutation-symmetric indexed system to one representative per symmetry class, exploiting that every atom of an indexed family acts on the system identically. scale! mutates eqs in place; scale returns a new system.\n\nKeyword arguments\n\nh=Int[]: the Hilbert subspaces to scale, given by their acts_on indices. Empty scales every symmetric (indexed) subspace and leaves the others untouched.\n\n\n\n\n\nscale(c::CorrelationFunction)\n\nScale both the τ-equations and the underlying eqs0 so the spectrum's steady-state lookup resolves against the same scaled state names.\n\n\n\n\n\n","title":"QuantumCumulants.scale"},{"category":"function","location":"api/#QuantumCumulants.scale!","page":"API","text":"scale(eqs::AbstractMeanfieldEquations; h=Int[])\nscale!(eqs::AbstractMeanfieldEquations; h=Int[])\n\nReduce a permutation-symmetric indexed system to one representative per symmetry class, exploiting that every atom of an indexed family acts on the system identically. scale! mutates eqs in place; scale returns a new system.\n\nKeyword arguments\n\nh=Int[]: the Hilbert subspaces to scale, given by their acts_on indices. Empty scales every symmetric (indexed) subspace and leaves the others untouched.\n\n\n\n\n\n","title":"QuantumCumulants.scale!"},{"category":"type","location":"api/#QuantumCumulants.NoiseMeanfieldEquations","page":"API","text":"NoiseMeanfieldEquations\n\nConcrete equation set produced by meanfield when efficiencies is supplied, adding a measurement-backaction noise drift to the deterministic equations. The direction tag (Forward or Backward) drives compile-time dispatch of the noise assembly. Like MeanfieldEquations it is graph-backed: graph is the source of truth and the vector fields are a derived view.\n\nFields\n\ngraph: the source of truth.\nequations: the deterministic averaged differential equations (derived view).\nnoise_equations: the averaged noise (Brownian) drift per state.\noperator_equations, operator_noise_equations: the operator-level counterparts.\nstates: the averages on the left-hand sides.\noperators: the operators on the left-hand sides.\nhamiltonian: the system Hamiltonian.\njumps, jumps_dagger: the collapse operators and their adjoints.\nrates: the decay rates corresponding to jumps.\nefficiencies: the detector efficiencies per jump.\niv: the independent (time) variable.\norder: the cumulant-expansion order, or nothing.\ndirection: Forward or Backward evolution.\n\n\n\n\n\n","title":"QuantumCumulants.NoiseMeanfieldEquations"},{"category":"function","location":"api/#QuantumCumulants.translate_W_to_Y","page":"API","text":"translate_W_to_Y(eqs::NoiseMeanfieldEquations; mix_choice=maximum)\n\nRewrite an SDE whose noise drift is parametrised by the underlying Wiener process dW into one parametrised by the measurement record dY instead. The substitution dW = dY - sqrt(2η)·⟨J + J†⟩·dt adds a deterministic correction to the drift; the noise drift itself is unchanged.\n\nFor each equation the RHS is augmented by cumulant_expansion(-_dY_dS_extra_term(lhs_op, J, Jdagger, rates .* efficiencies)), cumulant-expanded to eqs.order when set. Returns a fresh NoiseMeanfieldEquations of the same direction. The augmented RHS is left unsimplified; apply simplify yourself for a canonical form.\n\n\n\n\n\n","title":"QuantumCumulants.translate_W_to_Y"},{"category":"type","location":"api/#QuantumCumulants.EvolutionDirection","page":"API","text":"EvolutionDirection\n\nSupertype of the singleton tags Forward and Backward used to dispatch the noise/retrodiction code path at compile time.\n\n\n\n\n\n","title":"QuantumCumulants.EvolutionDirection"},{"category":"type","location":"api/#QuantumCumulants.Forward","page":"API","text":"Forward Heisenberg evolution (positive sign on i[H, ·]).\n\n\n\n\n\n","title":"QuantumCumulants.Forward"},{"category":"type","location":"api/#QuantumCumulants.Backward","page":"API","text":"Backward Heisenberg evolution (negative sign), used for retrodiction.\n\n\n\n\n\n","title":"QuantumCumulants.Backward"},{"category":"function","location":"api/#QuantumCumulants.find_missing","page":"API","text":"find_missing(eqs::AbstractMeanfieldEquations; filter_func=nothing)\n\nReturn the averages that appear on a right-hand side of eqs but are not yet among its tracked states: the equations still needed to close the system. A moment and its conjugate count as one: if either is already a state, neither is reported. filter_func mirrors complete!.\n\nThe report is representation-invariant: conjugate pairs are always folded onto one key, so the answer is the same whether eqs was closed with get_adjoints true or false.\n\nSee also: complete, complete!.\n\n\n\n\n\n","title":"QuantumCumulants.find_missing"},{"category":"function","location":"api/#SecondQuantizedAlgebra.find_operators","page":"API","text":"find_operators(h::HilbertSpace, order::Int; names=nothing) -> Vector\n\nGenerate all unique operator products up to order factors for Hilbert space h.\n\nStarts from fundamental_operators(h) and their adjoints, forms all products with up to order factors, then filters out zero terms and adjoint-duplicates. Useful for constructing the operator basis needed for cumulant expansions or moment equations. Pass names to override auto-generated operator names.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> length(find_operators(h, 1))\n1\n\nSee also fundamental_operators, unique_ops.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.find_operators"},{"category":"function","location":"api/#ModelingToolkitBase.complete","page":"API","text":"complete(eqs::AbstractMeanfieldEquations; order=nothing, kw...)\n\nNon-mutating complete!: derive equations of motion for every average that appears on a right-hand side but is not yet a state, returning a new closed system.\n\nKeyword arguments\n\norder=nothing: if given, the copy is first cumulant-expanded to this order (deterministic systems only) before closing.\nkw...: forwarded to complete! (max_iter, filter_func, get_adjoints).\n\nSee also: complete!, find_missing, meanfield.\n\n\n\n\n\n","title":"ModelingToolkitBase.complete"},{"category":"function","location":"api/#QuantumCumulants.complete!","page":"API","text":"complete!(eqs::AbstractMeanfieldEquations; max_iter=100_000, filter_func=nothing,\n          get_adjoints=true)\n\nClose eqs in place by repeatedly deriving equations of motion for every average that appears on a right-hand side but is not yet a state, until the set is self-contained. Closure reads and rewrites eqs.graph, so user RHS modifications and filter substitutions already recorded there survive. Right-hand sides are left unsimplified.\n\nKeyword arguments\n\nmax_iter=100_000: runaway backstop; an error is raised if closure has not converged within this many iterations.\nfilter_func=nothing: a predicate filter_func(avg); rejected averages are dropped (substituted by 0), e.g. to discard phase-invariant terms.\nget_adjoints=true: when false, track one representative per conjugate pair, with the partner recovered by conj at code generation. The default integrates each conjugate as its own (redundant) state; pass false for the minimal state set.\n\nSee also: complete, find_missing, meanfield.\n\n\n\n\n\n","title":"QuantumCumulants.complete!"},{"category":"function","location":"api/#SecondQuantizedAlgebra.unique_ops","page":"API","text":"unique_ops(ops) -> Vector\n\nReturn unique operators from ops, treating op and op' (adjoint) as the same degree of freedom. Only the first occurrence of each operator/adjoint pair is kept.\n\nExamples\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> length(unique_ops([a, a', a]))\n1\n\nSee also unique_ops!, fundamental_operators.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.unique_ops"},{"category":"function","location":"api/#QuantumCumulants.simplify!","page":"API","text":"simplify!(eqs::AbstractMeanfieldEquations; kwargs...)\nsimplify(eqs::AbstractMeanfieldEquations; kwargs...)\n\nRun SymbolicUtils.simplify on every RHS in eqs (and on the noise drift RHSs of a NoiseMeanfieldEquations). simplify! mutates; simplify returns a fresh struct. The derivation pipeline leaves expressions raw so the cost of SymbolicUtils.simplify is paid only when explicitly requested.\n\n\n\n\n\n","title":"QuantumCumulants.simplify!"},{"category":"function","location":"api/#SecondQuantizedAlgebra.fundamental_operators","page":"API","text":"fundamental_operators(h::HilbertSpace; names=nothing) -> Vector{QSym}\n\nReturn the minimal generating set of operators for Hilbert space h.\n\nReturns one Destroy per FockSpace; n(n+1)/2 - 1 Transition operators per NLevelSpace (upper-triangular plus diagonals, excluding the ground-state projector); three Pauli or Spin operators per PauliSpace/SpinSpace; one Position and one Momentum per PhaseSpace; and the concatenation of the above for a ProductSpace. Pass names to override the auto-generated operator names.\n\nExamples\n\njulia> h = FockSpace(:cavity) ⊗ NLevelSpace(:atom, 2);\n\njulia> length(fundamental_operators(h))\n3\n\nSee also find_operators, unique_ops.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.fundamental_operators"},{"category":"function","location":"api/#SecondQuantizedAlgebra.to_numeric","page":"API","text":"to_numeric(op, basis [, d::AbstractDict{<:QSym}])\nto_numeric(op, state [, d::AbstractDict{<:QSym}])\n\nConvert a symbolic operator expression to a numeric QuantumOpticsBase operator. d substitutes individual QSyms with custom numeric operators. Throws ArgumentError if any prefactor cannot be reduced to a concrete ComplexF64.\n\nExamples\n\njulia> using QuantumOpticsBase\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> b = FockBasis(5); ψ = fockstate(b, 2);\n\njulia> real(QuantumOpticsBase.expect(to_numeric(a' * a, ψ), ψ)) ≈ 2\ntrue\n\nSee also numeric_average.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.to_numeric"},{"category":"function","location":"api/#SecondQuantizedAlgebra.numeric_average","page":"API","text":"numeric_average(op, state[, d::AbstractDict{<:QSym}]) -> ComplexF64\nnumeric_average(op, states::AbstractVector[, d::AbstractDict{<:QSym}]) -> Vector\n\nExpectation value langle psi  hatO  psi rangle of a symbolic operator expression. Averaged BasicSymbolic expressions (from average) are unwrapped automatically. d substitutes symbolic operators with custom numeric operators before evaluation.\n\nExamples\n\njulia> using QuantumOpticsBase\n\njulia> h = FockSpace(:f);\n\njulia> @qnumbers a::Destroy(h);\n\njulia> b = FockBasis(5); ψ = fockstate(b, 2);\n\njulia> real(numeric_average(a' * a, ψ)) ≈ 2\ntrue\n\nSee also to_numeric, average.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.numeric_average"},{"category":"function","location":"api/#QuantumCumulants.initial_values","page":"API","text":"initial_values(eqs::AbstractMeanfieldEquations; defaults=Dict())\n\nMap every state's u(t) variable to its initial value, defaulting to zero(ComplexF64) for averages absent from defaults.\n\n\n\n\n\ninitial_values(eqs, state)            # from a numeric quantum state\ninitial_values(eqs, u0::AbstractVector)  # from a value vector aligned with eqs.states\n\n\n\n\n\n","title":"QuantumCumulants.initial_values"},{"category":"function","location":"api/#QuantumCumulants.get_solution","page":"API","text":"get_solution(sol, avg_or_op, eqs)\n\nQuery an ODE/SDE solution for the trajectory of an average (or a QField, averaged internally). Resolves the user's operator to a tracked state through the single structural matcher match_moment, which folds Hermitian conjugation (the side bit recovers a stored conjugate) and the system's index/symmetry treatment, so a query posed in any equivalent symbolic form hits the same state.\n\n\n\n\n\n","title":"QuantumCumulants.get_solution"},{"category":"function","location":"api/#QuantumCumulants.parameter_map","page":"API","text":"parameter_map(sys::MTK.System, pairs)\n\nKeep only the entries of pairs whose key is a live unknown or parameter of the compiled system sys (MTK rejects superfluous entries).\n\n\n\n\n\nparameter_map(eqs::AbstractMeanfieldEquations, pairs) -> Dict\n\nTranslate a user-facing Pair/Dict of parameter assignments into an MTK-compatible map. An IndexedVariable callable key (e.g. g from g(i) = IndexedVariable(:g, i)) is matched by name to the Symbolics array parameter that evaluate synthesised from the per-atom references; a scalar value is broadcast to fill that array, an AbstractArray value passes through as the explicit per-atom values. Scalar (non-array) parameters pass through.\n\n\n\n\n\n","title":"QuantumCumulants.parameter_map"},{"category":"function","location":"api/#QuantumCumulants.modify_equations","page":"API","text":"modify_equations(eqs::AbstractMeanfieldEquations, f::Function)\n\nReturn a copy of eqs whose RHS for each equation has been rewritten by f. The function is called as f(lhs_op, rhs), receiving the operator form of the LHS (undo_average(eq.lhs)) and the symbolic RHS, and returning a new RHS.\n\nf(lhs, rhs) = rhs + cumulant_expansion(average(commutator(1im * Hadd, lhs)), 2)\neqs_mod = modify_equations(eqs, f)\n\nSee also modify_equations!.\n\n\n\n\n\n","title":"QuantumCumulants.modify_equations"},{"category":"function","location":"api/#QuantumCumulants.modify_equations!","page":"API","text":"modify_equations!(eqs::AbstractMeanfieldEquations, f::Function)\n\nIn-place version of modify_equations. Rewrites each drift in eqs.graph with f(lhs_op, rhs), where lhs_op is the node key (the operator form of the LHS).\n\n\n\n\n\n","title":"QuantumCumulants.modify_equations!"},{"category":"function","location":"api/#SecondQuantizedAlgebra.make_time_dependent","page":"API","text":"make_time_dependent(expr, iv) -> expr\n\nLift every iv-free average leaf in expr into a time-dependent Number variable name(iv) carrying the operator in AverageOperator metadata (and the VariableSource set by @variables, so it reads as a ModelingToolkit unknown). Non-average structure is rebuilt only where a child changed. The lifted node is a leaf: the walk does not descend into iv. The default name is uniqueness-only; downstream code may rename for display without changing identity.\n\n\n\n\n\n","title":"SecondQuantizedAlgebra.make_time_dependent"},{"category":"section","location":"examples/optomechanical-cooling/#Optomechanical-Cooling","page":"Optomechanical Cooling","text":"In this example, we show how to implement a cooling scheme based on radiation pressure coupling of light to a mechanical oscillator, such as a membrane. The oscillator is placed inside an optical cavity. The cavity is driven by a laser and the resulting radiation pressure of the cavity field effectively couples the photons in the cavity mode to the vibrational phonons of the mechanical oscillator mode. This model is based on the one studied in C. Genes, et. al., Phys. Rev. A 77, 033804 (2008), and the Hamiltonian reads\n\nH = -hbarDelta a^dagger a + hbaromega_m b^dagger b + hbar Ga^dagger a left(b + b^daggerright) + hbar E left(a + a^daggerright)\n\nwhere Delta = omega_ell - omega_c is the detuning between the driving laser (omega_ell) and the cavity (omega_c). The amplitude of the laser is denoted by E, the resonance frequency of the mechanical oscillator by omega_m, and the radiation pressure coupling is given by G. Additionally, photons leak out of the cavity at a rate kappa. We start by loading the needed packages and specifying the model.\n\nusing QuantumCumulants\nusing OrdinaryDiffEqLowOrderRK, ModelingToolkitBase\nusing Plots\n\nhc = FockSpace(:cavity) # Hilbertspace\nhm = FockSpace(:motion)\nh = hc ⊗ hm\n\n@qnumbers a::Destroy(h, 1) b::Destroy(h, 2) # Operators\n\n\n@variables Δ ωm E G κ # Parameters\n\n\nH = -Δ * a' * a + ωm * b' * b + G * a' * a * (b + b') + E * (a + a') # Hamiltonian\n\n\nJ = [a] # Jump operators & rates\nrates = [κ]\nnothing # hide\n\nWe are specifically interested in the average number of photons langle a^dagger a rangle and phonons langle b^dagger b rangle. Thus, we first derive the equations for these two averages. We restrict our description to a second order cumulant expansion.\n\nops = [a' * a, b' * b] # Derive equations\neqs = meanfield(ops, H, J; rates = rates, order = 2)\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = -1 i E langle a^daggerrangle  + 1 i E langle arangle  -10 kappa langle a^dagger  arangle  \nfracddt langle b^dagger  brangle  = -1 i G left( langle a^daggerrangle  langle a  b^daggerrangle  + langle b^daggerrangle  langle a^dagger  arangle  + langle arangle  langle a^dagger  b^daggerrangle  -2 langle a^daggerrangle  langle b^daggerrangle  langle arangle  right) + 1 i G left( langle a^daggerrangle  langle a  brangle  + langle arangle  langle a^dagger  brangle  + langle brangle  langle a^dagger  arangle  -2 langle a^daggerrangle  langle arangle  langle brangle  right)\nendalign\n\nTo get a closed set of equations we automatically complete the system.\n\neqs_completed = complete!(deepcopy(eqs)) # Complete equations\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = -1 i E langle a^daggerrangle  + 1 i E langle arangle  -10 kappa langle a^dagger  arangle  \nfracddt langle b^dagger  brangle  = -1 i G left( langle a^daggerrangle  langle a  b^daggerrangle  + langle b^daggerrangle  langle a^dagger  arangle  + langle arangle  langle a^dagger  b^daggerrangle  -2 langle a^daggerrangle  langle b^daggerrangle  langle arangle  right) + 1 i G left( langle a^daggerrangle  langle a  brangle  + langle arangle  langle a^dagger  brangle  + langle brangle  langle a^dagger  arangle  -2 langle a^daggerrangle  langle arangle  langle brangle  right) \nfracddt langle a^daggerrangle  = 1 i E + G left( 1 i langle a^dagger  b^daggerrangle  + 1 i langle a^dagger  brangle  right) -1 i Delta langle a^daggerrangle  -05 kappa langle a^daggerrangle  \nfracddt langle a  b^daggerrangle  = G left( -2 i langle b^daggerrangle  langle a  b^daggerrangle  -1 i langle b^daggerrangle  langle a  brangle  -1 i langle arangle  langle b^dagger  b^daggerrangle  -1 i langle arangle  langle b^dagger  brangle  + 2 i langle arangle  langle b^daggerrangle ^2 -1 i langle brangle  langle a  b^daggerrangle  + 2 i langle b^daggerrangle  langle arangle  langle brangle  right) -1 i E langle b^daggerrangle  + 1 i G left( langle a^daggerrangle  langle a  arangle  -2 langle a^daggerrangle  langle arangle ^2 + 2 langle arangle  langle a^dagger  arangle  right) + 1 i Delta langle a  b^daggerrangle  -05 kappa langle a  b^daggerrangle  + 1 i omegam langle a  b^daggerrangle  \nfracddt langle b^daggerrangle  = 1 i G langle a^dagger  arangle  + 1 i omegam langle b^daggerrangle  \nfracddt langle a^dagger  b^daggerrangle  = G left( 1 i langle a^daggerrangle  + 2 i langle a^daggerrangle  langle a^dagger  arangle  + 1 i langle a^daggerrangle  langle b^dagger  b^daggerrangle  + 1 i langle a^daggerrangle  langle b^dagger  brangle  -2 i langle a^daggerrangle  langle b^daggerrangle ^2 + 2 i langle b^daggerrangle  langle a^dagger  b^daggerrangle  + 1 i langle b^daggerrangle  langle a^dagger  brangle  + 1 i langle arangle  langle a^dagger  a^daggerrangle  -2 i langle arangle  langle a^daggerrangle ^2 + 1 i langle brangle  langle a^dagger  b^daggerrangle  -2 i langle a^daggerrangle  langle b^daggerrangle  langle brangle  right) + 1 i E langle b^daggerrangle  -1 i Delta langle a^dagger  b^daggerrangle  -05 kappa langle a^dagger  b^daggerrangle  + 1 i omegam langle a^dagger  b^daggerrangle  \nfracddt langle b^dagger  b^daggerrangle  = 2 i G left( langle a^daggerrangle  langle a  b^daggerrangle  + langle b^daggerrangle  langle a^dagger  arangle  + langle arangle  langle a^dagger  b^daggerrangle  -2 langle a^daggerrangle  langle b^daggerrangle  langle arangle  right) + 2 i omegam langle b^dagger  b^daggerrangle  \nfracddt langle a  arangle  = G left( -2 i langle b^daggerrangle  langle a  arangle  + 4 i langle b^daggerrangle  langle arangle ^2 -4 i langle arangle  langle a  b^daggerrangle  -4 i langle arangle  langle a  brangle  -2 i langle brangle  langle a  arangle  + 4 i langle brangle  langle arangle ^2 right) -2 i E langle arangle  + 2 i Delta langle a  arangle  -10 kappa langle a  arangle\nendalign\n\nTo calculate the dynamics we create a system of ordinary differential equations, which can be used by DifferentialEquations.jl.\n\nsys = System(eqs_completed; name = :sys)\nsys_c = mtkcompile(sys)\nnothing # hide\n\nFinally, we need to define the numerical parameters and the initial state of the system. We will consider the membrane at room temperature. Its vibrational mode is in a thermal state with an average number of phonons that can be estimated from k_B T = n_mathrmvibhbar omega_m. If the resonator has a resonance frequency of omega_m = 10mathrmMHz, then the number of phonons at room temperature (Tapprox 300K) is approximately n_mathrmvib approx 4times 10^6.\n\nu0 = initial_values(eqs_completed; defaults = Dict(average(b' * b) => 4.0e6 + 0im)) # Initial state (4e6 phonons)\n\np0 = Dict{Num, ComplexF64}(Δ => -10.0 + 0im, ωm => 1.0 + 0im, E => 200.0 + 0im, G => 0.0125 + 0im, κ => 20.0 + 0im) # System parameters\nprob = ODEProblem(sys_c, merge(u0, p0), (0.0, 60000.0))\nsol = solve(prob, RK4())\nnothing # hide\n\nt = real.(sol.t) # Plot results\nphonons = real.(get_solution(sol, b'b, eqs_completed).(sol.t))\nT = 7.5e-5 * phonons\nphotons = real.(get_solution(sol, a'a, eqs_completed).(sol.t))\n\np1 = plot(t, T, ylabel = \"T in K\", legend = false)\np2 = plot(t, photons, xlabel = \"t⋅ωm\", ylabel = \"⟨a⁺a⟩\", legend = false)\nplot(p1, p2, layout = (2, 1), size = (650, 400))","title":"Optomechanical Cooling"},{"category":"section","location":"examples/optomechanical-cooling/#Package-versions","page":"Optomechanical Cooling","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqLowOrderRK\", \"ModelingToolkitBase\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"theory/#theory","page":"Theoretical background","text":"In this section, we will describe the fundamental theoretical concepts used within QuantumCumulants.jl.","title":"Theoretical background"},{"category":"section","location":"theory/#The-Quantum-Langevin-equation","page":"Theoretical background","text":"In an open quantum system, the equation of motion of an operator mathcalO is given by the Quantum Langevin equation. Let a system be described by the Hamiltonian H, and subject to a number of decay channels with the rates gamma_n and corresponding damping operators c_n. The equation for mathcalO is then given by\n\ndotmathcalO = fracihbarHmathcalO + sum_n fracgamma_n2left(2c_n^dagger mathcalOc_n - c_n^dagger c_n mathcalO - mathcalOc_n^dagger c_nright) + textnoise\n\nWe did not specify the noise term. For the unconditional dynamics (the average over all measurement outcomes) the white-noise term does not contribute to averages, so we neglect it in the following. This is no longer the case when a decay channel is continuously monitored: the measurement backaction reintroduces a noise term, and QuantumCumulants can derive those stochastic equations too (see Noise & measurement backaction). The above equation is an operator equation, i.e. solving it directly has the same numerical complexity as solving a stochastic master equation. However, averaging over the above we obtain a c-number equation, which, in principle, is easy to solve. This is the basic idea in QuantumCumulants.jl: derive equations of motions of operators, and then convert them to easily solvable c-number differential equations. However, as we will see, there is another crucial step required, namely the cumulant expansion.","title":"The Quantum Langevin equation"},{"category":"section","location":"theory/#A-brief-example","page":"Theoretical background","text":"To illustrate this, let us consider a simple example, the Jaynes-Cummings model. This model describes a two-level atom that is coupled to an optical resonator. The Hamiltonian reads\n\nH_mathrmJC = hbarDelta a^dagger a + hbar gleft(a^dagger sigma^ge + asigma^egright)\n\nwhere Delta = omega_mathrmc - omega_mathrma is the detuning between the cavity resonance frequency omega_mathrmc and the atomic transition frequency omega_mathrma. The frequency g describes the strength of the dipole-coupling between the atom and the cavity. The operators a and a^dagger are the photonic annihilation and creation operators of the cavity mode. The atom has a ground state grangle and an excited state erangle, and its dynamics are described by the projection operators sigma^ij = iranglelangle j, where ij inge. For simplicity, we will here assume that the system is closed, i.e. it is not subject to decay, such that the operator dynamics are described by the Heisenberg equation (i.e. only the first part of the Quantum Langevin equation).\n\nNow, say we want to compute the field dynamics, i.e. we want to derive the equation for dota. Using the fundamental relations\n\naa^dagger = 1 quad sigma^ijsigma^kl = delta_jksigma^il\n\nwe derive\n\nbeginalign*\ndota = -iDelta a - ig sigma^ge\n\ndotsigma^ge = 2ig a sigma^ee - ig a\n\ndotsigma^ee = igleft(a^daggersigma^ge - asigma^egright)\nendalign*\n\nSince dota couples to sigma^ge, and dotsigma^ge to sigma^ee, we needed to derive a total of three equations to arrive at a complete set. In order to make them easy to handle, we average over the above system of equations to obtain c-number equations. We find\n\nbeginalign*\nlangledotarangle = -iDelta langle arangle - ig langlesigma^gerangle\n\nlangledotsigma^gerangle = 2ig langle asigma^eerangle - ig langle a rangle\n\nlangle dotsigma^eerangle = -2gtextImleftlangle a^daggersigma^gerangleright\nendalign*\n\nThe above system can, however, not be solved since we encounter terms such as langle asigma^eerangle, meaning the set of equations is incomplete, since in general langle asigma^eerangle neq langle aranglelanglesigma^eerangle. A naive approach would be to derive the equations for all missing average values. Unfortunately, these equations will couple to averages of ever longer operator products. A complete set of equations can therefore not be derived, since it would consist of infinitely many equations.","title":"A brief example"},{"category":"section","location":"theory/#The-cumulant-hierarchy","page":"Theoretical background","text":"The coupling we just hit is generic. Every equation of motion sends an operator product through the commutator with H, and for an interacting Hamiltonian (the dipole coupling g here) the commutator is a longer product, so an average of order n couples to averages of order n+1. Those couple to order n+2, and so on. The equations form an infinite tower, the cumulant hierarchy:\n\norder   averages at this level         what couples them\n─────   ────────────────────────────   ──────────────────────────────────\n  1     ⟨a⟩   ⟨σ^ge⟩   ⟨σ^ee⟩           the dipole coupling g\n        │                             raises the order by one\n        ▼\n  2     ⟨a σ^ee⟩   ⟨a† σ^ge⟩   …\n        │\n        ▼\n  3     ⟨a† a σ^ge⟩   …\n        ⋮   infinite: the coupling never stops climbing\n\nFor an open system the two parts of the Quantum Langevin equation play opposite roles in this tower. The interaction terms in H are what climb it (the vertical edges above), while the dissipator sum_n gamma_n(ldots) and the free, quadratic part of H couple an average only to others of the same or lower order. Dissipation damps the dynamics but does not grow the hierarchy. A purely quadratic Hamiltonian with linear damping therefore never climbs and closes exactly at order 2; the interesting case is the non-quadratic one, where the tower is genuinely infinite and has to be cut. That cut is the cumulant expansion.","title":"The cumulant hierarchy"},{"category":"section","location":"theory/#Cumulant-expansion","page":"Theoretical background","text":"To obtain a closed set of c-number equations, we truncate the in principle infinite set of equations at a certain order. By order, we essentially mean the length of an operator product, e.g. langle a rangle is of order 1, langle a^dagger a rangle and langle a^dagger sigma^gerangle are of the order 2. The order of a system determines its size and the accuracy of the underlying approximation. It is therefore an essential concept in QuantumCumulants.jl.\n\nThe way in which we truncate a system of equations is called the generalized cumulant expansion (see also R. Kubo, Generalized Cumulant Expansion Method). The joint cumulant, which we denote by langlecdotrangle_c of a product of operators X_1 X_2  X_n of order n is given by\n\nlangle X_1 X_2  X_n rangle_c = sum_p in P(mathcalI) left(p - 1right) (-1)^p-1 prod_B in p langle prod_iin B X_irangle\n\nIn the above, mathcalI=12n, P(mathcalI) is the set of all partitions of mathcalI, p denotes the length of the partition p, and B runs over the blocks of each partition. For example, in the case of n=3 we find\n\nbeginalign*\nlangle X_1X_2X_3 rangle_c = langle X_1X_2X_3rangle  -langle X_1X_2ranglelangle X_3rangle - langle X_1X_3ranglelangle X_2rangle\n\n- langle X_1ranglelangle X_2X_3rangle + 2langle X_1ranglelangle X_2ranglelangle X_3rangle\nendalign*\n\nNote that the joint cumulant of order n is proportional to averages of order leq n. Furthermore, the average of order n occurs precisely once on the right-hand-side.\n\nThe joint cumulant can be thought of as a general measure for the correlation between operators. According to Theorem I from R. Kubo, Generalized Cumulant Expansion Method, the joint cumulant is zero iff any of the operators is statistically independent of the others. The key assumption we are making is to essentially invert this statement: instead of computing the joint cumulant of a given order to see if it is zero, we assume that it is. Since the average value of the same order occurs only once in the definition of the joint cumulant, we may invert the relation to express the average in terms of lower-order terms; i.e., if we assume langle X_1 X_2  X_nrangle_c = 0, we can write\n\nlangle X_1X_2X_n rangle = sum_p in P(mathcalI)backslash mathcalI left(p - 1right) (-1)^p prod_B in p langle prod_iin B X_irangle\n\nwhere now P(mathcalI)backslashmathcalI is the set of all partitions of mathcalI that does not contain mathcalI itself. In the example of n=3, we have\n\nlangle X_1X_2X_3rangle   = langle X_1X_2ranglelangle X_3rangle + langle X_1X_3ranglelangle X_2rangle + langle X_1ranglelangle X_2X_3rangle - 2langle X_1ranglelangle X_2ranglelangle X_3rangle\n\nIn other words, by neglecting the cumulant of order n we can express all averages of order n in terms of averages of order n-1 and below. By applying this expansion recursively, we can reduce the order of any term to one as low as we choose.\n\nReturning to the example of the Jaynes-Cummings Hamiltonian, we could use the cumulant expansion to express all the terms of order 2 in first order only. This is also called the mean-field approach which neglects all quantum correlations of a system. For the Jaynes-Cummings model, we would then have\n\nbeginalign*\nlangledotarangle = -iDelta langle arangle - ig langlesigma^gerangle\n\nlangledotsigma^gerangle = 2ig langle aranglelanglesigma^eerangle - ig langle a rangle\n\nlangle dotsigma^eerangle = -2gtextImleftlangle a^daggerranglelanglesigma^gerangleright\nendalign*\n\nNow this system of equations forms a closed set, and can readily be implemented and solved numerically. Of course, solving the Jaynes-Cummings model in mean field is not very interesting. We could make it more interesting by doing a second-order cumulant expansion. However, this would involve deriving the equations for all the second-order averages, which is quite tedious. So instead, let's just be lazy and use QuantumCumulants.jl to do it,\n\nusing Latexify # hide\nset_default(double_linebreak=true) # hide\nusing QuantumCumulants\n\n# Symbolic parameters\n@variables Δ::Real g::Real\n\n# Hilbert space\nhf = FockSpace(:cavity)\nha = NLevelSpace(:atom,(:g,:e))\nh = hf⊗ha\n\n# Operators\n@qnumbers a::Destroy(h)\nσ(i, j) = Transition(h, :σ, i, j)\n\n# Hamiltonian\nH = Δ*a'*a + g*(a'*σ(:g,:e) + a*σ(:e,:g))\n\n# List of first and second-order operators\nops = find_operators(h,2)\n\n# Derive equations\nmeanfield(ops,H;order=2) # expand to second order\nnothing # hide\n\nbeginalign*\nfracddt langle arangle = -10 i langle arangle Delta -10 i langle sigma^gerangle g\n\nfracddt langle sigma^gerangle = -10 i langle arangle g + 20 i g langle a sigma^eerangle\n\nfracddt langle sigma^eerangle = 10 i g langle a^dagger sigma^gerangle -10 i g langle a sigma^egrangle\n\nfracddt langle a sigma^gerangle = 20 i left( langle sigma^eerangle langle a arangle + 2 langle arangle langle a sigma^eerangle -2 langle sigma^eerangle langle arangle ^2 right) g -10 i g langle a arangle -10 i Delta langle a sigma^gerangle\n\nfracddt langle a sigma^eerangle = -10 i left( langle sigma^egrangle langle a arangle + 2 langle arangle langle a sigma^egrangle -2 langle sigma^egrangle langle arangle ^2 right) g + 10 i left( langle a^daggerrangle langle a sigma^gerangle + langle arangle langle a^dagger sigma^gerangle + langle sigma^gerangle langle a^dagger arangle -2 langle a^daggerrangle langle arangle langle sigma^gerangle right) g -10 i Delta langle a sigma^eerangle\n\nfracddt langle a^dagger arangle = -10 i g langle a^dagger sigma^gerangle + 10 i g langle a sigma^egrangle\n\nfracddt langle a sigma^egrangle = -10 i langle sigma^eerangle g -20 i left( langle a^daggerrangle langle a sigma^eerangle + langle arangle langle a^dagger sigma^eerangle + langle sigma^eerangle langle a^dagger arangle -2 langle a^daggerrangle langle arangle langle sigma^eerangle right) g + 10 i g langle a^dagger arangle -10 i Delta langle a sigma^egrangle\n\nfracddt langle a arangle = -20 i g langle a sigma^gerangle -20 i Delta langle a arangle\nendalign*\n\nNote that QuantumCumulants.jl automatizes the derivation of equations and the cumulant expansion. The final step of numerical implementation is handled by converting to the ModelingToolkitBase.jl framework via System.","title":"Cumulant expansion"},{"category":"section","location":"theory/#References","page":"Theoretical background","text":"R. Kubo. \"Generalized cumulant expansion method.\" Journal of the Physical Society of Japan 17.7 (1962): 1100-1120. URL: https://www.jstage.jst.go.jp/article/jpsj1946/17/7/1771100/_article/-char/ja/","title":"References"},{"category":"section","location":"changelog/#Changelog","page":"Changelog","text":"All notable changes to QuantumCumulants.jl will be documented in this file.","title":"Changelog"},{"category":"section","location":"changelog/#[0.5.0]","page":"Changelog","text":"Breaking release: a clean rewrite of QuantumCumulants.jl atop SecondQuantizedAlgebra.jl v0.6 (which now owns the operator algebra) and ModelingToolkitBase.jl (in place of the full ModelingToolkit). Migration from the v0.4 series is hand-driven; there are no deprecation shims. The substantive changes fall into three groups: direct renames, constructs replaced by more general machinery, and behavioural changes in shared API names. All 14 examples from the v0.4 series are ported and pass an automated smoke run. This entry is the migration reference for users with code written against QuantumCumulants.jl v0.4. Algebra-surface migration (Hilbert spaces, operators, indices, sums) lives in the SecondQuantizedAlgebra.jl changelog.","title":"[0.5.0]"},{"category":"section","location":"changelog/#Added","page":"Changelog","text":"Graph-native core (MomentGraph). The cumulant hierarchy is now carried by a single MomentGraph: nodes are canonicalised moments, edges are the drift couplings between them, and per-subspace treatments record whether a subspace is left free, unrolled, or scaled. meanfield seeds and closes it, and complete/complete!, scale/scale!, and evaluate transform it; System reads from it to emit code. Every *Equations struct now stores graph as the source of truth, with equations a derived view regenerated from the graph rather than a hand-maintained parallel list. Correlation functions and spectra are derived on the same graph.\n\nIntrospection helpers. New exported accessors over the graph: states, moments, moment_variable_map (moment to BasicSymbolic variable), closure_report (which moments were truncated and how), noise_channels (per-jump noise drift on a NoiseMeanfieldEquations), and parameter_map.\n\nDirection types. EvolutionDirection with Forward and Backward are exported, replacing the separate meanfield_backward entry point (meanfield(...; direction=Backward())).\n\nIn-place variants. scale! and simplify! join the existing complete! / modify_equations!.\n\nCorrelation functions of time-dependent Hamiltonians. CorrelationFunction now supports systems whose Hamiltonian, jumps, or rates depend on the time variable (e.g. a drive f(t)); previously these failed with UndefVarError: t not defined. See issues #171 and #93. Per the quantum regression theorem the τ-evolution is governed by the generator at t₀+τ, so the parent time variable is substituted by iv0 + τ. Pass the parent evolution's stop time t₀ via the new iv0 keyword: @variables t0::Real; CorrelationFunction(op1, op2, eqs; iv0 = t0), and supply its value in correlation_p0. Time-independent systems are unaffected (the keyword is ignored when no time dependence is present).","title":"Added"},{"category":"section","location":"changelog/#Renamed","page":"Changelog","text":"One-to-one renames. Replace the left column with the right column anywhere it appears in your code.\n\nv0.4 v0.5\nindexed_meanfield(...) meanfield(...)\nindexed_complete(...) / indexed_complete!(...) complete(...) / complete!(...)\nfind_missing_sums(...) find_missing(...)\nscaleME(...) scale(...) / scale!(...)\nevalME(...) evaluate(...)\nIndexedCorrelationFunction(...) CorrelationFunction(...)\nheisenberg(...) meanfield(...)\nmeanfield_backward(...) meanfield(...; direction=Backward())\nIndexedMeanfieldEquations / EvaledMeanfieldEquations / ScaledMeanfieldEquations MeanfieldEquations\nMeanfieldNoiseEquations / IndexedMeanfieldNoiseEquations / BackwardMeanfieldNoiseEquations NoiseMeanfieldEquations{…,Direction}\nto_system(eqs) System(eqs; name)\nsol[op] / get_scale_solution(...) get_solution(sol, op, eqs)(t)\n@cnumbers a b c @variables a b c (reexported from Symbolics)\n@rnumbers a b c @variables a::Real b::Real c::Real\nParameter(:x) @variables x","title":"Renamed"},{"category":"section","location":"changelog/#Removed","page":"Changelog","text":"A handful of constructs that lived at the type level in v0.4 have been replaced by mechanisms that scale better and avoid bespoke types.\n\nClusterSpace. Indexed families of identical subsystems are now expressed through the SQA Index and Σ machinery. Where v0.4 wrote\n\nhc = ClusterSpace(NLevelSpace(:atom, 2), N, k)\nσ(i, j) = Transition(hc, :σ, i, j)\nH = Δ * sum(σ(2, 2))\n\nv0.5 writes\n\nha = NLevelSpace(:atom, 2)\ni  = Index(ha, :i, N, ha)\nσ(α, β, k) = IndexedOperator(Transition(ha, :σ, α, β, 2), k)\nH = Δ * Σ(σ(2, 2, i), i)\n\nN stays symbolic through equation derivation. The cumulant truncation order, which used to be the k parameter on ClusterSpace, is now passed to the derivation: meanfield(…; order=k).\n\nThe indexed-sum type zoo. IndexedAverageSum, IndexedAverageDoubleSum, SingleSum, DoubleSum, AvgSums, NumberedOperator, and SpecialIndexedTerm are gone. They were SQA-internal in v0.4; SQA v0.5 represents indexed sums uniformly as a QAdd with .indices metadata, so the type zoo collapsed. Code that pattern-matched on these should iterate the resulting QAdd and inspect term.ne directly.\n\nIndexed-helper exports. value_map, split_sums, scale_term, eval_term, insert_index, and order_by_index are removed. The equivalent transformations are expressed through SQA's change_index and Σ directly.\n\nParameter machinery. Parameter, CNumber, RNumber, RealParameter, the Average type, and the @cnumbers/@rnumbers/cnumbers/cnumber/rnumbers/rnumber macros are removed. Use @variables x::Real (or ::Complex) for symbolic parameters.\n\nThe simplify keyword. Removed from every QC entry point that exposed it (meanfield, complete/complete!, cumulant, cumulant_expansion, evaluate, translate_W_to_Y, CorrelationFunction, Spectrum). QC no longer runs SymbolicUtils.simplify on its derived right-hand sides; canonical-form post-processing is the caller's responsibility (Symbolics.simplify(eq.rhs; expand=true)). The old default simplify=true was a 99% time sink at higher cumulant orders (e.g. an order-6 CorrelationFunction dropped from ~100s to ~10ms) for no downstream numerical benefit.\n\nsubst_reds. Redundant-conjugate substitution is folded into the System(eqs) codegen path automatically.","title":"Removed"},{"category":"section","location":"changelog/#Changed","page":"Changelog","text":"A few API names survive but their behaviour has shifted. The differences are intentional and pay off in correctness or performance, so be aware of them when porting tests.\n\nmeanfield is the single derivation entry. meanfield(ops, H, J; …) replaces the scalar/indexed/noise/backward split. Noise is opt-in via efficiencies=… (yielding a NoiseMeanfieldEquations); retrodiction via direction=Backward().\n\nscale and evaluate take a subspace selector. scale(eqs; h=Int[]) and evaluate(eqs; limits, h=Int[]) accept a h::Vector{Int} of acts_on indices selecting which Hilbert subspaces are collapsed / unrolled. The empty default targets every subspace (the previous behaviour). Hybrid systems can unroll some subspaces with evaluate and collapse others with scale, in any order.\n\nscale drops additive constants from the LHS. When SQA's commutator pipeline produces a c + ⟨…⟩ shape (e.g. ⟨a a'⟩ collapsing to 1 + ⟨a'a⟩), the constant-offset equation deduplicates against its normal-ordered sibling.\n\nEquation LHS is the raw Average. In the user-facing struct, the LHS is the bare Average BasicSymbolic. The Differential(t)(u(t)) form is built only inside ModelingToolkitBase.System(eqs; name), which QC extends to dispatch on MeanfieldEquations, NoiseMeanfieldEquations, and CorrelationFunction.\n\nSystem conjugate substitution survives compilation. System(eqs) builds conj(avg_op(t)) via SymbolicUtils.term(conj, var; type=Number) rather than Base.conj(var), so the symbolic conj node survives mtkcompile and build_function. Without this, every ⟨X⟩ - ⟨X†⟩ driving term silently zeroed on the compiled RHS because Symbolics simplifies conj(::SymReal) to identity. Related ModelingToolkit gap tracked at https://github.com/SciML/ModelingToolkit.jl/issues/4548.\n\ncumulant_expansion redistributes sum-scope metadata. The SumIndices/SumNonEqual metadata is pushed onto each factorised product term so scale can recover the per-index range prefactor after Wick factorisation. Bound indices that no factored leaf references keep SQA's \"spurious bound idx, factor 1\" convention.\n\nDependencies and CI. Updated to ModelingToolkitBase 1.36, SymbolicUtils 4, Symbolics 7, SecondQuantizedAlgebra 0.6.3. Quality gates (Aqua + ExplicitImports + CheckConcreteStructs + JET) are now part of CI.","title":"Changed"},{"category":"section","location":"changelog/#Migration","page":"Changelog","text":"Side-by-side, the single-atom-laser workflow from the tutorial.\n\n# v0.4\nusing QuantumCumulants, ModelingToolkit\n@cnumbers Δ g γ κ ν\nhf = FockSpace(:cavity); ha = NLevelSpace(:atom, (:g, :e)); h = hf ⊗ ha\n@qnumbers a::Destroy(h)\nσ(i, j) = Transition(h, :σ, i, j)\nH = Δ*a'*a + g*(a'*σ(:g, :e) + a*σ(:e, :g))\nJ = [a, σ(:g, :e), σ(:e, :g)]\neqs  = meanfield([a'*a, σ(:e, :e), a'*σ(:g, :e)], H, J; rates=[κ, γ, ν], order=2)\neqs  = complete(eqs)\nsys  = to_system(eqs)\nprob = ODEProblem(sys, u0, (0.0, 10.0), p0)\nsol  = solve(prob, Tsit5())\nn    = sol[a'*a]\n\n# v0.5\nusing QuantumCumulants, ModelingToolkitBase\n@variables Δ::Real g::Real γ::Real κ::Real ν::Real\nhf = FockSpace(:cavity); ha = NLevelSpace(:atom, (:g, :e)); h = hf ⊗ ha\n@qnumbers a::Destroy(h)\nσ(i, j) = Transition(h, :σ, i, j)\nH = Δ*a'*a + g*(a'*σ(:g, :e) + a*σ(:e, :g))\nJ = [a, σ(:g, :e), σ(:e, :g)]\neqs  = meanfield([a'*a, σ(:e, :e), a'*σ(:g, :e)], H, J; rates=[κ, γ, ν], order=2)\neqs  = complete(eqs)\nsys  = mtkcompile(System(eqs; name=:laser))\nprob = ODEProblem(sys, merge(initial_values(eqs, u0), Dict(p0)), (0.0, 10.0))\nsol  = solve(prob, Tsit5())\nn    = real.(get_solution(sol, a'*a, eqs).(ts))\n\nThe differences are local: @cnumbers becomes @variables, using ModelingToolkit becomes using ModelingToolkitBase, to_system(eqs) becomes mtkcompile(System(eqs; name)), and sol[op] becomes get_solution(sol, op, eqs)(t). For numeric initial values from symbolic level names, pre-translate the :g/:e labels to integer levels before calling initial_values(eqs, ψ) (the v0.4 level_map keyword was dropped). Everything else (meanfield, complete, the Hamiltonian) keeps the same names and meaning.","title":"Migration"},{"category":"section","location":"changelog/#Unchanged","page":"Changelog","text":"These QC names keep their meaning across the migration; code that only uses them ports without changes:\n\nmeanfield, complete/complete!, find_missing, find_operators, cumulant, cumulant_expansion, get_order, average, undo_average, CorrelationFunction, Spectrum, correlation_u0, correlation_p0, scale/scale!, evaluate, initial_values.\n\nThe entire SecondQuantizedAlgebra algebra surface (Hilbert spaces, operators, indices, sums, to_numeric, numeric_average) is reexported unchanged; see the SecondQuantizedAlgebra.jl migration notes for that layer.\n\n<!– Links generated by Changelog.jl –>","title":"Unchanged"},{"category":"section","location":"devdocs/#Internal-architecture","page":"Internal architecture","text":"This page is for contributors and for users who want to understand what happens inside QuantumCumulants.jl between meanfield and a numerical solution. It explains the internal architecture and, more importantly, why it is built the way it is. For the user-facing walkthrough of the same pipeline see The mean-field pipeline; for the cumulant-expansion mathematics see the theory page.","title":"Internal architecture"},{"category":"section","location":"devdocs/#Scope:-the-moment-layer-on-top-of-the-algebra","page":"Internal architecture","text":"QuantumCumulants is a thin moment layer. The operator algebra it manipulates (QSym/QAdd atoms and sums, normal ordering, indices, symbolic sums, the diagonal split, projector completeness, sum metadata) lives in SecondQuantizedAlgebra.jl (SQA), which QuantumCumulants re-exports. Read SQA's developer documentation for that layer; this page does not re-derive it.\n\nThe contract between the two is sharp and worth stating once:\n\nSQA owns every algebraic transformation. SQA.change_index, SQA.Σ, *, +, expand_completeness, assume_distinct_index, and the SumIndices/SumNonEqual metadata are the only primitives that carry canonicalisation (projector squashing, commuting reorder, the off-diagonal/diagonal split, NE propagation). QuantumCumulants never hand-rolls a QAdd and never sorts operators by hand. When QC needs a new index it derives the name from the user's declared vocabulary, never an invented prefix.\n\nEverything below is the moment logic that sits on that algebra: turning operators into a closed set of c-number differential equations.","title":"Scope: the moment layer on top of the algebra"},{"category":"section","location":"devdocs/#From-the-hierarchy-to-a-graph","page":"Internal architecture","text":"The physics this layer encodes is the cumulant hierarchy: the infinite tower of coupled moment equations an open system produces, cut by the cumulant expansion (the theory page derives both). The key observation is that the hierarchy is already a graph. Each moment is a vertex; the statement \"langle Xrangle appears on the right-hand side of the equation for langle Yrangle\" is a directed edge X to Y. The untruncated hierarchy is an infinite such graph, and the two operations that make it usable are exactly the two ways of making that graph finite:\n\nTruncation (the cumulant expansion) bounds where edges may point. It rewrites every right-hand side moment above the order cap as a product of lower-order moments, so no edge ever lands on a moment of order  m. The tower can no longer climb.\nClosure bounds the vertex set. Starting from the user's requested moments, follow every edge to the moment it lands on, add any vertex not yet present, and repeat until no new vertex appears. A hierarchy is closed precisely when its graph is closed under this edge relation: every right-hand side references only vertices already in the graph.\n\nThis is why the moment graph is a graph and not, say, a flat list of equations. The hierarchy is intrinsically a \"moments couple to moments\" relation, which is a directed graph, and every workflow step is then a standard operation on it: closure is a reachability sweep, a filter_func deletes vertices, scale and evaluate quotient the vertices under an equivalence, and building the numerical system is a walk that resolves each edge to a state variable. Holding the hierarchy as one graph is what lets these be a single mechanism rather than five separate passes over a list.\n\nTwo properties of that graph drive the rest of the design:\n\nIt is finite and usually cyclic. Truncation guarantees finiteness; the cycles are generic (below, langle arangle and langle a^dagger arangle feed each other), which is why closure must test membership before adding a vertex rather than recurse blindly.\nA vertex is a physical moment, not a written expression. The closure test \"do I already have this moment?\" is only correct if two symbolically different but physically identical averages are the same vertex (langlesigma^ge_irangle and langlesigma^ge_jrangle under a renamed index, langle Arangle and langle A^daggerrangle^* under conjugation). Pinning that identity is the job of the canonical label (Moment identity and canonicalisation below); it is what keeps the closed graph non-redundant, so the hierarchy closes on the smallest set of distinct expectation values.\n\nFor the driven Kerr cavity at order 2 (H = Delta a^dagger a + U a^dagger a^dagger a a + eta(a + a^dagger) with decay kappa, the worked example below) the three core moments all couple to one another, and langle a^daggerrangle folds onto langle arangle by conjugation:\n\n              ⟨a⟩\n             ╱   ╲\n            ╱     ╲\n        ⟨a†a⟩ ─── ⟨aa⟩\n\n  Every edge is bidirectional: each moment it joins appears on the\n  other's d⟨·⟩/dt right-hand side. ⟨a†a†⟩ is tracked as well under\n  get_adjoints=true, a conjugate shadow of ⟨aa⟩.\n\nThis finite graph, not the infinite tower, is the object the package holds in memory and that every workflow step transforms. The rest of this page is its representation (the MomentGraph) and how each step rewrites it.","title":"From the hierarchy to a graph"},{"category":"section","location":"devdocs/#One-central-object:-the-cumulant-hierarchy","page":"Internal architecture","text":"The whole package is organised around a single observation.\n\nEvery step the user calls (deriving the equations of motion, closing the hierarchy, scaling, evaluating, building the numerical system) acts on one shared object: the cumulant hierarchy, held in memory as a graph of coupled moment equations (the moment graph). Each moment carries a canonical label that is identical for two expectation values exactly when they are physically the same, and a per-subspace treatment (each subspace is independently Free, Scaled, or Concrete) sets how strong that identification is. Because every step is just a relabelling of the same object, closing, scaling, evaluation, and hybrid systems all follow from one mechanism.\n\nEach public equation set (MeanfieldEquations / NoiseMeanfieldEquations) carries its MomentGraph as the authoritative field; the equation, state, and operator vectors are a derived view regenerated from it. A step reads eqs.graph, transforms it, and either returns a fresh wrapper (assemble_equations) or, for an in-place ! form, rewrites the graph and regenerates the view (resync!). There is no round-trip back through the vectors: the graph is never rebuilt from the equation list, so user RHS modifications, filter substitutions, treatment state, and the mix_choice truncation scheme all persist across steps. meanfield builds the first graph directly from the list of operators via seed.\n\n  MeanfieldEquations\n  ├─ graph  :: MomentGraph              ◀── source of truth\n  └─ equations / states / operators         derived view\n            │  read eqs.graph                  ▲  resync! (in place) / assemble_equations (fresh)\n            ▼                                   │\n   ┌─────────────────────────────────────────────────────────────┐\n   │                      MomentGraph                            │\n   │   seed · closure · quotient · specialize · map_drifts       │   each step transforms it\n   └─────────────────────────────────────────────────────────────┘\n\nThe whole workflow is then just a sequence of these steps:\n\n ops, H, J ──▶ meanfield ──▶ complete ──▶ scale / evaluate ──▶ System ──▶ solve\n               (build)       (close)      (reduce, optional)   (numerics)\n\nThe source files are loaded in dependency order (src/QuantumCumulants.jl):\n\nLayer Files Role\nEarly types & identity equations.jl, tree.jl, canonical.jl abstract equation supertype, treatment enum, direction tags, leaf traversal, moment keys\nAlgebra to moments operator_drift.jl, moments.jl Heisenberg RHS, per-moment derivation\nThe hierarchy & its wrappers graph.jl, equations_concrete.jl, cumulant.jl the cumulant hierarchy as a graph of moment equations; the graph-backed MeanfieldEquations / NoiseMeanfieldEquations and their derived view; cumulant expansion\nWorkflow steps meanfield.jl, completion.jl, scaling.jl, evaluate.jl, mtk.jl, correlation.jl, spectrum.jl the operations the user calls\nDisplay printing.jl plain-text and LaTeX","title":"One central object: the cumulant hierarchy"},{"category":"section","location":"devdocs/#The-data-model:-the-moment-graph","page":"Internal architecture","text":"The moment graph (graph.jl) is the in-memory form of a (possibly incomplete) cumulant hierarchy.\n\nMomentGraph\n├─ nodes :: OrderedDict{QAdd ⟶ NodeData}    one entry per tracked moment\n│    key   = canonical-representative QAdd   (the moment's identity)\n│    value = NodeData\n│             ├─ drift     :: Num    averaged + truncated RHS  (leaves couple to other moments)\n│             ├─ op_drift  :: QAdd   operator-level RHS (inspection / latex / re-truncation)\n│             ├─ noise     :: Num?   measurement-backaction drift (optional)\n│             ├─ op_noise  :: Num?   always nothing (operator-level noise is deferred)\n│             ├─ order     :: Int    cached get_order\n│             └─ aon       :: Vector{Int}   cached acts_on\n├─ sys  :: SystemSpec    frozen derivation inputs (see below)\n├─ ctx  :: CanonCtx      index vocabulary + which subspaces are symmetric / selected\n└─ treatments :: Dict{Int ⟶ SubspaceTreatment}   per-subspace: Free | Scaled | Concrete\n\nSystemSpec is the immutable bundle of everything derive needs: hamiltonian, jumps, jumps_dagger, rates, efficiencies (nothing means deterministic), iv, order, mix_choice, and direction. It is frozen so a moment can be derived on demand at any point during a workflow step without threading a dozen arguments through every call. Because mix_choice (the mixed-order truncation scheme) lives here, it is system state set once at meanfield and carried through every step; it is not a keyword on complete/complete! or cumulant_expansion(eqs, order).\n\nTwo design choices are worth the rationale:\n\nWhy an OrderedDict keyed by the moment. The closure membership test (\"do I already have this moment?\") established above must be a fast lookup, so the vertex set is an OrderedDict whose key is the moment's physical identity (the canonical-representative QAdd), not its written form. Lookup, insertion, and the \"couples to\" edge relation (read off the drift's leaves) are then all O(1) hash operations on that key. Ordered keeps the derivation order stable so the derived equation view is reproducible.\nWhy two representations. The MomentGraph is the persistent source of truth each step transforms; the equation/state/operator vectors on MeanfieldEquations/NoiseMeanfieldEquations (equations_concrete.jl) are a derived view, regenerated from the graph by resync! (in place) or assemble_equations (fresh). Keeping the view distinct means the user-facing object stays a simple, stable, indexable list of equations, while the graph carries the extra bookkeeping (sys, ctx, treatments, the cached per-moment data) that only the workflow steps need.\n\nEvery Int key in treatments, in CanonCtx.vocab, and in CanonCtx.symmetric is an SQA space_index: the position of a Hilbert-space factor (subspace) in the system's ProductSpace, as returned by acts_on.\n\nctx.vocab stores only the user's declared reps; the k-th slot a higher-body moment needs is derived on demand by nth_index(reps, k) = reps[1](k) (SQA naming policy), shared by symmetric_min, _concrete_index, and _relabel_spaces so scale and evaluate name the same atoms. The extended vocabulary is therefore a pure function of the declared reps, so the ctx is frozen at meanfield time and never mutated (the memo's reliance on immutability follows). Minted slots surface as concrete indices on the graph's moments, which is where scale's _build_sym_to_space reads back the realised inventory.","title":"The data model: the moment graph"},{"category":"section","location":"devdocs/#Moment-identity-and-canonicalisation","page":"Internal architecture","text":"This is the heart of the moment layer, and it is what makes the hierarchy close on a non-redundant set. The job of canonical.jl is to assign each average a key that is identical for two averages exactly when they are the same expectation value. Two averages can coincide three ways:\n\nRelabelling of free atom indices. langlesigma^ge_irangle and langlesigma^ge_jrangle are the same moment under a rename of the bound index.\nThe permutation symmetry S_n of identical atoms. In a scaled ensemble every atom is interchangeable, so langlesigma_isigma_jrangle collapses onto one representative.\nHermitian conjugation, langle A^daggerrangle = langle Arangle^*.\n\nCanonCtx holds the vocabulary used for the relabelling (always derived from the user's declared indices, never freshly minted prefixes, per SQA's naming policy) and which subspaces are symmetric or selected. Each subspace carries a SubspaceTreatment, and the treatment chooses which of the three identifications a key applies.\n\n subspace treatment      key function     identifies up to\n ────────────────────────────────────────────────────────────────────────────\n Free      (default)     canon_key        free-index relabelling\n Scaled    (scale)       scaled_key       + permutation symmetry of identical atoms\n Concrete  (evaluate)    concrete_key     nothing (fixed sites σ_1, σ_2, … stay distinct)\n\n conjugation-aware variants (used to merge conjugate pairs and build the numerics):\n   canonical_rep / concrete_rep  →  (key, is_conjugate),  folding ⟨A⟩ with ⟨A†⟩\n\n Each subspace is independently Free / Scaled / Concrete.\n A hybrid system can be Scaled on the atoms and Concrete on a filter cavity at once.\n\ncanon_key and scaled_key are the same engine, _treatment_key, called with different treatment maps (all subspaces Free, and the selected subspaces Scaled). It puts the average into a comparable normal form (drop the summation scope, fix the order of commuting factors, drop the non-equal constraints), relabels each non-Concrete subspace to the vocabulary representatives, and reduces any Scaled subspace under the permutation symmetry of the identical atoms via symmetric_min. A Concrete subspace is simply left un-relabelled, so _treatment_key already handles mixed systems. concrete_key is a separate, simpler label for a fully materialised system: it skips the relabelling entirely so the fixed sites sigma_1 sigma_2 ldots stay distinct. canonical_rep and its materialised counterpart concrete_rep share one helper, _conjugation_rep: it labels both langle Arangle and langle A^daggerrangle and keeps whichever is smaller, with a flag for which side it came from, so one stored moment stands in for both halves of a conjugate pair.\n\n\"Smaller\" is decided by SQA's total, identity-faithful structural order: order_key per operator and qadd_order_key per expression, which tie exactly when two labels are isequal. QC defers the ordering to SQA rather than comparing rendered strings, so representative selection is reproducible and never depends on show.\n\nBoth labels are memoised per CanonCtx. _treatment_key and canonical_rep are pure in (op, ctx, treatments), so with ctx fixed each result is cached in ctx.cache (a CanonCache) under the operator and a treatment fingerprint (the sorted (space_index, treatment) pairs). One ctx is shared across a transform lineage (closure, scale, evaluate, the System and spectrum lookups), so a label computed once during closure is reused everywhere downstream. The fingerprint keeps the all-Free, Scaled, and Concrete configurations of a shared ctx in distinct slots; the memo is monotonic and pure, so it never needs invalidation.\n\nBecause the label is chosen per subspace, partial reductions compose: a system can be scaled on one subspace and evaluated on another, in any order, and the labelling stays consistent. _materialised_key is the rule that selects the conjugation-aware label once any subspace has been made Concrete, and the plain treatment label otherwise.\n\nThe MomentMap{V} in canonical.jl is the single lookup that matches each moment to its stored value, shared by the state-variable registry, System, get_solution, the spectrum routine, and the correlation steady-state lookup. It stores each value under canonical_rep and, on a query, reports whether the query sits on the same conjugation side as the stored representative, so a request for langle A^daggerrangle against a stored langle Arangle returns its conjugate.","title":"Moment identity and canonicalisation"},{"category":"section","location":"devdocs/#Deriving-one-moment's-equation-of-motion","page":"Internal architecture","text":"derive(op, sys, ctx) (moments.jl) produces the equation of motion for a single moment.\n\nderive(op, sys, ctx)\n   │\n   ├─ _operator_rhs(direction, op, iH, J, J†, rates)            (op_drift :: QAdd)\n   │      Forward :  i[H, op] + Σ_k (r_k/2)(J†[op,J] + [J†,op]J)\n   │      Backward: -i[H, op] + adjoint Lindblad + trace term      (retrodiction)\n   │\n   ├─ expand_completeness            σᵍᵍ fold (no-op without a ground projector)\n   ├─ _assume_distinct_atom_indices  ⟨X_i X_j⟩ is the i≠j cumulant; let the diagonal split fire\n   │\n   ├─ average_and_truncate           cross to moments: average WITH the sum scope, then\n   │      │                          cumulant_expansion to sys.order\n   │      └─▶ drift :: Num           (the leaves are the moments this one couples to)\n   │\n   └─ _reduce_ground_in_drift        moment-level σᵍᵍ completeness fold\n\nThe operator RHS (operator_drift.jl) is the Heisenberg (quantum Langevin) equation. Forward uses +iHcdot plus the Lindblad recycling sum_k r_k(J_k^daggercdotJ_k - tfrac12J_k^dagger J_kcdot); a matrix rate selects collective decay, a DoubleIndexedVariable rate selects the collective cross-jump dissipator (split into an explicit diagonal self-decay plus the off-diagonal term, because completeness cannot recover the diagonal), and otherwise the per-jump term is summed over the jump's free indices. Backward flips the commutator sign, swaps Jleftrightarrow J^dagger in the recycling, and adds the trace-preserving term for retrodiction.\n\naverage_and_truncate is where the operator RHS crosses into moments, and the ordering matters: it averages with the sum scope attached first, so SQA's diagonal split collapses same-index operator pairs, and only then applies the cumulant_expansion. Truncating the raw product first would split the operators into separate blocks and the collapse would never fire. Index-dependent coefficients ride inside the sum so the diagonal split substitutes them; scalar coefficients stay outside.","title":"Deriving one moment's equation of motion"},{"category":"section","location":"devdocs/#Cumulant-expansion-and-truncation-order","page":"Internal architecture","text":"cumulant_expansion (cumulant.jl) rewrites any average whose order exceeds the cap as the signed sum over partitions of its factors, neglecting the joint cumulant above the cap (see the theory page). get_order measures order: a number is 0, a single operator 1, a product the number of factors. order is normalised to a Vector{Int}, one cap per Hilbert subspace; a term acting on several subspaces combines its per-subspace caps through mix_choice (default maximum).\n\nThe subtle part is the sum-scope metadata round-trip. A summed average such as Sigma_ilangle a^dagger asigma_irangle carries its scope as SumIndices/ SumNonEqual metadata on that one averaged symbol. When the cumulant expansion factorises it into a product of smaller averages, that symbol is gone, so the scope would be lost. _stamp_sum_to_first_leaves re-attaches each bound index onto the first factor that uses it, per additive term, rebuilding the leaf through the canonical average(SQA.Σ(op, …)). It must use the canonical form and not a bare setmetadata, because a setmetadata leaf is isequal to the un-summed leaf and the two would never cancel in a later subtraction.","title":"Cumulant expansion and truncation order"},{"category":"section","location":"devdocs/#Closing-the-hierarchy","page":"Internal architecture","text":"seed (graph.jl) derives the equations for the user's requested operators, the first entries in the graph. closure then repeatedly takes one moment, looks at the moments on the right-hand side of its equation (the leaves of the symbolic drift), and derives an equation for any not yet present, until no new moments appear; it is pure, returning a new graph rather than mutating its input.\n\nA moment and its conjugate are one physical unknown, and the closure exploits that partially. A moment whose conjugate partner is already present is covered automatically (no new equation). The get_adjoints flag controls only the genuinely-new case. With get_adjoints=true (the default for meanfield/complete) a new moment's conjugate is also added as its own moment; with get_adjoints=false (used by CorrelationFunction) only one representative is kept and the partner is recovered by complex conjugation when the numerical system is built. The consequences are spelled out in the worked example below and in invariant 1.\n\nThe max_iter guard is a runaway backstop, not a truncation limiter: hitting it raises an error rather than silently returning a non-closed system, which the numerical build would otherwise mask.\n\ncomplete! is closure exposed publicly: it reads eqs.graph, closes it, writes the result back, and regenerates the view (resync!). Because it extends the existing graph rather than re-deriving, any user RHS modifications already on the graph survive. A filter_func drops unwanted moments (for example phase-invariant or ancilla terms) by zeroing them on every right-hand side (via map_drifts). find_missing reports the still-needed moments and is representation-invariant: it folds conjugate pairs onto one key, so the answer is the same whether the system was closed with get_adjoints true or false.","title":"Closing the hierarchy"},{"category":"section","location":"devdocs/#Reducing-the-system:-scale-and-evaluate","page":"Internal architecture","text":"scale and evaluate transform eqs.graph by relabelling the moments under a new treatment. They rewrite the stored drifts in place rather than re-deriving, so filter substitutions recorded by a prior complete(...; filter_func) are preserved.\n\nscale (scaling.jl) calls quotient, marking the selected symmetric subspaces Scaled and merging permutation-equivalent moments. Each surviving drift leaf langle Xrangle becomes textprefactorcdotlangle X_textreprangle, where the prefactor is prod_b (textrange_b - textne_b) over the bound indices on the scaled subspaces, and every per-atom IndexedVariable coupling is flattened to its scalar (all atoms share the coupling under the symmetry). The h::Vector{Int} keyword restricts the quotient to specific subspaces; empty means all symmetric subspaces.\nevaluate (evaluate.jl) calls specialize, unrolling each targeted symbolic range to its concrete integer size (from limits) and pinning indices to Concrete sites 1ldots M with distinct-site (ineq j) semantics. A final arrayize_graph rewrites any leftover per-site coupling (a callable Sym like g(i_2_3)) into getindex on a freshly minted Symbolics array parameter, which is what MTK can scalarise and bind; parameter_map later matches user values to that array by name.\n\nBecause both are keyed by treatment rather than a hardcoded representative, a system can be partially scaled and partially evaluated, in any order.","title":"Reducing the system: scale and evaluate"},{"category":"section","location":"devdocs/#Building-the-numerical-system","page":"Internal architecture","text":"mtk.jl bridges the moment layer to ModelingToolkitBase. _state_registry builds a time-dependent variable u(t) for each state (SQA's make_time_dependent, which turns the iv-free Number average leaf into a Number-symtype var(iv) carrying the operator in AverageOperator metadata), names it by avg_name (the average notation itself, e.g. langle a^dagger arangle becomes the symbol ⟨a' * a⟩), and builds a MomentMap keyed by the conjugate representative. Moment identity is structural (the MomentMap representative), not the name. MTK keys unknowns by name, so _state_vars appends a numeric suffix on the rare display-name collision to keep distinct states distinct. Building the System then looks up every moment on a right-hand side in that map.\n\nThe average symbol is not the MTK unknown; the unknown is a dedicated time-dependent variable var(t), and the average rides along only as metadata. This indirection reconciles three things the average itself cannot satisfy at once. First, an average leaf is independent of the integration variable, whereas an MTK unknown is a function var(iv); make_time_dependent is what introduces that dependence. Second, identity must be structural so that conjugate pairs and index- or symmetry-equivalent forms fold onto one unknown, but MTK keys unknowns by name, and a name cannot encode those identifications (the MomentMap, not the name, decides identity, and the display name is allowed to collide). Third, the symtype must stay Number: averages are complex, and Symbolics.Num accepts only a <:Real symtype while a Real-typed unknown would let conj fold langle Arangle and langle A^daggerrangle together. The unknown is therefore an unwrapped Number-symtype BasicSymbolic, carrying the operator in metadata for resolution and a separate human-readable name for display.\n\n right-hand side  (symbolic expression; each ⟨...⟩ average is a leaf)\n        │  mapleaves(resolve, rhs)\n        ▼\n for each leaf ⟨op⟩:\n        ├─ canonical_rep(op) ─▶ (rep, side)\n        ▼\n   MomentMap.by_rep[rep] = (var, rep_side)\n        ├─ side == rep_side ?  ─ yes ─▶  var          e.g. ⟨a⟩(t)\n        └─                       no  ─▶  conj(var)     e.g. conj(⟨a⟩(t))\n\nThe conjugate is emitted as SymbolicUtils.term(conj, var; type=Number). The state variables are Number-symtype because averages are complex: on a Real symtype conj folds to the identity and would erase langle Arangle vs langle A^daggerrangle, whereas on Number it stays distinct (the soundness reason for the Number symtype). The explicit term keeps the conjugate at Number symtype so it composes with the rest of the RHS and survives mtkcompile. The left-hand side becomes Differential(iv)(u(t)) only here; the equation struct itself always stores the raw Average.\n\nA NoiseMeanfieldEquations becomes a stochastic differential equation: the single Brownian term is the aggregated noise drift, with sign +1 for Forward and -1 for Backward, chosen by the evolution direction.\n\nThe remaining bridges: initial_values computes a u0 from a numeric Ket or density operator (via SQA's numeric_average), parameter_map expands indexed/array parameters to the compiled system's shapes, and get_solution reverses the registry to evaluate any operator-average trajectory, including products that never appeared as a stored state. Both System and get_solution resolve through the single MomentMap (match_moment), which already folds Hermitian conjugation and the system's index/symmetry treatment, so a query posed in any equivalent symbolic form resolves to the same state.","title":"Building the numerical system"},{"category":"section","location":"devdocs/#Correlation-and-spectrum","page":"Internal architecture","text":"CorrelationFunction (correlation.jl) is another step that builds its own hierarchy. For the two-time correlation langle A(t+tau)B(t)rangle it re-embeds B on a fresh ancilla subspace (aon = max(acts_on) + 1), derives the equations for A B_textancilla in the delay tau, and closes only on the averages that touch the ancilla (a closure filter), with get_adjoints=false. The non-ancilla averages are steady-state coefficients. The correlation inherits the parent system's treatments so its leaves resolve in the same keying. Spectrum is the Fourier transform of the correlation.","title":"Correlation and spectrum"},{"category":"section","location":"devdocs/#A-worked-example:-the-driven-Kerr-cavity","page":"Internal architecture","text":"The smallest model that exercises the whole pipeline, including a real cumulant truncation, is a single Kerr (anharmonic) cavity. A linear cavity is Gaussian and never truncates at order 2, so the Kerr term Ua^dagger a^dagger a a is what makes the machinery visible.\n\nusing QuantumCumulants\nh = FockSpace(:cavity)\n@qnumbers a::Destroy(h)\n@variables Δ U η κ\nH = Δ*a'*a + U*a'*a'*a*a + η*(a + a')\nnothing # hide\n\nDerivation. Without truncation, meanfield produces the exact Heisenberg equation for langle arangle. The Kerr term injects an order-3 moment langle a^dagger a arangle:\n\nmeanfield([a], H, [a]; rates=[κ])\n\nTruncation. Passing order=2 applies the cumulant expansion: the order-3 moment is factorised into products of moments of order le 2. This is the truncation firing.\n\neqs = meanfield([a], H, [a]; rates=[κ], order=2)\n\nClosure. The right-hand side now references langle a^dagger arangle, langle a arangle and langle a^daggerrangle. The conjugate langle a^daggerrangle is covered (its partner langle arangle is already a state), but because the default is get_adjoints=true, the new moment langle a arangle has its conjugate langle a^dagger a^daggerrangle tracked as its own state too, giving four states:\n\neqs_c = complete(eqs)\neqs_c.states\n\nAsking for the minimal set with get_adjoints=false keeps three, recovering langle a^dagger a^daggerrangle purely by conjugation:\n\ncomplete(eqs; get_adjoints=false).states\n\nNumerics. Building the MTK system names one variable per state and looks up every moment through the MomentMap. The langle a^daggerrangle terms resolve to the conj of the langle arangle unknown:\n\nusing ModelingToolkitBase\nsys = System(eqs_c; name=:kerr)\nunknowns(sys)\n\nequations(sys)[1]   # ⟨a⟩: note the conj of the ⟨a⟩ unknown standing in for ⟨a†⟩\n\nNote the honest subtlety from the default get_adjoints=true: the langle a^dagger a^daggerrangle unknown is integrated in its own right, but its right-hand side resolves to the conj of the langle a arangle unknown and nothing else reads it, so it is a redundant shadow. mtkcompile does not detect the conjugate alias and keeps it, so the default integrates one redundant ODE. Use get_adjoints=false when the minimal state set matters.","title":"A worked example: the driven Kerr cavity"},{"category":"section","location":"devdocs/#What-changes-for-an-indexed-system","page":"Internal architecture","text":"For a permutation-symmetric many-body system built from Index and Σ (see Indexed and scaled systems), the same graph carries indexed moments. A single-atom moment langlesigma_irangle keys to one Free representative regardless of the bound index. scale then marks the atom subspace Scaled, folds the permutation-equivalent moments onto one representative, and multiplies each by the (textrange - textne) prefactor that counts how many atoms the fold stands for. evaluate instead pins the atoms to Concrete sites sigma_1ldotssigma_M and arrayizes the per-site couplings. Treatments are per-subspace and independent, so a filter-cavity subspace can stay Concrete while the atoms are Scaled in the same system.","title":"What changes for an indexed system"},{"category":"section","location":"devdocs/#Design-invariants-and-pitfalls","page":"Internal architecture","text":"These are the moment-layer rules a contributor must not break, each with its failure mode.\n\nA conjugate pair is one physical unknown, but the default does not minimise on it. During closure a moment whose conjugate is already present is covered. get_adjoints=true (default) tracks the conjugate of a genuinely-new moment as a second state; the numerical build keys states by canonical_rep/concrete_rep, so every occurrence of the pair on a right-hand side resolves to the first representative (or its conj), leaving the second variable a redundant shadow that mtkcompile does not eliminate. Use get_adjoints=false for the minimal set.\nThe MTK unknown is a dedicated time-dependent variable var(t), never the average symbol itself. The average is iv-free, Number-symtype, and identified structurally; an MTK unknown is a named, time-dependent variable. Registering the average directly would either lose the integration variable, force a Real symtype that folds langle Arangle and langle A^daggerrangle under conj, or make MTK's name-based keying the source of truth for identity instead of the MomentMap. The bridge keeps the unknown a Number-symtype BasicSymbolic carrying the operator in metadata, and resolves identity through the map, not the name.\nclosure keys with canon_key, never scaled_key. Symmetric reduction during closure would collapse the unscaled moment count. The reduction steps relabel with the treatment-matched _materialised_key; calling bare canon_key on an already-Concrete subspace would relabel and merge sites that must stay distinct.\nSum-scope metadata must round-trip. Rebuild summed leaves through the canonical average(SQA.Σ(...)), not setmetadata, which is isequal to the un-summed leaf so the terms never cancel.\nUse Symbolics.IM, not Julia's im, in symbolic right-hand sides. A complex(0, …) literal does not unify with the factored symbolic form; _im_form/_qc_maketerm convert it.\nMint indices from the user's vocabulary via nth_index (reps[1](k)), never invent fresh prefixes (SQA naming policy). scale and evaluate agree by construction because both mint through the same nth_index.\nRoute index operations through SQA.change_index/Σ/*. Hand-rolling a QAdd and sorting it yourself reintroduces the bugs the algebra exists to prevent (sigma_ee^2 nesigma_ee, sigma_2sigma_1 nesigma_1sigma_2).\nAverage with the sum scope before truncating, so the diagonal split fires before factorisation.\nThe iszero coefficient drop in average_and_truncate uses Base.iszero deliberately; SQA's structural check misses uncombined zero forms like lambda2 - (12)lambda.\nmax_iter in closure errors, it does not truncate. It is a runaway backstop only.\nThe ground projector sigma^gg stays an atom except where the sigma^gg = 1 - Sigmasigma^kk fold is the explicit goal (expand_completeness/_reduce_ground_in_drift in derive).\nNodeData.op_noise is always nothing (deferred); the operator-level noise column is rebuilt in assemble_equations when a noise struct is assembled.","title":"Design invariants and pitfalls"},{"category":"section","location":"examples/superradiant_laser_indexed/#Superradiant-Laser","page":"Superradiant Laser","text":"Using symmetry properties of a system can reduce the number of needed equations dramatically. A common approximation for laser systems to handle sufficiently big atom numbers is to assume that several atoms in the system behave completely identically. This means all the identical atoms have the same averages.\n\nIn this example we describe a so-called superradiant laser, where we assume all atoms to be identical. This model has been described in D. Meiser et al., Phys. Rev. Lett. 102, 163601 (2009): The Hamiltonian of this system is\n\nbeginequation\nH = - hbar Delta a^dagger a +  hbar sumlimits_j=1^N  g_j (a^dagger sigma^12_j + a sigma^21_j) \nendequation\n\nwhere Delta = omega_a - omega_c is the detuning between the cavity (omega_c) and the atomic (omega_a) resonance frequency, the atom cavity coupling of the j-th atom is denoted by g_j. Additionally there are dissipative processes in the system: Atoms are incoherently pumped with the rate R, they decay individually with the rate Gamma and are affected by individual atomic dephasing with the rate nu. Furthermore, photons leak out of the system with the rate kappa.\n\nWe start by loading the packages.\n\nusing QuantumCumulants\nusing OrdinaryDiffEqTsit5, ModelingToolkitBase\nusing Plots\n\nDue to the implementation of symbolic indices and sums we only need to define the Hilbert space for one atom, even though we will simulate a system for several thousand. Creating an operator with an textttIndex is done with the constructor textttIndexedOperator.\n\nhc = FockSpace(:cavity)\nha = NLevelSpace(:atom, 2)\nh = hc ⊗ ha\n\n@qnumbers a::Destroy(h) # operators\nσ(α, β, i) = IndexedOperator(Transition(h, :σ, α, β), i)\nnothing # hide\n\nNow we define the indices and the parameters of the system. An textttIndex needs the system Hilbert space, a symbol, an upper bound and the specific Hilbert space of the indexed operator. textttIndexedVariable creates indexed variables. Actually we wouldn't need indexed variable in this example, this is just for demonstration purposes.\n\n@variables N Δ κ Γ R ν\ng(i) = IndexedVariable(:g, i)\n\ni = Index(h, :i, N, ha)\nj = Index(h, :j, N, ha)\n\nWe define the Hamiltonian using symbolic sums and define the individual dissipative processes. For an indexed jump operator the (symbolic) sum is build in the Liouvillian.\n\nH = -Δ * a'a + Σ(g(i) * (a' * σ(1, 2, i) + a * σ(2, 1, i)), i) # Hamiltonian\n\nJ = [a, σ(1, 2, i), σ(2, 1, i), σ(2, 2, i)] # Jump operators with corresponding rates\nrates = [κ, Γ, R, ν]\nnothing # hide\n\nFirst we want to derive the equation for langle a^dagger a rangle and langle sigma_j^22 rangle. Note that you can only use indices on the LHS which haven't been used for the Hamiltonian and the jumps.\n\nops = [a' * a, σ(2, 2, j)] # Derive equations\neqs = meanfield(ops, H, J; rates = rates, order = 2)\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = 1 i undersetioversetNsum g_i  langle a  sigma_i^21rangle  -1 i undersetioversetNsum g_i  langle a^dagger  sigma_i^12rangle  -10 kappa langle a^dagger  arangle  \nfracddt langle sigma_j^22rangle  = R -10 R langle sigma_j^22rangle  + 1 i g_j langle a^dagger  sigma_j^12rangle  -1 i g_j langle a  sigma_j^21rangle  -10 Gamma langle sigma_j^22rangle\nendalign\n\nTo get a closed set of equations we automatically complete the system. Since this system is phase invariant we know that all averages with a phase are zero, therefore we exclude these terms with a filter function. To be able to dispatch on all kind of sums containing averages we defined the Union textttAvgSums.\n\nimport QuantumCumulants.SecondQuantizedAlgebra as SQA\n\nφ(x) = 0 # custom filter function\nφ(::Destroy) = -1\nφ(::Create) = 1\nφ(x::Transition) = x.i - x.j\nfunction φ(q::SQA.QAdd) # walk operator-product expression\n    for (term, _) in q.arguments\n        p = 0\n        for op in term.ops\n            p += φ(op)\n        end\n        return p\n    end\n    return 0\nend\nfunction φ(avg)\n    SQA.is_average(avg) || return 0\n    return φ(SQA.undo_average(avg))\nend\nphase_invariant(x) = iszero(φ(x))\n\neqs_c = complete(eqs; filter_func = phase_invariant) # Complete equations\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = 1 i undersetioversetNsum g_i  langle a  sigma_i^21rangle  -1 i undersetioversetNsum g_i  langle a^dagger  sigma_i^12rangle  -10 kappa langle a^dagger  arangle  \nfracddt langle sigma_j^22rangle  = R -10 R langle sigma_j^22rangle  + 1 i g_j langle a^dagger  sigma_j^12rangle  -1 i g_j langle a  sigma_j^21rangle  -10 Gamma langle sigma_j^22rangle  \nfracddt langle a^dagger  sigma_j^12rangle  = 1 i undersetinejoversetNsum g_i  langle sigma_i^21  sigma_j^12rangle  + 1 i g_j langle sigma_j^22rangle  -1 i g_j langle a^dagger  arangle  -05 R langle a^dagger  sigma_j^12rangle  -05 Gamma langle a^dagger  sigma_j^12rangle  -05 kappa langle a^dagger  sigma_j^12rangle  -05 nu langle a^dagger  sigma_j^12rangle  -1 i Delta langle a^dagger  sigma_j^12rangle  + 2 i g_j langle sigma_j^22rangle  langle a^dagger  arangle  \nfracddt langle sigma_j^12  sigma_k^21rangle  = left( -10 R -10 Gamma right) langle sigma_j^12  sigma_k^21rangle  -1 i g_j langle a  sigma_k^21rangle  + 1 i g_k langle a^dagger  sigma_j^12rangle  -10 nu langle sigma_j^12  sigma_k^21rangle  + 2 i g_j langle sigma_j^22rangle  langle a  sigma_k^21rangle  -2 i g_k langle sigma_k^22rangle  langle a^dagger  sigma_j^12rangle\nendalign\n\nAs mentioned before, we assume that all atoms behave identically. This means that e.g. the excited state population is equal for all atoms, hence we only need to calculate it for the first langle sigma^22_1 rangle = langle sigma^22_j rangle. Furthermore, it is clear that a sum over N identical objects can be replaced by N times the object. The function textttscale() uses these rules to simplify the equations.\n\neqs_sc = scale(eqs_c)\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = -10 kappa langle a^dagger  arangle  -1 i N g_1 langle a^dagger  sigma_1^12rangle  + 1 i N g_1 langle a  sigma_1^21rangle  \nfracddt langle sigma_1^22rangle  = R -10 R langle sigma_1^22rangle  -10 Gamma langle sigma_1^22rangle  + 1 i g_1 langle a^dagger  sigma_1^12rangle  -1 i g_1 langle a  sigma_1^21rangle  \nfracddt langle a^dagger  sigma_1^12rangle  = -05 R langle a^dagger  sigma_1^12rangle  -05 Gamma langle a^dagger  sigma_1^12rangle  -05 kappa langle a^dagger  sigma_1^12rangle  -05 nu langle a^dagger  sigma_1^12rangle  + 1 i g_1 langle sigma_1^22rangle  -1 i g_1 langle a^dagger  arangle  -1 i Delta langle a^dagger  sigma_1^12rangle  + 1 i g_1 left( -1 + N right) langle sigma_1^12  sigma_2^21rangle  + 2 i g_1 langle sigma_1^22rangle  langle a^dagger  arangle  \nfracddt langle sigma_1^12  sigma_2^21rangle  = left( -10 R -10 Gamma right) langle sigma_1^12  sigma_2^21rangle  + 1 i g_1 langle a^dagger  sigma_1^12rangle  -1 i g_1 langle a  sigma_1^21rangle  -10 nu langle sigma_1^12  sigma_2^21rangle  -2 i g_1 langle sigma_1^22rangle  langle a^dagger  sigma_1^12rangle  + 2 i g_1 langle sigma_1^22rangle  langle a  sigma_1^21rangle\nendalign\n\nTo calculate the dynamics of the system we create a system of ordinary differential equations, which can be used by DifferentialEquations.jl.\n\nsys = System(eqs_sc; name = :sys)\nsys_c = mtkcompile(sys)\nnothing # hide\n\nFinally we need to define the numerical parameters and the initial value of the system. We will consider 2 cdot 10^5 Strontium atoms which are repumped with a rate of R = 1textHz on the clock transition (Gamma = 1 textmHz). The atom-cavity coupling rate is g = 1textHz, the cavity has a linewidth of kappa = 5textkHz and is detuned from the atomic resonance by Delta = 25textHz.\n\nu0 = initial_values(eqs_sc) # Initial state\n\nN_ = 2.0e5 # System parameters\nΓ_ = 1.0 #Γ=1mHz\nΔ_ = 2500Γ_ #Δ=2.5Hz\ng_ = 1000Γ_ #g=1Hz\nκ_ = 5.0e6 * Γ_ #κ=5kHz\nR_ = 1000Γ_ #R=1Hz\nν_ = 1000Γ_ #ν=1Hz\n\npmap = parameter_map(\n    eqs_sc, Dict(\n        N => N_, Δ => Δ_, g(i) => g_, κ => κ_, Γ => Γ_, R => R_, ν => ν_,\n    )\n)\nps = collect(keys(pmap))\np0 = collect(values(pmap))\n\ndict = merge(u0, pmap)\nprob = ODEProblem(sys_c, dict, (0.0, 1.0 / 50Γ_))\nnothing # hide\n\nsol = solve(prob, Tsit5(), maxiters = 1.0e7) # Solve the numeric problem\n\nt = sol.t # Plot time evolution\nn = real.(get_solution(sol, a'a, eqs_sc).(t))\ns22 = real.(get_solution(sol, σ(2, 2, j(1)), eqs_sc).(t))\np1 = plot(t, n, xlabel = \"tΓ\", ylabel = \"⟨a⁺a⟩\", legend = false) # Plot\np2 = plot(t, s22, xlabel = \"tΓ\", ylabel = \"⟨σ22⟩\", legend = false)\nplot(p1, p2, layout = (1, 2), size = (700, 300))","title":"Superradiant Laser"},{"category":"section","location":"examples/superradiant_laser_indexed/#Frequency-Spectrum","page":"Superradiant Laser","text":"We calculate the spectrum here with the Laplace transform of the two-time correlation function. This is implemented with the function textttSpectrum.\n\ncorr = CorrelationFunction(a', a, eqs_c; steady_state = true, filter_func = phase_invariant)\ncorr_sc = scale(corr)\nS = Spectrum(corr_sc, ps)\nnothing # hide\n\nThe set of equations for the correlation function is given by\n\ncorr_sc.eqs\nnothing # hide\n\nbeginalign\nfracddtau langle a^dagger  a_0rangle  = -05 kappa langle a^dagger  a_0rangle  -1 i Delta langle a^dagger  a_0rangle  + 1 i N g_1 langle sigma_1^21  a_0rangle  \nfracddtau langle sigma_1^21  a_0rangle  = -05 R langle sigma_1^21  a_0rangle  + 1 i g_1 langle a^dagger  a_0rangle  -05 Gamma langle sigma_1^21  a_0rangle  -05 nu langle sigma_1^21  a_0rangle  -2 i g_1 langle sigma_1^22rangle  langle a^dagger  a_0rangle\nendalign\n\nThe spectrum is then calculated with\n\nω = [-10:0.01:10;]Γ_\nspec = S(ω, sol.u[end], p0)\nspec_n = spec ./ maximum(spec)\nδ = abs(ω[(findmax(spec)[2])])\n\nplot(ω, spec_n, xlabel = \"ω/Γ\", legend = false, size = (500, 300))\n\nBeside the narrow linewidth we can also see another key feature of the superradiant laser here, namely the very weak cavity pulling. At a detunig of Delta = 2500Gamma there is only a shift of the laser light from the atomic resonance frequency of delta = 1Gamma.","title":"Frequency Spectrum"},{"category":"section","location":"examples/superradiant_laser_indexed/#Package-versions","page":"Superradiant Laser","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status([\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\"], mode = PKGMODE_MANIFEST)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/filter-cavity_indexed/#Laser-with-Filter-Cavities","page":"Laser with Filter Cavities","text":"An intuitive and straightforward approach to calculate the spectrum of a laser is to filter the emitted light. We can do this by coupling filter cavities with different detunings to the main cavity and observe the photon number in these 'filters', see for example K. Debnath et al., Phys Rev A 98, 063837 (2018).\n\nThe main goal of this example is to combine two indexed Hilbert spaces, where one will be scaled and the other evaluated. The model is basically the same as for the superradiant laser example, but with the additional terms due to the filter cavities. The Hamiltonian of this system is\n\nbeginequation\nH = - Delta a^dagger a + g sumlimits_j=1^N (a^dagger sigma^12_j + a sigma^21_j) - sumlimits_i=1^M delta_i b_i^dagger b_i +  g_f sumlimits_i=1^M (a^dagger b_i + a b_i^dagger)\nendequation\n\nwhere delta_i is the detuning of the i-th filter cavity and g_f the coupling to the normal cavity. Furthermore, their decay rate is kappa_f.\n\nWe start by loading the packages.\n\nusing QuantumCumulants\nusing OrdinaryDiffEqTsit5, ModelingToolkitBase\nusing Plots\n\nWe create the parameters of the system including the textttIndexedVariable delta_i. For the atoms and filter cavities we only need one Hilbert space each. We define the indices for each Hilbert space and use them to create textttIndexedOperators.\n\n@variables κ g gf κf R Γ Δ ν N M # Parameters\nδ(i) = IndexedVariable(:δ, i)\n\nhc = FockSpace(:cavity) # Hilbert spaces\nhf = FockSpace(:filter)\nha = NLevelSpace(:atom, 2)\nh = hc ⊗ hf ⊗ ha\n\ni = Index(h, :i, M, hf) # Indices\nj = Index(h, :j, N, ha)\n\n@qnumbers a::Destroy(h, 1)\nb(k) = IndexedOperator(Destroy(h, :b, 2), k)\n\ni is bound by the Hamiltonian sums and the dissipator. The canonical free-index slot on the filter Hilbert space mints i_2 (lex-first declared index, suffix 2) to keep state names disjoint from H's bound scope. Per-atom state lookups thus use i_2(k) = i_2_k, not i(k).\n\nb(k::Integer) = IndexedOperator(Destroy(h, :b, 2), i(2)(k))\nσ(α, β, k) = IndexedOperator(Transition(h, :σ, α, β, 3), k)\nσ(α, β, k::Integer) = IndexedOperator(Transition(h, :σ, α, β, 3), j(k))\nnothing # hide\n\nWe define the Hamiltonian using symbolic sums and define the individual dissipative processes. For an indexed jump operator the (symbolic) sum is build in the Liouvillian, in this case corresponding to individual decay processes.\n\nH =\n    Δ * Σ(σ(2, 2, j), j) +\n    Σ(δ(i) * b(i)'b(i), i) +\n    gf * (Σ(a' * b(i) + a * b(i)', i)) +\n    g * (Σ(a' * σ(1, 2, j) + a * σ(2, 1, j), j)) # Hamiltonian\n\nJ = [a, b(i), σ(1, 2, j), σ(2, 1, j), σ(2, 2, j)] # Jumps & rates\nrates = [κ, κf, Γ, R, ν]\nnothing # hide\n\nWe derive the equation for langle a^dagger a rangle and complete the system automatically in second order.\n\neqs = meanfield(a'a, H, J; rates = rates, order = 2)\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = 1 i undersetioversetMsum gf  langle a  b_i^daggerrangle  -1 i undersetioversetMsum gf  langle a^dagger  b_irangle  + 1 i undersetjoversetNsum g  langle a  sigma_j^21rangle  -1 i undersetjoversetNsum g  langle a^dagger  sigma_j^12rangle  -10 kappa langle a^dagger  arangle\nendalign\n\neqs_c = complete!(deepcopy(eqs));\nnothing # hide\n\nNow we assume that all atoms behave identically, but we want to obtain the equations for 20 different filter cavities. To this end we textttscale the Hilbert space of the atoms and textttevaluate the filter cavities. Specifying the Hilbert space is done with the kwarg texttth, which can either be the specific Hilbert space or its acts-on number. Evaluating the filter cavities requires a numeric upper bound for the used textttIndex, we provide this with a dictionary on the kwarg textttlimits.\n\nM_ = 20\neqs_sc = scale(eqs_c; h = [3])\neqs_eval = evaluate(eqs_sc; limits = Dict(M => M_))\nprintln(\"Number of eqs.: $(length(eqs_eval))\")\n\nTo calculate the dynamics of the system we create a system of ordinary differential equations, which can be used by DifferentialEquations.jl. Finally we need to define the numerical parameters and the initial state of the system.\n\nsys = System(eqs_eval; name = :sys)\nsys_c = mtkcompile(sys)\nnothing # hide\n\nu0 = zeros(ComplexF64, length(eqs_eval)) # Initial state\n\n\nN_ = 200 # System parameters\nΓ_ = 1.0\nΔ_ = 0Γ_\ng_ = 1Γ_\nκ_ = 100Γ_\nR_ = 10Γ_\nν_ = 1Γ_\n\ngf_ = 0.1Γ_\nκf_ = 0.1Γ_\nδ_ls = [0:(1 / M_):(1 - 1 / M_);] * 10Γ_\n\npmap = parameter_map(\n    eqs_eval, Dict(\n        Γ => Γ_, κ => κ_, g => g_, κf => κf_, gf => gf_, R => R_,\n        δ(i) => δ_ls,\n        Δ => Δ_, ν => ν_, N => N_,\n    )\n)\ndict = merge(initial_values(eqs_eval, u0), pmap)\nprob = ODEProblem(sys_c, dict, (0.0, 10.0 / κf_))\nnothing # hide\n\nsol = solve(prob, Tsit5(); abstol = 1.0e-10, reltol = 1.0e-10, maxiters = 1.0e7) # Solve the numeric problem\n\nt = sol.t\nn = abs.(get_solution(sol, a'a, eqs_eval).(t))\nn_b(i) = abs.(get_solution(sol, b(i)'b(i), eqs_eval).(t))\nn_f = [abs(get_solution(sol, b(i)'b(i), eqs_eval)(t[end])) for i in 1:M_] ./\n    (abs(get_solution(sol, b(1)'b(1), eqs_eval)(t[end])))\nnothing # hide\n\np1 = plot(t, n_b(1), alpha = 0.5, ylabel = \"⟨bᵢ⁺bᵢ⟩\", legend = false) # Plot results\nfor i in 2:M_\n    plot!(t, n_b(i), alpha = 0.5, legend = false)\nend\n\np2 = plot(\n    [-reverse(δ_ls); δ_ls],\n    [reverse(n_f); n_f],\n    xlabel = \"δ/Γ\",\n    ylabel = \"intensity\",\n    legend = false,\n)\nplot(p1, p2, layout = (1, 2), size = (700, 300))","title":"Laser with Filter Cavities"},{"category":"section","location":"examples/filter-cavity_indexed/#Package-versions","page":"Laser with Filter Cavities","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\", \"ModelingToolkitBase\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/many-atom-laser/#Many-atom-laser","page":"Many-atom laser","text":"This example describes a second order laser system consisting of N three-level atoms coupled to a single mode cavity. An auxiliary state 3rangle, which quickly decays into the upper lasing state 2rangle, is coherently pumped to achieve population inversion on the lasing transition 1rangle leftrightarrow 2rangle. The Hamiltonian of this system is given by\n\nH = -Delta_c a^dagger a  - sum_i=1^N left Delta_3^i sigma_i^33  + g_i (a^dagger sigma_i^12 + asigma_i^21) + Omega_i (sigma_i^31 + sigma_i^13) right\n\nIncluding dissipative processes as, e.g. the atomic decay or photon losses through the cavity mirrors, makes it an open quantum system. In the Schrödinger picture we would compute the dynamics of such open quantum systems with a density matrix rho according to a master equation (see e.g. https://docs.qojulia.org/),\n\nfracddt rho = - fracihbar left H rho right + mathcalLrho\n\nwith mathcalLrho = fracgamma2 (2 J rho J^dagger - J^dagger J rho - rho J^dagger J) the Liouvillian superoperator in standard Lindblad form for a dissipative process with jump operator J and rate R.\n\nWith QuantumCumulants.jl we describe the system dynamics with averages, which are deduced from the operator equations of motion in the Heisenberg picture. In the Heisenberg picture open systems are described by the quantum Langevin equation. Assuming white noise, we can omit the stochastic terms of the quantum Langevin equation when computing averages. Thus we get the following equation for the time evolution of a system operator average langle O rangle (if O is not explicitly time dependent):\n\nfracddt langle O rangle = fracihbar left H O right + barmathcalLO\n\nThe superoperator barmathcalLO is similar to the Lindblad term in the Schrödinger picture, except that J and J^dagger are swapped in the first term, i.e. barmathcalLO = fracgamma2 (2 J^dagger O J - J^dagger J O - O J^dagger J), for a dissipative process with jump operator J and rate R.\n\nFor our system we have four different dissipative processes with the jump operators a, sigma^12_i, sigma^13_i and sigma^23_i, and the corresponding decay rates kappa, Gamma^i_12, Gamma^i_13 and Gamma^i_23, respectively.\n\nWe start by loading the needed packages.\n\nusing QuantumCumulants\nusing ModelingToolkitBase, OrdinaryDiffEqTsit5\nusing Plots\n\nThen we define the symbolic parameters of the system, the Hilbertspace and the necessary operators. We define an atomic transition operator function sigma(ijk) for the transition from j rangle to i rangle of atom k. Since we only have one FockSpace we do not need to specify the Hilbertspace on which the Destroy operator acts. For the different atomic transitions, however, we need to specify this, since there is more than one NLevelSpace. This information is stored in the .aon field of each operator.\n\nN = 2 # number of atoms\n@variables κ g Γ23 Γ13 Γ12 Ω Δc Δ3\n\nhf = FockSpace(:cavity) # Hilbertspace\nha = ⊗([NLevelSpace(Symbol(:atom, i), 3) for i in 1:N]...)\nh = hf ⊗ ha\n\na = Destroy(h, :a) # Operators\nσ(i, j, k) = Transition(h, Symbol(\"σ_{$k}\"), i, j, k + 1)\nnothing # hide\n\nNow we create the Hamiltonian and the jumps with the corresponding rates of our laser system. We assume here that all atoms are identical.\n\nH =\n    -Δc * a'a +\n    sum(g * (a' * σ(1, 2, i) + a * σ(2, 1, i)) for i in 1:N) +\n    sum(Ω * (σ(3, 1, i) + σ(1, 3, i)) for i in 1:N) - sum(Δ3 * σ(3, 3, i) for i in 1:N) # Hamiltonian\n\nJ = [a; [σ(1, 2, i) for i in 1:N]; [σ(1, 3, i) for i in 1:N]; [σ(2, 3, i) for i in 1:N]] # Jumps\n\nrates = [κ; [Γ12 for i in 1:N]; [Γ13 for i in 1:N]; [Γ23 for i in 1:N]] # Rates\nnothing # hide\n\nLater we will complete the system automatically, which has the disadvantage that the equations are not ordered. Therefore we define a list of interesting operators, which we want to use later. Note that at least one operator(-product) is needed. We derive the equations for these operators, average them, and automatically complete the system of equations.\n\nops = [a'a, σ(2, 2, 1), σ(3, 3, 1)] # list of operators\n\neqs = meanfield(ops, H, J; rates = rates, order = 2) #second order average\nnothing # hide\n\nbeginalign\nfracddt langle a^dagger  arangle  = -10 i g left( langle a^dagger  sigma_1^12rangle  + langle a^dagger  sigma_2^12rangle  right) + 10 i g left( langle a  sigma_1^21rangle  + langle a  sigma_2^21rangle  right) -10 kappa langle a^dagger  arangle  \nfracddt langle sigma_1^22rangle  = langle sigma_1^33rangle  Gamma_23 -10 langle sigma_1^22rangle  Gamma_12 + 10 i g langle a^dagger  sigma_1^12rangle  -10 i g langle a  sigma_1^21rangle  \nfracddt langle sigma_1^33rangle  = -10 i langle sigma_1^31rangle  Omega + 10 i langle sigma_1^13rangle  Omega -10 langle sigma_1^33rangle  left( Gamma_13 + Gamma_23 right)\nendalign\n\ncomplete!(eqs) # automatically complete the system\nnothing # hide\n\nTo calculate the time evolution we create a Julia function which can be used by DifferentialEquations.jl to solve the set of ordinary differential equations.\n\nBuild a System out of the MeanfieldEquations\n\nsys = System(eqs; name = :laser)\nsys_c = mtkcompile(sys)\nnothing # hide\n\nFinally, we compute the time evolution after defining an initial state and numerical values for the parameters.\n\nu0 = initial_values(eqs) # initial state\n\nΓ12n = 1.0\nΓ23n = 20Γ12n\nΓ13n = 2Γ12n\nΩn = 5Γ13n\ngn = 2Γ12n\nΔcn = 0.0\nΔ3n = 0.0\nκn = 0.5Γ12n\n\nps = (g, Γ23, Γ13, Γ12, Ω, Δc, Δ3, κ) # list of parameters\np0 = Dict(ps .=> (gn, Γ23n, Γ13n, Γ12n, Ωn, Δcn, Δ3n, κn))\ntend = 10.0 / κn\n\nprob = ODEProblem(sys_c, merge(u0, p0), (0.0, tend))\nsol = solve(prob, Tsit5(), reltol = 1.0e-8, abstol = 1.0e-8)\nnothing # hide\n\nWe plot the average photon number and the population inversion of the lasing transition.\n\nn_t = real.(get_solution(sol, a' * a, eqs).(sol.t))\nσ22_t = real.(get_solution(sol, σ(2, 2, 1), eqs).(sol.t))\nσ33_t = real.(get_solution(sol, σ(3, 3, 1), eqs).(sol.t))\nσ22m11_t = 2 .* σ22_t .+ σ33_t .- 1 #σ11 + σ22 + σ33 = 𝟙\n\np1 = plot(sol.t, n_t, xlabel = \"tΓ₁₂\", ylabel = \"⟨a⁺a⟩\", legend = false) # Plot\np2 = plot(sol.t, σ22m11_t, xlabel = \"tΓ₁₂\", ylabel = \"⟨σ22⟩ - ⟨σ11⟩\", legend = false)\nplot(p1, p2, layout = (1, 2), size = (800, 300))","title":"Many-atom laser"},{"category":"section","location":"examples/many-atom-laser/#Package-versions","page":"Many-atom laser","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\", \"ModelingToolkitBase\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/excitation-transport-chain/#Noisy-excitation-transport","page":"Noisy excitation transport","text":"In this example, we will look at the energy transport in a one-dimensional chain of atoms, where only the first atom is driven. The excitation can then move from one atom to another via position-dependent dipolar interactions between nearest neighbors.\n\nThe Hamiltonian of this system reads\n\nH = -Delta sum_j sigma_j^ee + Omegaleft(sigma_1^eg + sigma_1^geright) + sum_j=1^N-1J(x_ix_j)left(sigma_j^egsigma_j+1^ge + sigma_j^gesigma_j+1^egright)\n\nThe first term describes the energy of the excited atomic state in the rotating frame of the driving laser with the detuning Delta. The second term describes the driving of the first atom with a laser with a Rabi frequency Omega. Finally, the interactions between nearest neighbors are included with the position-dependent interaction\n\nJ(x_ix_j) = fracJ_0x_i - x_j^3\n\nwhere J_0 is the strength of the interaction at unit distance. The positions of the atoms x_i are in units of the transition wavelength. Furthermore, each atom is subject to spontaneous emission at a rate gamma with the collapse operator sigma_j^ge.\n\nWe will start by simulating the system as described above. Specifically, we compute the efficiency of the excitation transport by looking at the excited state population of the atom at the end of the chain opposite to the driven atom. Finally, we show how one can generalize the system to include random fluctuations in the atomic position. We can then investigate the effect of the positional noise on the efficiency of the energy transport.\n\nAs always, we start by loading the packages we use and some basic definitions.\n\nusing QuantumCumulants\nusing ModelingToolkitBase, OrdinaryDiffEqLowOrderRK\nusing Plots\n\n\nN = 10 # Hilbert space for N atoms\nh = ⊗([NLevelSpace(Symbol(:atom, i), (:g, :e)) for i in 1:N]...)\n\nσ(i, j, k) = Transition(h, Symbol(:σ_, k), i, j, k) # Operators\n\n\n@variables Ω γ w Δ J0 # Define the symbolic parameters and the interaction\nx = [first(@variables $(Symbol(\"x_$i\"))) for i in 1:N]\nJ(xᵢ, xⱼ) = J0 / abs(xᵢ - xⱼ)^3\n\nH =\n    -Δ * sum(σ(:e, :e, k) for k in 1:N) +\n    Ω * (σ(:e, :g, 1) + σ(:g, :e, 1)) +\n    sum(\n    J(x[k], x[k + 1]) * (σ(:e, :g, k) * σ(:g, :e, k + 1) + σ(:g, :e, k) * σ(:e, :g, k + 1)) for\n        k in 1:(N - 1)\n)\n\nc_ops = [σ(:g, :e, k) for k in 1:N]\nnothing # hide\n\nThe above definitions are all we need to derive the set of equations. Here, we will include terms up to second order. Note, that in order to include noise, we will not need to make any adaptions on a symbolic level. Rather, we only need to derive the equations once and substitute the noisy positions accordingly when performing the numerical solutions.\n\neqs = meanfield(σ(:g, :e, 1), H, c_ops; rates = [γ for i in 1:N], order = 2) # Derive the equations to second order\ncomplete!(eqs)  # complete the set\n\nsys = System(eqs; name = :sys) # Generate the System\nsys_c = mtkcompile(sys)\nnothing # hide\n\nOnce we have our set of equations and converted it to an System we are ready to solve for the dynamics. First, let's have a look at the excitation transport for perfectly positioned atoms. We assume an equidistant chain, were neighboring atoms are separated by a distance d.\n\nd = 0.75 # Define parameters without noise\nx0 = [d * (k - 1) for k in 1:N]\np = Dict([γ => 1.0; Δ => 0.0; Ω => 2.0; J0 => 1.25; x .=> x0;])\n\n\nu0 = initial_values(eqs)  # initial state -- all atoms in the ground state\nprob = ODEProblem(sys_c, merge(u0, p), (0.0, 15.0)) # Create ODEProblem\n\nsol = solve(prob, RK4()) # Solve\n\npop1 = real.(get_solution(sol, σ(:e, :e, 1), eqs).(sol.t))\npopN = real.(get_solution(sol, σ(:e, :e, N), eqs).(sol.t))\ngraph = plot(\n    sol.t,\n    pop1,\n    label = \"Driven atom\",\n    xlabel = \"γt\",\n    ylabel = \"Excited state population\",\n) # Plot\nplot!(graph, sol.t, popN, label = \"End of chain\", leg = 1)\n\nAs you can see, the excitation transport is reasonably efficient, resulting in an excited state population at the end of the chain well above 10%.\n\nNow, we will introduce some noise in the atomic positions. In order to do so, we make use of the convenient ensemble simulation feature from the OrdinaryDiffEq library. For each trajectory, we recreate the original ODEProblem with a new set of atomic positions. We assume that each atom is located around the same point as before in the equidistant chain. However, we account for imperfect positioning in that the actual position of each atom is randomly modified. The strength of these position fluctuations is given by a parameter s, and the random fluctuations are sampled from a Gaussian distribution.\n\nEach trajectory we simulate can be thought of as one realization of an experiment, where the atoms are trapped with imperfect accuracy.\n\nIn the following, we define the function that sets up the new ODEProblem for a realization and solve a specified number of trajectories.\n\ns = d / 30  # strength of fluctuations\nfunction prob_func(prob, args...)\n    x_ = x0 .+ s .* randn(N) # Define the new set of parameters\n    p_ = Dict([γ => 1.0; Δ => 0.0; Ω => 2.0; J0 => 1.25; x .=> x_;])\n\n    return ODEProblem(sys_c, merge(u0, p_), (0.0, 15.0)) # Return new ODEProblem\nend\n\ntrajectories = 20\neprob = EnsembleProblem(prob, prob_func = prob_func)\nsim = solve(eprob, RK4(), EnsembleSerial(); trajectories)\nnothing # hide\n\nFinally, we average over the results and compare them against the results from before, where there was no noise in the atomic positioning.\n\ntspan = range(0.0, sol.t[end], length = 101)\npops_avg = zeros(length(tspan), N) # Average resulting excitations\nfor i in 1:N, j in 1:trajectories\n    pop = real.(get_solution(sim.u[j], σ(:e, :e, i), eqs).(tspan))\n    @. pops_avg[:, i] += pop / trajectories\nend\n\nplot!(graph, tspan, pops_avg[:, 1], color = :steelblue, ls = :dash, label = nothing)\nplot!(graph, tspan, pops_avg[:, N], color = :orange, ls = :dash, label = nothing)\n\nIn the above graph, the solid lines are the ones from before, where we did not include any position fluctuations. The dashed lines with the corresponding colors show the results when averaging over many noisy realizations.\n\nAs we can see, more of the excited state population remains in the driven atom at the beginning of the chain. The efficiency of the transport is somewhat reduced and only approximately 10% excited state population are reached in the atom at the other end.\n\nWe can also plot the results for each trajectory on top of the average. Let's look at each trajectory of the excited state population of the atom at the end of the chain.\n\ngraph2 = plot(xlabel = \"γt\", ylabel = \"Excitation at end of chain\")\nfor i in 1:trajectories\n    popN_i = real.(get_solution(sim.u[i], σ(:e, :e, N), eqs).(sim.u[i].t))\n    plot!(\n        graph2,\n        sim.u[i].t,\n        popN_i,\n        color = :steelblue,\n        label = nothing,\n    )\nend\nplot!(\n    graph2,\n    tspan,\n    pops_avg[:, N],\n    lw = 4,\n    label = \"Average over trajectories\",\n    color = :orange,\n)\n\nAs you can see, in some realizations the transport is almost not hindered at all. On average, however, the transport efficiency is reduced quite a bit.","title":"Noisy excitation transport"},{"category":"section","location":"examples/excitation-transport-chain/#Package-versions","page":"Noisy excitation transport","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqLowOrderRK\", \"ModelingToolkitBase\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"examples/pulsed_mollow_spectrum/#Mollow-Triplet-from-a-Pulsed-Drive","page":"Mollow Triplet from a Pulsed Drive","text":"The Mollow Triplet example computes the resonance fluorescence spectrum of a constantly driven atom. Here we drive the atom with a time-dependent field that is smoothly switched on, and compute the emission spectrum from the two-time correlation function once the atom has settled into a quasi-steady state. This showcases correlation functions of time-dependent Hamiltonians.\n\nThe atom obeys\n\nH(t) = Deltasigma^ee + r(t)Omega(sigma^ge + sigma^eg)\n\nwhere the dimensionless envelope r(t) ramps the Rabi drive from 0 to 1. The emission spectrum is the Fourier transform of the first-order correlation\n\ng(t_0tau) = langle sigma^eg(t_0+tau)sigma^ge(t_0)rangle\n\nBy the quantum regression theorem, with a time-dependent generator the tau-evolution is governed by H(t_0+tau), not H(tau): the drive keeps running during the correlation delay, starting from the absolute time t_0 at which the original evolution stopped. QuantumCumulants.jl handles this by substituting the original time variable t to t_0 + tau in the correlation equations. We pass t_0 via the iv0 keyword and supply its value alongside the other parameters.\n\nusing QuantumCumulants\nusing ModelingToolkitBase, OrdinaryDiffEqTsit5, OrdinaryDiffEqLowOrderRK\nusing QuantumOptics: timecorrelations\nusing Plots\n\nWe register the envelope r(t) as a time-dependent function. As in the Ramsey Spectroscopy example, we seed a meanfield call first so the envelope is built on the system's own independent variable t.\n\nh = NLevelSpace(:atom, (:g, :e))\nσ(i, j) = Transition(h, :σ, i, j)\n\n@variables Δ Ω γ\n@variables t0::Real   # absolute time at which the drive evolution stops (t₀)\n@register_symbolic r(t)\n\neqs_seed = meanfield([σ(:e, :e), σ(:e, :g)], Δ * σ(:e, :e), [σ(:g, :e)]; rates = [γ])\nt = eqs_seed.iv\n\nH = Δ * σ(:e, :e) + r(t) * Ω * (σ(:g, :e) + σ(:e, :g))\nJ = [σ(:g, :e)]\n\neqs = meanfield([σ(:e, :e), σ(:e, :g)], H, J; rates = [γ], iv = t)\ncomplete!(eqs)\n\nWe drive on resonance (Delta = 0) with Omega = 3gamma, switching the field on smoothly with r(t) = 1 - e^-tt_r so it has effectively saturated long before the stop time t_0.\n\nr(t) = 1 - exp(-t / 2.0)\n\nγv, Ωv, Δv = 1.0, 3.0, 0.0\nps = [γ, Ω, Δ]\np0 = [γv, Ωv, Δv]\n\nsys = mtkcompile(System(eqs; name = :atom))\nu0 = initial_values(eqs, zeros(ComplexF64, length(eqs)))\ntstop = 20.0\nprob = ODEProblem(sys, merge(u0, Dict(ps .=> p0)), (0.0, tstop))\nsol = solve(prob, Tsit5(); abstol = 1.0e-10, reltol = 1.0e-10)\n\nplot(\n    sol.t,\n    real.(get_solution(sol, σ(:e, :e), eqs).(sol.t)),\n    xlabel = \"γt\",\n    ylabel = \"⟨σᵉᵉ⟩\",\n    label = \"excited-state population\",\n    size = (600, 300),\n)\n\nThe population settles onto a plateau: at t0 = tstop the atom is in a quasi-steady state under the (now constant) drive. We build the emission correlation function on this system, passing iv0 = t0.\n\nc = CorrelationFunction(σ(:e, :g), σ(:g, :e), eqs; iv0 = t0)\nnothing # hide\n\nWithout iv0, a time-dependent system raises an informative error, since the correlation equations would otherwise contain the orphaned time variable t:\n\ntry\n    CorrelationFunction(σ(:e, :g), σ(:g, :e), eqs)\ncatch e\n    println(e isa ArgumentError ? \"ArgumentError: iv0 is required\" : e)\nend\n\nWe solve the tau-evolution. The steady-state initial values are read off the original solution with correlation_u0; the parameters, including t0, are propagated with correlation_p0.\n\ncsys = mtkcompile(System(c; name = :corr))\nu0_c = correlation_u0(c, sol.u[end])\np0_c = correlation_p0(c, sol.u[end], [γ => γv, Ω => Ωv, Δ => Δv, t0 => tstop])\n\nτ_end = 30.0\nprob_c = ODEProblem(csys, merge(u0_c, Dict(p0_c)), (0.0, τ_end))\nsol_c = solve(prob_c, Tsit5(); abstol = 1.0e-10, reltol = 1.0e-10, save_idxs = 1)\nnothing # hide\n\nThe spectrum is the Fourier transform of the inelastic (connected) part of the correlation, g(t_0tau) - langlesigma^gerangle^2, with the coherent (elastically scattered) contribution removed. We borrow the FFT helper from QuantumOptics.jl.\n\ncoh = abs2(get_solution(sol, σ(:g, :e), eqs)(tstop))   # |⟨σᵍᵉ⟩|² at t₀ (elastic peak)\nτ = collect(range(0.0, τ_end; length = 2001))          # equidistant grid for the FFT\ng_inel = sol_c.(τ) .- coh\nω, S = timecorrelations.correlation2spectrum(τ, g_inel)\n\nTo confirm the result, we compare against the textbook constant-drive Mollow triplet: the same atom driven by a time-independent field Omega, evolved to steady state, with its correlation computed the standard way (no iv0).\n\nH_const = Δ * σ(:e, :e) + Ω * (σ(:g, :e) + σ(:e, :g))\neqs_const = meanfield([σ(:e, :e), σ(:e, :g)], H_const, J; rates = [γ])\ncomplete!(eqs_const)\n\nsys_const = mtkcompile(System(eqs_const; name = :atom_const))\nsol_const = solve(\n    ODEProblem(sys_const, merge(initial_values(eqs_const, zeros(ComplexF64, 2)), Dict(ps .=> p0)), (0.0, 40.0)),\n    Tsit5(); abstol = 1.0e-10, reltol = 1.0e-10,\n)\n\nc_const = CorrelationFunction(σ(:e, :g), σ(:g, :e), eqs_const)\ncsys_const = mtkcompile(System(c_const; name = :corr_const))\nsol_cc = solve(\n    ODEProblem(\n        csys_const,\n        merge(\n            correlation_u0(c_const, sol_const.u[end]),\n            Dict(correlation_p0(c_const, sol_const.u[end], [γ => γv, Ω => Ωv, Δ => Δv]))\n        ),\n        (0.0, τ_end),\n    ),\n    Tsit5(); abstol = 1.0e-10, reltol = 1.0e-10, save_idxs = 1,\n)\ncoh_const = abs2(get_solution(sol_const, σ(:g, :e), eqs_const)(40.0))\n_, S_const = timecorrelations.correlation2spectrum(τ, sol_cc.(τ) .- coh_const)\n\nThe two spectra lie on top of each other: the pulsed drive, sampled from a plateau time t_0 via iv0, reproduces the steady-state Mollow triplet. The sidebands sit at omega approx pm 2Omega (the dressed-state splitting at resonance), flanking the central line at omega = 0.\n\nplot(ω, S; xlims = (-12, 12), xlabel = \"ω\", ylabel = \"S(ω)\", label = \"pulsed drive (iv0 = t₀)\", lw = 2, size = (600, 350))\nplot!(ω, S_const; xlims = (-12, 12), label = \"constant drive (steady state)\", ls = :dash, lw = 2)","title":"Mollow Triplet from a Pulsed Drive"},{"category":"section","location":"examples/pulsed_mollow_spectrum/#Package-versions","page":"Mollow Triplet from a Pulsed Drive","text":"These results were obtained using the following versions:\n\nusing InteractiveUtils\nversioninfo()\n\nusing Pkg\nPkg.status(\n    [\"QuantumCumulants\", \"OrdinaryDiffEqTsit5\", \"ModelingToolkitBase\", \"QuantumOptics\", \"Plots\"],\n    mode = PKGMODE_MANIFEST,\n)\n\n\n\nThis page was generated using Literate.jl.","title":"Package versions"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"The basic usage is probably best illustrated with a brief example. In the following, we solve a simple model for a single-atom laser.\n\nWe start by loading the package, defining some symbolic parameters and the photonic annihilation operator a as well as the atomic transition operator σ, which denotes a transition from level j to level i as σ(i,j). This allows us to quickly write down the Hamiltonian and the collapse operators of the system with their corresponding decay rates.\n\nusing Latexify # hide\nset_default(double_linebreak=true) # hide\nusing QuantumCumulants\n\n# Define parameters\n@variables Δ::Real g::Real γ::Real κ::Real ν::Real\n\n# Define hilbert space\nhf = FockSpace(:cavity)\nha = NLevelSpace(:atom, (:g, :e))\nh = hf ⊗ ha\n\n# Define the fundamental operators\n@qnumbers a::Destroy(h)\nσ(i, j) = Transition(h, :σ, i, j)\n\n# Hamiltonian\nH = Δ*a'*a + g*(a'*σ(:g, :e) + a*σ(:e, :g))\n\n# Collapse operators\nJ = [a, σ(:g, :e), σ(:e, :g)]\nrates = [κ, γ, ν]\nnothing # hide\n\nNow, we define a list of operators of which we want to compute the mean-field equations. We will only consider products of two operators. This is because later we will compute the dynamics of the system up to second order.\n\n# Derive a set of equations\nops = [a'*a, σ(:e, :e), a'*σ(:g, :e)]\neqs = meanfield(ops, H, J; rates=rates)\n\nTo obtain a closed set of equations, we expand higher-order products to second order.\n\n# Expand the above equations to second order\neqs_expanded = cumulant_expansion(eqs, 2)\n\nThe first-order contributions are always zero and can therefore be neglected. You can try adding a and σ(:g,:e) to the list of operators ops in order to see that yourself. Even more conveniently, complete automatically finds all missing averages and derives the corresponding equations, giving a closed system in one call.\n\n# Close the system by deriving equations for any missing averages\neqs_full = complete(eqs_expanded)\n\nFinally, we convert the MeanfieldEquations to a System from ModelingToolkitBase, which can be solved numerically with OrdinaryDiffEq.\n\n# Generate a ModelingToolkitBase System\nusing ModelingToolkitBase\nsys = mtkcompile(System(eqs_full; name=:laser))\n\n# Solve the system using the OrdinaryDiffEq package\nusing OrdinaryDiffEqTsit5\nu0 = zeros(ComplexF64, length(eqs_full.states))\np0 = [Δ => 0.0, g => 1.5, γ => 0.25, κ => 1.0, ν => 4.0]\nprob = ODEProblem(sys, merge(initial_values(eqs_full, u0), Dict(p0)), (0.0, 10.0))\nsol = solve(prob, Tsit5())\nnothing # hide\n\nNumeric trajectories of any operator-average are recovered with get_solution, which substitutes the symbolic average into the compiled solution and returns a callable in t:\n\nusing Plots\nts = range(0.0, 10.0; length=200)\nn  = real.(get_solution(sol, a'*a, eqs_full).(ts))\npe = real.(get_solution(sol, σ(:e, :e), eqs_full).(ts))\nplot(ts, n, label=\"Photon number\", xlabel=\"t\")\nplot!(ts, pe, label=\"Excited state population\")\nsavefig(\"tutorial.svg\") # hide\nnothing # hide\n\n(Image: Photon number and excited state population)","title":"Tutorial"},{"category":"section","location":"#QuantumCumulants.jl","page":"QuantumCumulants.jl","text":"(Image: ) (Image: ) (Image: Codecov) (Image: Benchmarks)\n\n(Image: Aqua QA) (Image: jet)\n\nQuantumCumulants.jl is a package for the symbolic derivation of mean-field equations for quantum mechanical operators in Julia. The equations are derived using fundamental commutation relations of operators. When averaging these equations they can be automatically expanded in terms of cumulants to an arbitrary order (generalized mean-field approximation). This results in a closed set of symbolic differential equations, which can also be solved numerically.\n\nFor the application of commutation relations QuantumCumulants.jl implements a simple noncommutative algebra, where any commutation relations are applied immediately. All other symbolic simplification and rewriting is done using the Symbolics.jl package.\n\nThe basic working principle boils down to the following steps:\n\nThe model (Hamiltonian) is specified.\nEquations of motion for average values are derived from the fundamental commutation relations of operators. The resulting equations are stored as symbolic equations using the Symbolics.jl framework, which also handles any additional simplification and rewriting.\nThe key step: the equations of motion for the averages are truncated at a specified order, neglecting higher-order quantum correlations via the generalized cumulant expansion method. This yields a closed set of c-number ordinary differential equations.\nFinally, the symbolic system of equations is turned into a System from ModelingToolkitBase.jl, which bridges the gap from symbolics to numerics. This makes it straightforward to obtain the time dynamics of a system within the DifferentialEquations.jl ecosystem.\n\nIf you only need the second quantized algebra, you can depend on SecondQuantizedAlgebra.jl directly.","title":"QuantumCumulants.jl"},{"category":"section","location":"#Relationship-to-SecondQuantizedAlgebra.jl","page":"QuantumCumulants.jl","text":"QuantumCumulants is the cumulant layer. The operator algebra it builds on (Hilbert spaces, operators, indices, symbolic sums, normal ordering, numeric conversion) lives in SecondQuantizedAlgebra.jl (SQA), which QuantumCumulants re-exports, so using QuantumCumulants gives you the full algebra surface. When you need the details of building a model (defining FockSpace/NLevelSpace, Destroy/Transition, Index/Σ), reach for SQA's documentation; the documentation here focuses on what QuantumCumulants adds on top: deriving mean-field equations, the cumulant expansion, completion, scaling, correlations, noise, and the bridge to a numerical solution.","title":"Relationship to SecondQuantizedAlgebra.jl"},{"category":"section","location":"#Installation","page":"QuantumCumulants.jl","text":"QuantumCumulants.jl is a registered Julia package and can be installed with the package manager:\n\npkg> add QuantumCumulants\n\nFor a full list of functions, see the API documentation.","title":"Installation"},{"category":"section","location":"#Short-example","page":"QuantumCumulants.jl","text":"To briefly illustrate how QuantumCumulants.jl works, here's how you can implement a first-order mean-field model of a laser with a single atom as a gain medium:\n\nusing QuantumCumulants\n\nh_cav = FockSpace(:cavity)\nh_atom = NLevelSpace(:atom, (:g,:e))\nh = tensor(h_cav, h_atom)\n\n@variables Δ::Real g::Real κ::Real γ::Real ν::Real\n@qnumbers a::Destroy(h)\nσ(i, j) = Transition(h, :σ, i, j)\n\nH = Δ*a'*a + g*(a'*σ(:g,:e) + a*σ(:e,:g))\nJ = [a,σ(:g,:e),σ(:e,:g)]\nrates = [κ,γ,ν]\n\neqs = meanfield([a,σ(:g,:e),σ(:e,:e)], H, J; rates=rates, order=1)\n\nusing ModelingToolkitBase, OrdinaryDiffEqLowOrderRK\nsys = mtkcompile(System(eqs; name=:laser))\np0 = [Δ=>0, g=>1.5, κ=>1, γ=>0.25, ν=>4]\nu0 = ComplexF64[1e-2, 0, 0]\nprob = ODEProblem(sys, merge(initial_values(eqs, u0), Dict(p0)), (0.0,50.0))\nsol = solve(prob,RK4())\n\nusing Plots\nts = range(0.0, 50.0; length=200)\nn = abs2.(get_solution(sol, a, eqs).(ts))\nplot(ts, n, xlabel=\"t\", label=\"n\")\n\n(Image: photon-number)\n\nThe above code implements the Jaynes-Cummings Hamiltonian describing an optical cavity mode that couples to a two-level atom. Additionally, the decay processes are specified. Then, mean-field equations for the average values of the operators [a,σ(:g,:e),σ(:e,:e)] are derived and expanded to first order (average values of products are factorized). For the numerical solution a System (from ModelingToolkitBase.jl) is created and solved with the OrdinaryDiffEq.jl library. Finally, the time dynamics of the photon number n is plotted.","title":"Short example"},{"category":"section","location":"#Citing","page":"QuantumCumulants.jl","text":"If you find QuantumCumulants.jl useful in your research, please consider citing this paper:\n\n@article{plankensteiner2022quantumcumulants,\n  doi = {10.22331/q-2022-01-04-617},\n  url = {https://doi.org/10.22331/q-2022-01-04-617},\n  title = {Quantum{C}umulants.jl: {A} {J}ulia framework for generalized mean-field equations in open quantum systems},\n  author = {Plankensteiner, David and Hotter, Christoph and Ritsch, Helmut},\n  journal = {{Quantum}},\n  issn = {2521-327X},\n  publisher = {{Verein zur F{\\\"{o}}rderung des Open Access Publizierens in den Quantenwissenschaften}},\n  volume = {6},\n  pages = {617},\n  month = jan,\n  year = {2022}\n}","title":"Citing"},{"category":"section","location":"implementation/#The-mean-field-pipeline","page":"The mean-field pipeline","text":"This page walks through the steps QuantumCumulants takes you through, from a Hamiltonian to a numerical solution: deriving the Heisenberg equations, truncating them with the cumulant expansion, closing the system, and handing it to a numerical solver.\n\nThe building blocks of a model (Hilbert spaces, operators, symbolic parameters, commutation relations) belong to SecondQuantizedAlgebra.jl (SQA), which QuantumCumulants re-exports. If you have not met them yet, read SQA's implementation guide first; this page assumes you can already build a Hamiltonian. As a one-line reminder, here is the driven cavity used throughout:\n\nusing QuantumCumulants\nh = FockSpace(:fock)\n@qnumbers a::Destroy(h)\n@variables ω::Real η::Real\nH = ω*a'*a + η*(a + a') # driven cavity Hamiltonian\nnothing # hide\n\nFockSpace, Destroy, @qnumbers and the symbolic scalar @variables all come from SQA / Symbolics. For many-body systems built from Index and Σ, see Indexed and scaled systems.","title":"The mean-field pipeline"},{"category":"section","location":"implementation/#Cumulant-expansion","page":"The mean-field pipeline","text":"Equations of motion are written for expectation values. SQA's average converts an operator product to a c-number, but a closed set of equations requires truncating at a specified order. The order of an average is the number of operator factors in the product:\n\nusing QuantumCumulants # hide\nh = FockSpace(:fock)\n@qnumbers a::Destroy(h)\n\nget_order(average(a))      # 1\nget_order(average(a'*a))   # 2\nnothing # hide\n\nThe cumulant_expansion expresses any average through averages up to a chosen order (see the theory section):\n\ncumulant_expansion(average(a'*a), 1)\nnothing # hide","title":"Cumulant expansion"},{"category":"section","location":"implementation/#Deriving-equations-of-motion","page":"The mean-field pipeline","text":"meanfield derives the Heisenberg (quantum Langevin) equations for a list of operators under a Hamiltonian H and, optionally, a list of collapse operators J with their rates. Each jump adds the Lindblad term\n\nsum_i r_i left( J_i^dagger mathcalO J_i - tfrac12J_i^dagger J_i mathcalO right)\n\nPass order to apply the cumulant_expansion immediately:\n\nusing Latexify # hide\nset_default(double_linebreak=true) # hide\nme = meanfield([a, a'*a], H; order=2)\n\nThe result is a MeanfieldEquations holding both the operator-level and the averaged equations. Returned right-hand sides are left unsimplified; call simplify! (or Symbolics.simplify) on what you want to inspect.\n\nA derived system is generally not closed: right-hand sides reference averages that are not yet on a left-hand side. find_missing lists them, and complete / complete! close the system by deriving an equation for each until the set is self-contained:\n\nme_complete = complete(me)\nnothing # hide\n\nFor opt-in measurement-backaction noise (efficiencies=...) and retrodiction (direction=Backward()), see Noise & measurement backaction. For permutation-symmetric many-body systems, see Indexed and scaled systems.","title":"Deriving equations of motion"},{"category":"section","location":"implementation/#Numerical-solution","page":"The mean-field pipeline","text":"A MeanfieldEquations is converted to a ModelingToolkitBase.System of ordinary differential equations, compiled with mtkcompile, and solved with OrdinaryDiffEq:\n\nusing ModelingToolkitBase\nsys = mtkcompile(System(me_complete; name=:cavity))\n\nusing OrdinaryDiffEqTsit5\nu0 = zeros(ComplexF64, length(me_complete.states))\np0 = [ω => 1.0, η => 0.1]\nprob = ODEProblem(sys, merge(initial_values(me_complete, u0), Dict(p0)), (0.0, 1.0))\nsol = solve(prob, Tsit5())\nnothing # hide\n\nget_solution substitutes a symbolic average into the compiled solution and returns a callable in t. It works both for averages on the left-hand side of the system and for derived products that are not:\n\nts  = range(0.0, 1.0; length=100)\na_t = get_solution(sol, a, me_complete).(ts)\nn_t = real.(get_solution(sol, a'*a, me_complete).(ts))\nnothing # hide","title":"Numerical solution"},{"category":"section","location":"implementation/#Computing-the-initial-state","page":"The mean-field pipeline","text":"A trivial u0 = zeros(...) is rarely the physical initial condition. initial_values computes the expectation values of every state from a numerical Ket or density operator, using SQA's numeric_average / to_numeric bridge to QuantumOpticsBase.jl. For example, a coherent initial state of the cavity:\n\nusing QuantumOpticsBase\nb = FockBasis(10)\npsi_0 = coherentstate(b, 0.3 + 0.4im)\nu0 = initial_values(me_complete, psi_0)\nnothing # hide\n\nMixed states work the same way by passing a density operator (initial_values(me, dm(psi_0))). The details of the symbolic-to-numeric conversion (to_numeric, numeric_average, NLevelSpace level maps, lazy tensor products for large systems) are documented in SQA's numeric conversion section.","title":"Computing the initial state"},{"category":"section","location":"implementation/#Extending-the-operator-algebra","page":"The mean-field pipeline","text":"Adding a new operator type (a custom QSym with its commutation hooks) is an SQA concern, not a QuantumCumulants one. SQA's implementation guide documents the QSym interface and walks through a worked example; once a type is defined there, meanfield and the rest of this pipeline handle it automatically.","title":"Extending the operator algebra"}]
}
